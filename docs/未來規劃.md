# Cloud Alarm Overlay 未來規劃（構想參考）

最後更新：2026-09-24　目前版本：**v1.2.0**

這份文件整理可以考慮新增或優化的功能構想，供維護者參考，**不代表已決定要做**。每項附上現況、構想與注意事項；決定採納後，再到[問題與優化追蹤](問題與優化追蹤.md)建立 CAO 編號並排入版本。

「現況」依 2026-09-24 的原始碼核對；工作量為粗估（小：1 天內，中：數天，大：一週以上）。

## 推薦優先順序

整體來看，最值得先做的是下列五項：投入小，而且直接改善日常使用或減少維護負擔。

1. **忙碌中稍後提醒**：CAO-018 已完成原始碼實作，待正式版本驗收。
2. **錯過提醒摘要**：解決關機、休眠時漏看通知的問題。
3. **每月農曆改用內建曆法**：省去每年維護 `LunarCalendar` 分頁。
4. **任務產生器離線化**：避免 CDN 版本變動或斷網造成表單壞掉。
5. **GitHub Actions 自動測試**：讓 307 項測試在每次推送時自動跑。

## 總覽

| 構想 | 類型 | 效益 | 工作量 |
| --- | --- | --- | --- |
| [忙碌中稍後提醒](#忙碌中稍後提醒) ✅ 已實作 CAO-018（未發布） | 新增 | 高 | 小～中 |
| [首頁天氣（今天與明天）](#首頁天氣今天與明天) ✅ 已實作 CAO-019（未發布） | 新增 | 中 | 中 |
| [本機任務 CSV 匯入](#本機任務-csv-匯入) ✅ 已實作 CAO-020（未發布） | 新增 | 中 | 中 |
| [倒數卡片分享圖](#倒數卡片分享圖) ✅ 已實作 CAO-021 | 新增 | 低～中 | 小～中 |
| [部署時預載設定](#部署時預載設定) ✅ 已採納 | 新增 | 中（公司部署） | 小～中 |
| [集中設定下發](#集中設定下發) ✅ 已採納 | 新增 | 高（公司部署） | 中 |
| [番茄鐘背景音](#番茄鐘背景音) ✅ 已採納 | 新增 | 低～中 | 小～中 |
| [訂閱外部行事曆](#訂閱外部行事曆) ✅ 已採納 | 新增 | 中 | 中 |
| [錯過提醒摘要](#錯過提醒摘要) | 新增 | 高 | 小～中 |
| [勿擾／專注模式](#勿擾專注模式) | 新增 | 中 | 中 |
| [多螢幕顯示緊急與強制通知](#多螢幕顯示緊急與強制通知) | 新增 | 中 | 小 |
| [確認回報集中查看](#確認回報集中查看) | 新增 | 高（公司部署） | 大 |
| [程式內自動下載更新](#程式內自動下載更新) | 新增 | 中 | 中 |
| [快速新增與任務範本](#快速新增與任務範本) | 新增 | 中 | 中 |
| [行事曆（ICS）匯出](#行事曆ics匯出) | 新增 | 低～中 | 小～中 |
| [番茄鐘綁定任務](#番茄鐘綁定任務) | 新增 | 低～中 | 中 |
| [每月農曆改用內建曆法](#每月農曆改用內建曆法) | 優化 | 高 | 中 |
| [任務產生器離線化](#任務產生器離線化) | 優化 | 中 | 小～中 |
| [GitHub Actions 自動測試](#github-actions-自動測試) | 優化 | 中 | 小 |
| [日誌自動清理](#日誌自動清理) | 優化 | 低～中 | 小 |
| [自動備份排程](#自動備份排程) | 優化 | 中 | 小～中 |
| [程式碼簽章](#程式碼簽章) | 優化 | 中 | 小（需費用） |
| [移除未使用的占位服務](#移除未使用的占位服務) | 優化 | 低 | 小 |
| [首次登入強制變更預設管理者密碼](#首次登入強制變更預設管理者密碼) | 優化 | 中 | 小～中 |

## 新增功能構想

### 忙碌中稍後提醒

> ✅ CAO-018 已實作（未發布）。一般、重要、緊急與強制任務可延後 5／10／15／30 分鐘，單次上限 3 次；管理者可關閉緊急提醒延後。

- **已實作**：提醒視窗提供「忙碌中，稍後提醒」；延後次數與最後確認時間納入歷史和 CSV，重開程式不補跳。倒數通知與番茄鐘不在本次任務延後範圍。
- **構想**：上班忙碌、一時無法處理時，按「忙碌中，稍後提醒」延後 5～30 分鐘再通知；用詞避免「貪睡」，強調是工作中暫緩處理。
- **注意**：強制通知可直接延後、不需輸入確認碼，正式確認仍需驗證碼；設定延後次數上限，管理者可關閉緊急提醒的延後。

### 首頁天氣（今天與明天）

> ✅ CAO-019 已實作（未發布）。設定頁支援城市搜尋、個人地點、公司預設地點與關閉；首頁顯示現在天氣和明天預報。

- **已實作**：首頁資訊列新增天氣，窄視窗自動換行；現在降雨使用 API 的毫米數，明天使用降雨機率，避免混淆兩種單位。
- **構想**：在資訊列加上第五欄「天氣」，顯示所在地現在的天氣與明天預報，辦公室和在家都適用。

**顯示方式**

```text
國曆日期        農曆日期    現在時間    節氣    天氣（台北市）
2026/09/24 週四  八月十四   14:05:32   -      ⛅ 現在 29°  降雨 20%
                                              🌧️ 明天 24–31°  降雨 60%
```

- 滑鼠停留顯示詳細資訊：天氣描述、體感溫度、更新時間與資料來源。
- 窄視窗時資訊列換行，天氣欄移到第二列。
- 尚未設定地點時顯示「設定天氣地點」，點擊前往設定頁。

**資料來源（建議 Open-Meteo）**

| 項目 | Open-Meteo（建議） | 中央氣象署開放資料（替代） |
| --- | --- | --- |
| 性質 | 開源（可自架），免註冊、免金鑰 | 政府開放資料，需免費註冊授權碼 |
| 符合需求 | `current` 取現在天氣，`daily` + `forecast_days=2` 取今天與明天 | 「一般天氣預報－今明 36 小時」依縣市提供，描述為中文（如「晴時多雲」） |
| 授權 | 資料採 CC BY 4.0，須標示來源；免費 API 限非商業用途，公司部署需確認是否適用或改用付費方案／自架 | 政府資料開放授權條款，可商業使用，須標示來源 |
| 注意 | 天氣代碼（WMO code）需自行對應中文與圖示 | 授權碼須由使用者或管理者自行申請填入，不可寫入公開原始碼 |

建議第一版先用 Open-Meteo：不用設定金鑰，個人安裝即可使用。若公司部署在意授權或需要中文官方描述，再加上氣象署作為第二來源。

Open-Meteo 查詢範例（以台北為例）：

```text
https://api.open-meteo.com/v1/forecast?latitude=25.05&longitude=121.53
  &current=temperature_2m,apparent_temperature,weather_code,precipitation
  &daily=weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max
  &timezone=Asia/Taipei&forecast_days=2
```

**地點設定**

- 在「設定」新增「天氣」：輸入城市名稱，以 Open-Meteo Geocoding API 搜尋，選取後儲存名稱與經緯度。
- 不使用 IP 自動定位，避免蒐集位置資訊；管理者可設定公司預設地點，使用者可改為自己的地點。
- 可完全關閉天氣欄，關閉後不發出任何天氣連線。

**更新與失敗處理**

- 啟動時、電腦喚醒後各更新一次，之後每 30 分鐘更新；逾時 10 秒即放棄，下次再試。
- 保存最後一次成功的結果；離線或失敗時以灰色顯示舊資料與「上次更新 HH:mm」，超過 6 小時改顯示「天氣資料暫不可用」。
- 天氣服務與提醒、同步完全隔離：天氣失敗不影響提醒觸發，也不寫入同步紀錄或系統異常狀態。

**架構對應**

| 層 | 新增內容 |
| --- | --- |
| Core | `WeatherSnapshot` 模型、`IWeatherService` 介面、WMO 天氣代碼對應中文描述 |
| Infrastructure | `OpenMeteoWeatherClient`（HTTP 呼叫與 JSON 解析） |
| BackgroundServices | `WeatherWorker`（定時更新、喚醒後更新） |
| Data | 天氣設定與最後結果存入既有設定儲存；若需新資料表，再新增遷移檔 |
| App | 資訊列天氣欄、設定頁「天氣」區塊、天氣圖示（可沿用 `EmojiGlyph`） |

**延伸（可選）**

- 明天降雨機率 ≥ 60% 時，下班前以一般提醒顯示「明天可能下雨，記得帶傘」。預設關閉，只作為本機提醒，由使用者自行開啟。

**注意事項**

- 公司網路若有防火牆，需允許連線至 `api.open-meteo.com`、`geocoding-api.open-meteo.com`。
- 設定頁與提示中須標示「天氣資料來源：Open-Meteo.com（CC BY 4.0）」。
- 測試以固定的 JSON 回應模擬，不在自動測試中呼叫真實 API。

### 本機任務 CSV 匯入

> ✅ CAO-020 已實作（未發布）。支援選檔預覽、逐列勾選、重複編號略過／換新編號、匯出與下載兩筆範例。

- **已實作**：「我的任務」新增 CSV 匯入／匯出與範本；任務產生器新增 CSV 匯出。UTF-8 含 BOM 可直接匯入，Big5 會提示改存 UTF-8。
- **構想**：在「我的任務」新增「匯入 CSV」，一次建立多筆本機任務，例如整年的繳費日、換電腦時搬移任務，或把任務產生器匯出的資料轉成本機任務。同時加上「匯出本機任務 CSV」，匯出格式與匯入相同，方便搬移與批次修改後再匯入。
- **格式**：沿用 Sheet `Tasks` 分頁的欄位（第 1 列英文標題、第 2 列中文說明），可直接重用 `CsvSheetParser.ParseTasks`；任務產生器匯出的檔案也能直接匯入。`TargetDeviceOrName`、`ExcludeDeviceOrName` 對本機任務無意義，匯入時忽略。
- **流程**：選檔 → 預覽清單（標示有效、錯誤原因、與既有任務重複）→ 勾選要匯入的列 → 確認建立；全部匯入後顯示成功與略過筆數。
- **注意**：
  - 匯入的任務一律是本機任務，不影響雲端任務。
  - 單次任務時間已過的列標示錯誤並略過；重複任務依規則計算下一次提醒（與 CAO-006 一致）。
  - `Id` 與既有本機任務重複時，可選擇略過或改用新編號，不覆蓋既有任務。
  - 支援 UTF-8（含 BOM）；Excel 另存的 Big5 CSV 需偵測或提示改存 UTF-8。
  - 提供「下載範本 CSV」按鈕，範本含兩列範例。

### 倒數卡片分享圖

> ✅ CAO-021 已實作，待實機驗收；尚未發布。

- **現況**：倒數／正數卡片、資料表操作列及首頁倒數釘選卡已提供「分享圖片」。
- **構想**：在倒數卡片的操作選單加上「分享圖片」，產生一張可傳到 LINE 或社群的圖片，例如「距離日本旅行還有 30 天」、「我們在一起 1,000 天」。
- **內容**：事件名稱、剩餘／已過天數（沿用卡片的顯示方式：天數、週＋天、月＋天、年＋月＋天）、目標／起始日期、分類圖示；右下角小字標示程式名稱，可在設定中關閉。
- **輸出**：
  - 「複製到剪貼簿」可直接貼到 LINE、Teams；「另存 PNG」存檔。
  - 尺寸可選 **1:1 正方形 1080×1080** 與 **2:3 直式 1080×1620**，預覽與輸出保持所選比例。
  - 使用目前的色彩風格與淺色／暗色，並提供幾種背景樣式。
- **實作方向**：WPF 以 `RenderTargetBitmap` 將專用的分享版面繪製成點陣圖，再以 `PngBitmapEncoder` 輸出；版面與首頁卡片分開，避免受視窗大小影響。
- **注意**：
  - 圖片以產生當下的數字為準；時間型倒數顯示到「天」即可，避免秒數立即過時。
  - 備註內容預設不放進圖片，避免無意間分享私人資訊。
  - 以高 DPI 輸出，確保文字與 emoji 清晰。

### 部署時預載設定

> ✅ 已採納並改為「管理員 JSON 設定匯入」，原始碼已實作，尚未重新封裝。

- 管理員登入後，在「同步來源」下載範本、選取 JSON，驗證成功即一次儲存，省去逐欄填寫。
- 支援 Sheet A／B、同步間隔、更新網址、連結鎖定、稍後提醒政策、閃爍／靜音政策與公司預設天氣地點。
- 未提供的設定群組保留原值；錯誤檔案整份拒絕，寫入使用交易，已鎖定的同步來源須先手動解鎖。
- 不含管理者密碼或裝置身分；不提供安裝器 `/CONFIG` 或首次啟動自動套用。
- JSON 是明文、需由管理者妥善保管；不複製來源檔，新增稽核紀錄遮蔽 Sheet ID 與更新網址。
- [操作方式與欄位說明](管理員JSON設定匯入.md)。

### 集中設定下發

> ✅ 已採納，尚未登記 CAO 編號。

- **現況**：通知政策（閃爍時間、安靜模式及鎖定）、同步連結鎖定等設定存在各台電腦，IT 要調整時須逐台登入管理者專區修改。
- **構想**：Sheet A 新增 `Settings` 分頁，程式同步時一併讀取並套用，IT 改一次即生效於全公司。

| Key | Value | Locked | Note |
| --- | --- | --- | --- |
| `SyncIntervalSeconds` | `45` | `TRUE` | 同步間隔（30–60） |
| `FlashMilliseconds` | `800` | `TRUE` | 提醒閃爍時間 |
| `SnoozeMaxCount` | `3` | `TRUE` | 稍後提醒上限（CAO-018） |
| `WeatherDefaultLocation` | `台北市` | `FALSE` | 天氣預設地點（CAO-019） |

- **規則**：
  - `Locked=TRUE`：強制套用，使用者與本機管理者都不能修改，介面沿用現有的鎖定圖示並顯示「由公司設定」。
  - `Locked=FALSE`：作為預設值，使用者可自行調整。
  - 只接受白名單內的 Key 與合法範圍；不認得或不合法的值略過並寫入同步紀錄。
  - 同步失敗時保留上次成功套用的設定。
- **注意**：
  - Sheet A 以公開連結讀取，**不可放密碼、金鑰或任何機密**；結束程式密碼、管理者密碼不納入下發。
  - 與既有的本機鎖定設定合併時，以 Sheet 鎖定優先；需在文件說明優先順序。
  - 舊版程式會忽略 `Settings` 分頁，不影響既有同步；`Sheet範例/` 需新增範例 CSV 與欄位說明。

### 番茄鐘背景音

> ✅ 已採納，尚未登記 CAO 編號。

- **現況**：番茄鐘只有階段結束的提示音（`SoundService` 以 Windows `PlaySound` 播放單次音效），沒有專注時的背景音。
- **構想**：番茄鐘設定新增「背景音」，專注期間循環播放，幫助進入專注狀態。
- **音源**：
  - 白噪音、粉紅噪音、棕噪音：由程式即時產生，不需音檔、沒有授權問題，也不增加安裝檔大小。
  - 雨聲、咖啡廳、森林等環境音：採用 CC0 等可商用授權素材，隨安裝檔附上，並比照 Emoji 素材附授權聲明。
- **操作**：選擇音源、音量；可選「僅專注時播放」或「休息時也播放」；開始、暫停、結束時淡入淡出。
- **注意**：
  - 背景音改用 WPF `MediaPlayer` 或音訊串流播放，才能循環與調整音量；與現有提示音分開。
  - 任何提醒彈出時自動降低背景音量或暫停，避免蓋過提醒音效；確認後恢復。
  - 預設關閉；程式結束或番茄鐘停止時必定停止播放。

### 訂閱外部行事曆

> ✅ 已採納，尚未登記 CAO 編號。

- **現況**：公司或個人行事曆（Google 日曆、Outlook）的會議與行程，須在本程式重複建立才能收到提醒。
- **構想**：在設定新增「行事曆訂閱」，貼上 ICS 訂閱網址（Google 日曆的「iCal 格式私人網址」、Outlook 的「發佈行事曆」連結），程式定期讀取，自動在事件前提醒。
- **設定項目**：訂閱名稱、網址、提前提醒時間（例如 10 分鐘前）、提醒等級（一般或重要）、全天事件是否提醒、啟用／停用。
- **顯示**：行事曆事件在「我的任務」與首頁以「行事曆」來源標示，唯讀不可編輯；可依訂閱整批停用。
- **更新**：每 15–30 分鐘重新讀取；事件新增、修改、刪除會同步反映。讀取失敗時保留上次結果，並在側邊欄狀態顯示該訂閱異常。
- **注意**：
  - ICS 需處理重複規則（RRULE）、例外日期（EXDATE）、時區與全天事件，建議使用成熟套件（例如 MIT 授權的 Ical.Net），新增套件須更新 `packages.lock.json`。
  - 私人 iCal 網址等同存取權，只存在本機；介面與日誌中遮蔽顯示，備份時提示是否包含。
  - 只讀取、不寫回外部行事曆。
  - 與既有的「行事曆（ICS）匯出」構想方向相反，可共用 ICS 解析與產生元件。

### 錯過提醒摘要

- **現況**：關機或休眠期間錯過的通知只寫入歷史紀錄，不會補跳。
- **構想**：開機或喚醒後，在系統匣或首頁顯示一則「你錯過了 N 則提醒」的摘要，列出清單，可逐筆確認或一次標記已讀。
- **注意**：只摘要、不補跳個別通知，避免開機時一次跳出一大堆視窗。

### 勿擾／專注模式

- **現況**：番茄鐘專注期間，一般提醒照常彈出。
- **構想**：番茄鐘專注中或自訂時段（例如午休、會議）時，暫時保留一般提醒，結束後合併顯示；緊急以上等級照常通知。
- **注意**：預設關閉，並在側邊欄狀態清楚顯示勿擾中，以免使用者誤以為程式沒在運作。

### 多螢幕顯示緊急與強制通知

- **現況**：`AlarmWindow` 依主螢幕尺寸定位，緊急與強制通知只出現在主螢幕。
- **構想**：強制通知在每個螢幕都顯示遮罩，確認碼輸入框放在主螢幕；緊急通知可選擇顯示在滑鼠所在的螢幕。
- **注意**：需驗證不同 DPI 縮放的多螢幕組合。

### 確認回報集中查看

- **現況**：確認紀錄只存在各台電腦，管理者無法集中看到誰已確認強制通知。
- **構想**：用 Google Apps Script 網頁應用程式或 Google 表單接收確認結果，寫入 Sheet C，讓管理者看到每則通知的完成率。
- **注意**：這會改變目前「程式不回寫雲端」的設計原則，需要評估隱私、公司政策與網路失敗重送機制，建議預設關閉。

### 程式內自動下載更新

- **現況**：程式讀取 `version.json` 檢查版本，下載則交給瀏覽器，使用者要自己執行安裝檔。
- **構想**：程式直接下載安裝檔，比對 `version.json` 中的 SHA-256，驗證無誤後詢問是否立即安裝。
- **注意**：雜湊不符時必須拒絕執行；下載來源限定 GitHub Releases 網域。

### 快速新增與任務範本

- **現況**：新增任務須開啟主視窗並填寫完整表單。
- **構想**：加上全域快捷鍵，開啟精簡的快速新增視窗；常用任務（例如每月報表、週會）可存成範本，一鍵套用。
- **注意**：快捷鍵要能自訂，並偵測是否與其他程式衝突。

### 行事曆（ICS）匯出

- **構想**：將本機任務與倒數事件匯出為 `.ics`，匯入 Outlook 或 Google 日曆。
- **注意**：農曆與「假日略過」規則無法完整轉成 ICS 的重複規則，可改為展開未來一年的個別日期。

### 番茄鐘綁定任務

- **構想**：開始番茄鐘時可選擇對應的任務，統計每個任務累積的專注時間，歷史紀錄可依任務篩選。

## 優化構想

### 每月農曆改用內建曆法

- **現況**：「指定農曆月日」已使用 .NET 內建的 `ChineseLunisolarCalendar` 換算，「每月農曆日期」仍依賴 Sheet A 的 `LunarCalendar` 分頁，每年必須補資料（現有資料只到 2027-12-31）。
- **構想**：每月農曆也改用內建曆法計算，`LunarCalendar` 分頁只保留節氣顯示，或連節氣一併改為內建計算。
- **注意**：切換前先用 2026–2027 的 730 筆資料逐日比對，確認內建結果一致；假日資料仍需每年更新。

### 任務產生器離線化

- **現況**：`task_builder_tailwind.html` 從 CDN 載入 Tailwind（play CDN）、SheetJS 與 lucide；其中 lucide 使用 `@latest`，未鎖定版本，網路中斷或新版不相容時，表單可能無法使用。
- **構想**：建置時預先產生 Tailwind CSS，並將 SheetJS 與鎖定版本的 lucide 一併打包進安裝檔，由本機服務提供。
- **注意**：打包後安裝檔略為變大；更新相依套件時需重新驗證匯出 Excel 功能。

### GitHub Actions 自動測試

- **現況**：儲存庫沒有 CI，測試靠手動執行。
- **構想**：推送或建立 PR 時，在 `windows-latest` 執行 `dotnet test`；另可在建立標籤時自動建置發布檔。
- **注意**：WPF 測試需要 Windows runner；截圖類測試可能要另外處理。

### 日誌自動清理

- **現況**：`logs\runtime-yyyyMMdd.log` 每天新增一個檔案，沒有自動刪除舊檔。
- **構想**：預設保留 30 天，可在管理者專區調整。

### 自動備份排程

- **現況**：備份須由管理者手動建立。
- **構想**：每週或每次升級前，自動在本機建立 `.calbak`，保留最近 N 份；升級安裝前先備份，降低資料庫遷移失敗的風險。

### 程式碼簽章

- **現況**：安裝檔沒有數位簽章，安裝時會出現 SmartScreen 警告，因此另外寫了[安裝教學](安裝教學.md)說明。
- **構想**：購買程式碼簽章憑證，在 `build-installer.ps1` 中簽署主程式與安裝檔。
- **注意**：需要年費；一般憑證仍須累積下載信譽，警告才會逐漸消失。

### 移除未使用的占位服務

- **現況**：`HolidayService`、`LunarScheduleService`、`OverdueGraceService` 是 Milestone 0 占位實作，呼叫即拋例外，仍在 DI 註冊但沒有程式使用；實際判斷已在 `TaskSchedulingService`。
- **構想**：移除三個類別、介面與註冊，減少誤用風險。

### 首次登入強制變更預設管理者密碼

- **現況**：新資料庫預設帳密 `admin`／`12345`，且公開於 README，只提醒使用者自行變更。
- **構想**：以預設帳密登入時，須先設定 8–128 字的新密碼才能進入管理者專區；已變更密碼的電腦不受影響。

## 例行維護

- **2028 年假日與農曆資料**：現有資料只到 2027-12-31，須於 2027 年底前更新 `Sheet範例/` 與正式 Sheet A（採納「每月農曆改用內建曆法」後只需更新假日）。
- **網站展示圖**：`site-assets/screenshots/` 尚未換成共用釘選與精簡圖釘畫面。

## 已登記的待辦

以下項目已在[問題與優化追蹤](問題與優化追蹤.md)建立紀錄，不在此重複說明：

- CAO-018 忙碌中稍後提醒。
- CAO-019 首頁天氣（現在與明天）。
