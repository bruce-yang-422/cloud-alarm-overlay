# Cloud Alarm Overlay 前端 UI 規劃書

本文件為 [cloud_alarm_overlay_規劃書.md](./cloud_alarm_overlay_規劃書.md) 的前端延伸，聚焦於畫面清單、每個畫面的區塊配置、狀態變化與互動行為，供 WPF / XAML 實作時參考。細節（像素尺寸、XAML 結構）留待實作階段決定。

每個畫面除了線框圖外，另補上兩張表：**元件清單**（該畫面有哪些控制項、類型、預設值、驗證規則）與**使用邏輯**（操作流程、狀態轉換、邊界情況／錯誤處理），供實作與測試時對照。

---

## 0. 設計基準

沿用主規劃書 34 節的視覺方向，前端所有畫面共用同一套基準。

```text
風格            Windows 11 / Fluent Design
主題模式        深色 / 淺色 / 跟隨系統（見主規劃書 34.1 節），預設跟隨系統
主視窗          單一 MainWindow + 左側導覽 + 右側內容區
全域字體        系統預設（Segoe UI Variable），標題 18-20px / 內文 13-14px / 輔助文字 11-12px
圓角            Card 8px / Button 6px / 便利貼 10px
陰影            Card 與便利貼皆帶淺陰影，強化層次感（Fluent Acrylic 風格可選配）
```

配色（沿用主規劃書 34 節，兩套主題皆須實作，非僅深色）：

```text
                深色（Dark）      淺色（Light）
背景            #0F1722          #F5F7FA
Card            #182433          #FFFFFF
Border          #30445A          #DDE3EC
Primary         #1683FF          #0F6FDE
Success         #37D67A          #1E9A5A
Warning         #FFC928          #B87700
Danger          #FF3B30          #D6362C
文字（主要）     #EAF1FB          #1B2430
文字（次要）     #93A6BF          #5B6B80
```

四個通知等級與顏色的對應關係（貫穿全部畫面的標籤色，兩套主題各自使用對應色值）：

| 等級 | 標籤色 | 意涵 |
| --- | --- | --- |
| 低級 | 灰藍（Border 色系） | 低急迫性 |
| 中級 | Primary 藍 | 需留意 |
| 高級 | Warning（深色用亮黃、淺色用琥珀色，確保對比） | 重要 |
| 最高級 | Danger 紅 | 強制 / 高風險 |

### 0.1 主題切換元件（設定頁）

對應主規劃書 34.1 節，位於設定頁「一般」區塊（見 6 節）：

```text
外觀模式   ○ 深色   ○ 淺色   ● 跟隨系統
```

```text
選擇「跟隨系統」：程式監聽 Windows 系統主題變更事件，即時套用對應主題，不需重啟程式
選擇「深色」或「淺色」：固定使用該主題，忽略系統設定
切換時全畫面即時套用新主題（不需切換分頁或重開視窗），包含當下正在顯示的通知視窗（低/中/高/最高級皆適用）
```

---

## 1. 畫面總覽

```text
主視窗（MainWindow）
├── 首頁 Dashboard
├── 我的任務 Tasks
├── 歷史紀錄 History
├── 番茄鐘 Pomodoro（獨立小工具，與任務提醒系統無關，見 6A 節）
├── 設定 Settings
└── 管理者頁 Admin（需先通過登入 Dialog，見 2.1；單一身分，僅管本機設定）

獨立視窗 / 疊層
├── 首次使用設定視窗 FirstRunSetup（僅首次啟動出現，見 6.1）
├── 通知－低級（便利貼，免確認）
├── 通知－中級（便利貼，需確認）
├── 通知－高級（60% 螢幕）
├── 通知－最高級（全螢幕）
├── 新增 / 編輯任務 Dialog
├── 管理者登入 Dialog
└── 更新提示 Dialog

系統匣
└── System Tray 選單
```

---

## 2. 主視窗外殼（MainWindow Shell）

一般使用者、尚未登入管理者身分時的預設外觀。「管理者專區」為**常駐可見**的單一導覽項目，點擊後才要求輸入密碼：

```text
┌──────────────────────────────────────────────────────────┐
│ ☰ Cloud Alarm Overlay                          🔔  ─  □  ✕│
├───────────┬──────────────────────────────────────────────┤
│           │                                              │
│  🏠 首頁    │                                              │
│  📋 我的任務 │              （對應頁面內容區）                │
│  🕘 歷史紀錄 │                                              │
│  🍅 番茄鐘   │                                              │
│  ⚙ 設定    │                                              │
│  🔒 管理者專區│                                            │
│           │                                              │
│  ● 同步中   │                                              │
└───────────┴──────────────────────────────────────────────┘
```

```text
左側導覽：垂直圖示 + 文字，選中項目以 Primary 色底線 / 底色標示
「管理者專區」項目對所有人可見，左側不展開管理者子選單
左下角常駐「同步狀態燈」：綠點=正常 / 黃點=同步中 / 紅點=失敗（詳細同步狀態於管理者頁檢視，見 8.1）
關閉主視窗（✕）：依設定頁「關閉時縮小至 Tray」決定是真的關閉還是隱藏到系統匣
```

### 2.1 管理者入口（常駐可見，點擊即要求登入）

對應主規劃書 15、19 節「管理員與 IT 合併為單一管理者身分」的決定。導覽入口本身不隱藏，任何人都看得到「管理者專區」，但未登入時點擊必須通過 7 節的登入 Dialog 才能進入。管理者頁面**只處理這台電腦本身的技術設定**，不包含任何全公司範圍的資料（公司政策、Sheet A／Employees 通知上限例外等一律改到 Google Sheet 上直接編輯，見主規劃書 19B 節）：

```text
點擊「🔒 管理者專區」→ 未登入時跳出登入 Dialog（見 7 節），輸入帳號密碼

登入成功後，左側仍只有「管理者專區」且顯示選取狀態；右側展開五個 Tabs：同步來源、本機設定、系統紀錄、操作稽核、備份與還原。登出按鈕位於右側頁首：

┌───────────┐
│ 🏠 首頁     │
│ 📋 我的任務  │
│ 🕘 歷史紀錄  │
│ 🍅 番茄鐘    │
│ ⚙ 設定     │
│ 🔒 管理者專區│
│           │
│ ● 同步中    │
└───────────┘

點擊右側頁首「登出管理者」或 15 分鐘無操作自動登出後，
  右側返回首頁；左側「管理者專區」入口仍可見，下次點擊需重新登入
15 分鐘無操作自動登出（見主規劃書 15 節），登出時彈出提示「已自動登出管理者模式」
```

## 3. 首頁 Dashboard

對應主規劃書第 8 節。畫面採**雙欄版面**：左欄由上到下堆疊兩塊內容（「下一個提醒」主視覺卡片＋「即將到來的提醒」表格卡片），右欄是三張直式堆疊的資訊卡片（今日任務概況／雲端來源同步狀態／快速操作）。兩欄各自獨立延伸，互不跨欄。

```text
┌───────────────────────────────────┐┌─────────────────────────┐
│ 🔔 下一個提醒            [高級]     ││ 📊 今日任務概況   查看詳情 ›│
│                                    ││ 今日總任務 待處理  已確認   │
│ 團隊同步會議              ╭─────╮  ││   12 項    8 項    4 項   │
│                          │17:28│  │├─────────────────────────┤
│ 📅 今天 18:00（還有17分鐘） │後提醒│  ││ ☁ 雲端來源同步狀態          │
│                          ╰─────╯  ││ ● Sheet A 同步成功         │
│ 📄 團隊下班同步會議，確認今日        ││   最後同步：20:54:30       │
│    進度是否已同步完成。              ││              公司共同(唯讀)│
│                                    ││ ● Sheet B 同步成功         │
│ [Sheet A] [公司共同]                ││   最後同步：20:54:10       │
└───────────────────────────────────┘│              團隊共用(唯讀)│
┌───────────────────────────────────┐│        [⟳ 立即同步]        │
│ 🕐 即將到來的提醒   查看完整任務列表 ›│├─────────────────────────┤
│ 時間   任務標題      來源  等級  狀態 ││ ⚡ 快速操作                │
│ 18:00  團隊同步會議  SheetA 中級 等待中││ [+ 新增本機任務           ›]│
│ 18:15  臨時討論會    SheetB 中級 等待中││ [⟳ 立即同步               ›]│
│ 18:30  每日備份確認  本機  高級 等待中││ [▶ 預覽提醒等級（測試用） ›]│
│ 19:00  關閉設備      本機 最高級等待中││                          │
└───────────────────────────────────┘└─────────────────────────┘
```

```text
左欄「下一個提醒」卡片：視覺份量刻意放大，是整頁唯一的主視覺焦點，理由是「使用者最需要立刻知道的
  一件事」就是下一個提醒何時到來、內容是什麼，其餘資訊（今日概況、同步狀態、快速操作）都是次要參考
  資訊，因此收斂到右欄以較小的卡片堆疊呈現，版面主從關係更清楚（相較舊版「三張等寬卡片橫排」的做法，
  舊版讓「下一個提醒」與「今日待辦」「同步狀態」視覺權重相同，容易分散注意力）
左欄「即將到來的提醒」表格：與「下一個提醒」卡片同屬左欄、由上到下堆疊，而非橫跨全寬放在雙欄
  區塊之後——這樣左欄的總高度自然貼近右欄三張卡片疊起來的高度，避免左右兩欄高度差距過大時，
  較矮的一欄下方出現大片不自然的留白（雙欄各自延伸到底，不需要互相牽就對方的高度）
右欄「今日任務概況」：三個數字並排（今日總任務／待處理／已確認），取代舊版單一 Card 的「7 個」+
  「3 等待中/4 已簽收」文字堆疊寫法，三欄並排比純文字換行更快掃視
右欄「雲端來源同步狀態」：改為逐條列出每個來源（Sheet A、Sheet B）各自的同步狀態、最後同步時間、
  唯讀標記，而非舊版合併成一行「Sheet A/B 正常」文字——來源狀態各自獨立顯示，才不會讓其中一個來源
  同步失敗時被另一個來源的「正常」文字掩蓋掉
右欄「快速操作」：三個按鈕改為「圖示＋標題＋副說明文字」的完整列表項卡片，取代舊版單純的小按鈕列，
  副說明文字讓使用者不需要 hover 或試點就知道每個操作實際會做什麼
```

### 3.0 元件清單

| 元件 | 類型 | 說明 / 預設值 |
| --- | --- | --- |
| 「下一個提醒」主卡片（左欄，上） | 唯讀 Card | 顯示最早的未觸發 Pending 任務：等級徽章、任務標題、日期時間、任務描述、來源徽章列、右側大型倒數環形進度圈；無任務時整張卡片顯示「目前沒有排定的提醒」空狀態文案 |
| 倒數環形進度圈 | 唯讀，每秒刷新 | 中央顯示 `MM:SS` 倒數時間，環的填色比例反映距離觸發時間的進度（例如以「提前 25 分鐘顯示」為基準）；超過 24 小時的任務改顯示天數文字，不使用環形進度 |
| 「即將到來的提醒」表格卡片（左欄，下） | DataGrid，唯讀，包在 Card 容器內 | 欄位：時間/任務標題/來源/提醒等級/狀態；最多 8 列，依 ScheduledAt 升冪排序；卡片標題列含「查看完整任務列表」連結 |
| 「查看完整任務列表」連結 | Hyperlink | 僅在任務數 > 8 筆時出現，點擊切換至「我的任務」頁 |
| 「今日任務概況」卡片（右欄） | 唯讀 Card | 三欄並排數字：今日總任務／待處理／已確認，統計今天 `ScheduledAt` 落在當天的任務 |
| 「雲端來源同步狀態」卡片（右欄） | 唯讀 Card | 逐條列出 Sheet A、Sheet B 各自的同步狀態（成功/失敗徽章）、最後同步時間、唯讀標記；卡片內含 [立即同步] 按鈕 |
| 「快速操作」卡片（右欄） | 唯讀 Card + 3 個可點擊列表項 | 每項含圖示、標題、副說明文字；點擊整列即觸發對應動作，不需精準點在按鈕上 |
| [+ 新增本機任務] | 快速操作列表項 | 開啟 4.1 節 Dialog |
| [⟳ 立即同步] | 快速操作列表項 / 同步狀態卡片內按鈕（兩處功能相同） | 觸發一次 SyncWorker，觸發時短暫顯示 Spinner 並停用避免重複點擊 |
| [▶ 預覽提醒等級（測試用）] | 快速操作列表項 | 開啟 3.1 節選單 |

### 3.0 使用邏輯

```text
進入畫面：
  VM 讀取本機 SQLite 快取立即渲染（不等待網路），
  背景同步完成後若資料有變動才局部更新對應卡片/表格（避免整頁閃爍）

「立即同步」點擊（快速操作列表項或同步狀態卡片內按鈕，行為一致）：
  按鈕 disabled → 呼叫 SyncService → 完成後還原按鈕
  成功：卡片與表格局部刷新，Toast 顯示「同步完成，取得 N 筆任務」
  失敗：Toast 顯示「同步失敗：{原因}」，「雲端來源同步狀態」卡片對應該來源的那一列轉紅點與失敗徽章，
    另一個來源若正常則維持綠點顯示不受影響，資料不清空（見主規劃書 29 節）

倒數計時歸零：
  歸零瞬間該任務由 AlarmWorker 觸發實際通知視窗（見 9 節），
  左欄「下一個提醒」卡片同時切換為佇列中下一筆任務的內容，不需使用者手動刷新

無任何任務時：
  左欄「下一個提醒」卡片與下方即將到來的提醒表格均顯示各自的空狀態文案，
  右欄三張卡片不受影響（今日任務概況顯示 0，同步狀態與快速操作維持正常顯示）
```

### 3.1 預覽提醒等級（新增互動）

點擊右欄快速操作卡片中的「預覽提醒等級（測試用）」跳出一個小選單，讓使用者選擇要預覽哪個等級：

```text
┌───────────────────┐
│ 選擇測試等級        │
│ ○ 低級             │
│ ○ 中級             │
│ ○ 高級             │
│ ○ 最高級           │
│      [ 開始測試 ]   │
└───────────────────┘
```

測試觸發的通知視窗與真實通知外觀完全相同，但**不寫入 AcknowledgementLogs**，僅供預覽/教育使用者用途。

---

## 4. 我的任務 Tasks

對應主規劃書第 9-10 節。

```text
┌─────────────────────────────────────────────────────────────────┐
│ [搜尋________]  篩選: [全部▾][來源▾][等級▾]  [重新同步] [+新增]   │
├─────────────────────────────────────────────────────────────────┤
│ 時間   任務        來源     等級   重複  啟用狀態    操作           │
│ 18:00 團隊同步會議 Sheet A  中級   -    (● 啟用)   🔒 請至來源試算表編輯│
│ 18:15 臨時討論會   Sheet B  中級   -    (● 啟用)   🔒 請至來源試算表編輯│
│ 18:30 每日備份確認  本機    高級   每天  (● 啟用)   [✎編輯][⧉複製][🗑刪除]│
│ 19:00 關閉設備     本機    最高級 -    (○ 停用)   [✎編輯][⧉複製][🗑刪除]│
└─────────────────────────────────────────────────────────────────┘
```

```text
表格支援排序（點欄位標題）、篩選（來源/等級/啟用狀態）、搜尋（任務名稱關鍵字）
「重複」欄顯示規則：不重複顯示「-」；Daily 顯示「每天」；Weekly:1,2,3,4,5 顯示「每個工作日」而非
  展開列出星期幾，其餘 Weekly 組合顯示「每週 (X)」列出實際勾選的星期；Monthly 顯示「每月 X 號」；
  LunarDay:n,n,... 顯示「農曆 X、X」列出實際點選的農曆日期（例如「農曆 十四、十五」）
「啟用狀態」欄改為 iOS 風格 Toggle 開關（藍色=啟用／灰色=停用），取代原本的勾選框樣式，
  視覺上更容易一眼分辨大量任務中哪些已停用；行為不變，仍可直接在列表切換、不需開啟編輯視窗
「來源=Sheet A」或「Sheet B」的列：操作欄不提供編輯/複製/刪除按鈕，改為直接顯示鎖頭圖示
  + 常駐文字「請至來源試算表編輯」（不需 hover 才看到），依來源不同可各自附上更完整的提示文字：
  Sheet A →「此任務由公司核心設定管理，請聯繫 IT 至 Sheet A 修改」；
  Sheet B →「此任務為團隊共用任務，請至 Sheet B 修改（見主規劃書 19B 節，開放團隊成員自行編輯）」
「來源=本機」的列：操作欄提供三顆「圖示＋文字」的完整按鈕 [✎ 編輯] [⧉ 複製] [🗑 刪除]，
  取代原本純圖示的 IconButton，避免使用者需要 hover 才知道圖示代表的動作
```

### 4.0 元件清單

| 元件 | 類型 | 說明 / 預設值 |
| --- | --- | --- |
| 搜尋框 | TextBox | 即時過濾任務名稱（輸入時 debounce 300ms 再套用，避免逐字重新渲染） |
| 篩選：來源 | ComboBox | 選項：全部 / 本機 / Sheet A / Sheet B；預設「全部」 |
| 篩選：等級 | ComboBox | 選項：全部 / 低級 / 中級 / 高級 / 最高級；預設「全部」 |
| [+新增] | Button | 開啟 4.1 節 Dialog（新增模式） |
| 啟用勾選框 | CheckBox（表格內） | 對「來源=Sheet A/B」的列停用（灰階不可點），僅本機任務可切換 |
| 編輯 ✎ | IconButton（表格內） | 僅本機任務顯示，開啟 4.1 節 Dialog（編輯模式，欄位帶入既有值） |
| 刪除 🗑 | IconButton（表格內） | 僅本機任務顯示，點擊跳二次確認 Dialog「確定要刪除『{任務名稱}』嗎？」 |
| 複製 ⧉ | IconButton（表格內） | 僅本機任務顯示，開啟 4.1 節 Dialog（新增模式，欄位預先帶入來源任務的值，時間清空待填） |
| 🔒 唯讀圖示 | IconButton（表格內） | Sheet A/B 任務皆顯示，點擊彈出提示（依來源不同，見上方線框圖說明），不可操作 |

### 4.0 使用邏輯

```text
篩選 / 搜尋：
  多重條件為 AND 關係（例如「來源=本機」+「等級=高級」+搜尋關鍵字同時套用）
  切換分頁 / 重新整理後篩選條件重置為預設（不記憶上次篩選狀態）

刪除任務：
  二次確認 Dialog 確認後才真正刪除，刪除為硬刪除（本機任務無回收桶，符合小工具的單純定位）
  刪除當下若該任務已進入 Triggering/Displayed 狀態（見主規劃書 27 節）：
    禁止刪除，Dialog 顯示「此任務通知正在顯示中，請先完成或關閉通知後再刪除」

啟用/停用切換：
  Sheet A/B 任務的勾選框不可互動（來源鎖定，見主規劃書 9 節）
  本機任務點擊勾選框立即生效，不需額外儲存動作，AlarmWorker 下次計算時排除已停用任務

清單為空時：
  表格區顯示置中文案「尚無任務，點擊右上角新增第一筆任務」+ 快捷 [+新增] 按鈕
```

### 4.1 新增 / 編輯本機任務 Dialog

對應主規劃書第 10、10.1 節。

```text
┌───────────────────────────────────────────────────┐
│ 新增本機任務                                     ✕ │
├───────────────────────────────────────────────────┤
│ 日期 [2026/09/14 ▾]        時間 [18:30]             │
│ 任務名稱 [__________________________________]      │
│ 說明/備註 [_________________________________]      │
│                                                     │
│ 重要程度 *                                          │
│ ┌────────┐┌────────┐┌────────┐┌──🔒最高級──┐        │
│ │  低級   ││  中級   ││ ●高級  ││  (權限受限)  │        │
│ │  ○      ││  ○      ││ 已選中  ││  已鎖定      │        │
│ └────────┘└────────┘└────────┘└──────────────┘        │
│                                                     │
│ 需輸入確認碼          (● 開啟)                       │
│  當提醒時需輸入確認碼才能關閉                          │
│                                                     │
│ 重複設定                                            │
│  ○ 無                                              │
│  ○ 每天                                             │
│  ○ 每個工作日                                        │
│  ○ 每週   ☐一 ☐二 ☐三 ☐四 ☐五 ☐六 ☐日            │
│  ○ 每月   [1 ▾] 日                                  │
│  ○ 農曆   1 2 3 4 5 6 7 ... 28 29 30（可複選）        │
│                                                     │
│ 國定假日略過 (●)          啟用此任務 (●)              │
│                                                     │
│                          [ 取消 ]  [ 儲存 ]          │
└───────────────────────────────────────────────────┘
```

```text
提醒等級改為 4 張並排的大卡片（低/中/高/最高各一張），而非單選按鈕列：
  選中的卡片有明顯外框高亮＋內部圓點勾選標記，比純文字單選按鈕更容易一眼看出目前選了哪一級
  若使用者權限受 Sheet A／Employees 的 MaxAllowedLevel 限制（見主規劃書 19.1），
    超出上限的卡片直接以整張卡片灰階鎖定樣式呈現（含小鎖頭圖示），
    hover 或點擊時顯示 tooltip 說明原因，例如「管理員限制：此帳號不可使用最高級」，
    比「停用選項＋提示文字」的呈現方式更直覺，不需要額外去找哪裡有說明文字
選中「最高級」卡片時「需輸入確認碼」自動開啟且鎖定（不可關閉）
選中「高級」卡片時同樣顯示為需要確認（點擊型），但不出現驗證碼相關欄位
「需輸入確認碼」改用 Toggle 開關（與其他頁面的開關樣式一致），取代原本的 Checkbox
「重複設定」改為 Radio 選項列表，「每週」「每月」「農曆」被選中時，其複選框/輸入框直接內嵌顯示
  在該選項右側或下方（無需展開/收合動畫），使用者一眼就能看到所有可用的重複規則與其附屬選項：
  不重複 / 每天 / 每個工作日 / 每週（可複選星期）/ 每月固定日 / 農曆日期（可複選初一至三十）
  「每個工作日」為快捷選項，選中後等同於「每週」勾選週一至週五，底層存入同一種格式（見主規劃書 10.1 節），
    不需要額外的星期複選框；編輯既有任務若其重複規則剛好是週一至週五，應直接選中「每個工作日」，
    而非顯示為「每週」＋五個打勾方塊，避免使用者誤以為系統把設定改掉了
  「農曆日期」選中後於選項下方顯示一組 1-30 的日期按鈕（樣式參考月曆的多選日期格），使用者直接點選要
    觸發的農曆日期（可複選），例如點選「十四」與「十五」即代表「十五前一天與當天」，不需要使用者自己
    換算或輸入任何代碼；底層依所選數字組合存成 LunarDay:14,15（見主規劃書 20.2b 節），
    對使用者完全隱藏這個內部格式，UI 只呈現「選了哪幾個農曆日期」
```

### 4.1 元件清單

| 元件 | 類型 | 預設值 / 驗證規則 |
| --- | --- | --- |
| 日期 | DatePicker | 預設今天；不可選過去日期（編輯已逾期任務時例外，允許保留原值） |
| 時間 | TimePicker | 預設目前時間 +1 小時，取整到 5 分鐘；必填 |
| 任務名稱 | TextBox | 必填，1-50 字；為空時「儲存」停用且欄位顯示紅框 |
| 任務描述 | TextBox（多行） | 選填，上限 200 字，超過即時截斷並顯示字數提示 |
| 備註 | TextBox | 選填，上限 100 字 |
| 提醒等級 | 4 張並排選擇卡片 | 預設「中級」；卡片點擊選中，選中狀態以外框高亮＋圓點標記呈現；選項依 Sheet A／Employees 的 MaxAllowedLevel 上限動態鎖定（見上方說明） |
| 需要閱讀確認碼 | ToggleSwitch | 選中「最高級」時強制開啟並停用（不可關閉）；其餘等級預設關閉、可自由切換 |
| 重複規則 | RadioButton 列表 + 內嵌附屬選項 | 選項：不重複／每天／每個工作日／每週／每月固定日／農曆日期；預設「不重複」；選「每個工作日」不展開任何附屬選項（底層直接存 Weekly:1,2,3,4,5）；選「每週」時該選項旁直接顯示星期複選框；選「每月」時顯示日期數字選擇（1-31）；選「農曆日期」時顯示 1-30 的可複選日期按鈕（底層存 LunarDay:n,n,...，見主規劃書 20.2b 節），至少需選一個日期；所有附屬選項皆為版面內嵌顯示，不使用展開/收合動畫 |
| 國定假日略過 | CheckBox | 預設勾選（見主規劃書 20.2） |
| 是否啟用 | CheckBox | 預設勾選 |
| [取消] | Button | 關閉 Dialog，不儲存任何變更（若有未儲存修改，二次確認「捨棄變更？」） |
| [儲存] | Button | 通過驗證後寫入，未通過則反白第一個錯誤欄位並捲動至該處 |

### 4.1 使用邏輯

```text
開啟模式：
  新增：所有欄位為預設值
  編輯：欄位帶入既有任務資料；「日期+時間」若已逾期（早於現在），維持原值但顯示提示
        「此任務已過期，儲存後將視為逾期任務」
  複製：比照新增模式，但除日期/時間外欄位皆帶入來源值，日期/時間清空待填

儲存驗證順序（由上至下，第一個失敗即中止並提示）：
  1. 任務名稱不可為空
  2. 日期 + 時間組合不可早於現在（新增模式；編輯模式放行但顯示上述提示）
  3. 重複規則若選「每週」，星期複選框至少勾選一項；若選「農曆日期」，日期按鈕至少點選一個
  4. 提醒等級不可超過 Sheet A／Employees 的 MaxAllowedLevel 允許上限（即使繞過 UI 停用狀態，Service 層仍需二次檢查）

儲存成功：
  Dialog 關閉，回到 4 節列表，新/修改的列以短暫高亮動畫標示（1.5 秒淡出）
  若為 Recurrence 任務，列表僅顯示「範本」一列，不展開個別日期實例（實例僅存在於 AlarmWorker 排程與歷史紀錄）

視窗大小與相依欄位：
  「重複規則」的星期複選框／日期選擇／農曆日期按鈕皆為各自選項的內嵌顯示，固定佔用版面空間
    （不因選中而觸發展開/收合動畫），Dialog 整體高度固定，不會因為選了哪個重複規則而忽大忽小
```

---

## 5. 歷史紀錄 History

對應主規劃書第 11 節「提醒紀錄」。一般使用者只看得到自己裝置上的通知簽收歷史，**不含**同步紀錄與系統事件——這兩項屬於技術維運資訊，移至管理者頁（見 8.3）。

本頁分為兩個頁籤（Tab）：**任務提醒**（資料來源 AcknowledgementLogs）與**番茄鐘**（資料來源 PomodoroLog，見主規劃書 13A、21 節）。兩者是完全獨立的資料表與統計口徑——「任務有沒有被簽收」與「番茄鐘用了幾輪」是性質不同的事，不會互相加總或混合篩選；UI 上只是共用同一個頁面入口與版面風格，方便使用者不必在導覽列切換頁面就能查兩種紀錄。番茄鐘頁面（見 6A 節）側邊卡片的「查看詳細紀錄」連結會直接跳轉並切到本頁的「番茄鐘」頁籤。

### 5.1 任務提醒頁籤

考量到資料量會隨使用時間持續累積（例如一天 5 筆任務、一季即累積 400 筆以上），本頁籤提供**關鍵字搜尋 + 日期區間 + 來源 + 結果**四種篩選條件並存使用，避免使用者在大量歷史資料中「找資料找到死」：

```text
┌─────────────────────────────────────────────────────────┐
│ [任務提醒] [番茄鐘]  ← 頁籤                                 │
├─────────────────────────────────────────────────────────┤
│ 準時簽收 15 筆 │ 逾期簽收 3 筆 │ 逾期未簽收 3 筆 │ 未開機 2 筆 ←依篩選結果即時變動│
├─────────────────────────────────────────────────────────┤
│ [搜尋任務關鍵字___] [起始日期▾] 至 [結束日期▾]              │
│ [來源：全部▾] [結果：全部▾] [清除篩選]        [匯出 CSV]    │
├─────────────────────────────────────────────────────────┤
│ ℹ 狀態於程式下次啟動時更新。                                │
├─────────────────────────────────────────────────────────┤
│ 任務   來源   預定時間  觸發時間  簽收時間  耗時  結果        │
│ 團隊進度會議 Sheet A 18:00:00 18:00:01 18:01:17 76s 準時簽收 ✓│
│ 每日備份確認 Sheet A 09:00:00 09:00:00  09:22:10 22分10秒 逾期簽收 ◐│
│ 系統維護檢查 本機   22:00:00 22:00:04  -        -   逾期未簽收 ⚠│
│ 下班檢查    本機    18:00:00  -        -        -   未開機 ○   │
├─────────────────────────────────────────────────────────┤
│ 顯示 1-15 筆，共 23 筆                    [◂] 1 / 2 [▸]     │
└─────────────────────────────────────────────────────────┘
```

版面調整：統計卡片（BI 摘要）置於篩選工具列**上方**，讓使用者先看到整體結果分布，再往下決定要怎麼篩選；原本的順序（篩選在上、統計卡片在下）容易讓人誤以為統計卡片是「篩選結果的附屬說明」而非頁面的重點摘要。

```text
結果欄用顏色 + 圖示區分（準時簽收=綠勾 / 逾期簽收=橘半圓 / 逾期未簽收=黃警示 / 未開機=灰圈），
  對應主規劃書 20.2、20.2a 節新增的 Acknowledged / Overdue_Acknowledged / Overdue_Unacked / NotLaunched 四種狀態
「準時」與「逾期」的判斷基準：觸發後 15 分鐘內完成簽收＝準時，超過 15 分鐘才簽收＝逾期簽收
  （見主規劃書 20.2a 節逾期寬限時間），耗時欄位在逾期簽收的列會明顯偏長，兩者一眼可辨識
「來源」欄與「我的任務」頁（見 4 節）採用相同圖示樣式（本機 / Sheet A / Sheet B），
  讓使用者能一眼對應篩選條件與表格內容
```

### 5.0 元件清單

| 元件 | 類型 | 說明 |
| --- | --- | --- |
| 關鍵字搜尋 | TextBox | 即時篩選任務名稱，不分大小寫，不需按 Enter；比對規則見下方「關鍵字搜尋規則」 |
| 日期區間選擇 | 兩個 DatePicker（起訖） | 預設「最近 30 天」；結束日期不可早於起始日期 |
| 來源篩選 | ComboBox | 選項：全部 / 本機 / Sheet A / Sheet B |
| 結果篩選 | ComboBox | 選項：全部 / 準時簽收 / 逾期簽收 / 逾期未簽收 / 未開機 |
| [清除篩選] | Button | 一鍵重置全部篩選條件為預設值（含日期區間回到最近 30 天） |
| 統計卡片（準時簽收／逾期簽收／逾期未簽收／未開機） | 唯讀，依篩選即時更新 | 對應主規劃書 8 節首頁的卡片樣式，數字反映**目前篩選條件下**的統計，而非全部歷史資料 |
| 提醒紀錄表格 | DataGrid，唯讀 | 依「觸發時間」新到舊排序；新增「來源」欄；欄位不可編輯；每頁固定顯示 15 列 |
| 分頁列（顯示筆數 + 頁碼） | 唯讀文字 + 分頁控制項 | 格式「顯示 {起始}-{結束} 筆，共 {總數} 筆」，右側配上一頁／下一頁按鈕與目前頁碼 |
| [◂ 上一頁] | IconButton | 第 1 頁時停用（灰階） |
| [▸ 下一頁] | IconButton | 最後一頁時停用（灰階） |
| [匯出 CSV] | Button | 匯出**目前篩選條件下**的所有列（非僅目前分頁顯示的列） |

### 5.0a 關鍵字搜尋規則

一般使用者打關鍵字時不一定會照「開頭比對」的直覺輸入，因此搜尋邏輯需兼顧「打得快」與「篩得準」兩種情境：

```text
1. 以空白分隔多個詞 → 各詞之間為 AND 關係，須同時命中才算符合
   例："會議 每日"  → 任務名稱需同時包含「會議」與「每日」（不要求順序、不要求相鄰）

2. 支援 % 作萬用字元，且位置不限（不強制只能放頭尾）
   例："會%"        → 以「會」開頭的任務
       "%議"        → 以「議」結尾的任務
       "%會%"       → 任務名稱中含「會」（等同不加 % 的預設模糊比對）
       "每%確認"    → 以「每」開頭、「確認」結尾，中間任意字元的任務

3. 未輸入任何 % 時，整個詞視為「模糊包含比對」（等同自動套用前後 %），
   讓使用者不需要每次都手動加萬用字元也能用最常見的方式找到資料

4. 不分大小寫（為未來可能出現的英文任務名稱預留）

5. 特殊符號跳脫：使用者輸入中若含正規表示式保留字元（如 . * + ? 等），
   一律視為純文字比對，不會被誤判成語法，避免踩到非預期的比對結果
```

### 5.0 使用邏輯

```text
資料來源：僅讀取本機 SQLite 的 AcknowledgementLogs，不發任何網路請求，開啟畫面即秒開

與番茄鐘頁籤（見 5.2 節）的關係：兩個頁籤的資料表完全獨立（本頁籤讀 AcknowledgementLogs，
  番茄鐘頁籤讀 PomodoroLog），彼此的篩選條件、統計卡片、匯出 CSV 互不影響。
  本頁籤「來源」篩選（本機／Sheet A／Sheet B）僅涵蓋任務提醒的來源，不包含番茄鐘紀錄，
  避免「任務有沒有被簽收」與「番茄鐘用了幾輪」這兩件性質不同的事混在同一套統計裡

四種篩選條件（關鍵字／日期區間／來源／結果）為 AND 關係，可同時套用：
  任一條件變更時，立即重新計算篩選結果、重繪表格、更新統計卡片數字，並將頁碼重置為第 1 頁
  （避免使用者停留在篩選前的頁碼，卻對應到篩選後不存在的資料範圍）

日期區間變更：
  即時重新查詢並刷新表格，區間超過 90 天時顯示提示「查詢區間較大，載入可能需要數秒」

「準時簽收」「逾期簽收」「逾期未簽收」「未開機」四種結果的產生時機（見主規劃書 20.2、20.2a 節）：
  「未開機」（NotLaunched）：非即時計算，由「下次程式啟動時」的啟動補登邏輯回填，
    故電腦真的關機/程式真的沒開時，需等下次開機才會變成「未開機」
  「準時簽收」（Acknowledged）：觸發後 15 分鐘內完成簽收
  「逾期未簽收」（Overdue_Unacked）：程式持續運行、電腦不關機也不睡眠時，不需要等待重啟——
    由背景排程每隔一段時間檢查一次，觸發後 15 分鐘仍未簽收即視為錯過並自動標記（由 IT 於 Sheet A
    統一設定，非使用者可調整），先落地為這個暫定結果，不會無限期停留在「等待中」
  「逾期簽收」（Overdue_Acknowledged）：已經被標記為「逾期未簽收」的記錄，
    若使用者之後回來點擊「確認」，該筆記錄會被改寫成「逾期簽收」（見 20.2a 節）——
    畫面上此類記錄的耗時通常明顯偏長，一眼就能與準時簽收的列區分開來
  四種狀態合計覆蓋「電腦有開/沒開」與「有沒有確認」的所有情境，不存在永遠卡在中間態的記錄
  這點以固定提示 banner 說明：「通知觸發後 15 分鐘內未簽收即視為錯過，自動標記逾時未簽收；
    未開機狀態則於程式下次啟動時回填」

匯出 CSV：
  格式與流程統一見主規劃書 21.1 節「CSV 匯出規格」（UTF-8 BOM、CRLF、檔名規則等）
  匯出範圍為目前篩選條件下的完整結果（不受分頁影響），點擊後跳系統另存新檔對話框，
    預設檔名 AckLog_{起始日yyyyMMdd}_{結束日yyyyMMdd}.csv
  資料量大時（>1000 列）顯示短暫進度提示，避免使用者誤以為沒反應

空狀態：
  篩選結果為 0 筆時，表格顯示「此篩選條件下沒有通知紀錄」置中文字，分頁按鈕皆停用但分頁列仍顯示「共 0 筆」
  （不完全隱藏分頁列，讓使用者清楚知道目前是「篩選後沒結果」而非頁面尚未載入）

分頁行為：
  每頁固定 15 列（大於原本測試用的 6 列，避免資料量成長後總頁數過多、翻頁次數暴增）
  依目前完整篩選結果的總筆數計算總頁數；若目前頁碼超出新的總頁數（例如篩選後資料變少），自動修正回最後一頁
  頁數只有 1 頁時，上一頁／下一頁按鈕皆停用（但分頁列文字仍顯示「顯示 1-N 筆，共 N 筆」）
```

### 5.2 番茄鐘頁籤

資料來源為 PomodoroLog（見主規劃書 13A、21 節），每一列記錄一段「專注」或「休息／長休息」的區間，由番茄鐘計時器（見 6A 節）在每次階段結束或使用者按下「提前結束」時寫入一筆：

```text
┌─────────────────────────────────────────────────────────┐
│ [任務提醒] [番茄鐘]  ← 頁籤（目前在番茄鐘）                  │
├─────────────────────────────────────────────────────────┤
│ [起始日期▾] 至 [結束日期▾]                                  │
│ [階段：全部▾] [狀態：全部▾] [清除篩選]          [匯出 CSV]  │
├─────────────────────────────────────────────────────────┤
│ 完整完成 12 輪 │ 提前結束 2 輪 │ 累積專注 300 分鐘            │
├─────────────────────────────────────────────────────────┤
│ 日期        階段     開始時間  結束時間  設定時長  狀態       │
│ 2026-09-14 🍅 專注   09:02:10  09:27:10  25 分鐘 完整完成 ✓ │
│ 2026-09-14 ☕ 休息   09:27:10  09:32:10  5 分鐘  完整完成 ✓ │
│ 2026-09-14 🍅 專注   10:03:05  10:15:22  25 分鐘 提前結束 ○ │
├─────────────────────────────────────────────────────────┤
│ 顯示 1-15 筆，共 19 筆                    [◂] 1 / 2 [▸]     │
└─────────────────────────────────────────────────────────┘
```

#### 5.2 元件清單

| 元件 | 類型 | 說明 |
| --- | --- | --- |
| 日期區間選擇 | 兩個 DatePicker（起訖） | 預設「最近 30 天」，與任務提醒頁籤的日期範圍互相獨立（各自記自己的篩選狀態） |
| 階段篩選 | ComboBox | 選項：全部 / 專注 / 休息 / 長休息 |
| 狀態篩選 | ComboBox | 選項：全部 / 完整完成 / 提前結束 |
| [清除篩選] | Button | 重置本頁籤篩選條件為預設值，不影響任務提醒頁籤的篩選狀態 |
| 統計卡片（完整完成輪次／提前結束／累積專注時數） | 唯讀，依篩選即時更新 | 「完整完成輪次」與「累積專注時數」僅計入 phase=專注 且 status=完整完成 的列；休息／長休息不計入輪次或時數統計 |
| 番茄鐘紀錄表格 | DataGrid，唯讀 | 依「開始時間」新到舊排序；欄位：日期／階段／開始時間／結束時間／設定時長／狀態；每頁固定顯示 15 列 |
| 分頁列、上一頁／下一頁 | 同任務提醒頁籤規則 | 沿用 5.0 節分頁行為（每頁 15 列、頁碼自動修正、空狀態不隱藏分頁列） |
| [匯出 CSV] | Button | 匯出目前篩選條件下的完整結果，欄位見主規劃書 21.1 節 PomodoroLog 匯出規格，檔名 PomodoroLog_{起始日}_{結束日}.csv |

#### 5.2 使用邏輯

```text
資料來源：僅讀取本機 SQLite 的 PomodoroLog，與任務提醒頁籤的 AcknowledgementLogs 完全分開查詢

寫入時機（見主規劃書 13A 節番茄鐘邏輯）：
  每次「專注」或「休息／長休息」階段正常倒數結束時，寫入一筆 status=完整完成 的紀錄
  使用者按下「提前結束」（pomoSkip）中斷當前階段時，寫入一筆 status=提前結束 的紀錄，
    結束時間記錄為實際中斷的時間點，設定時長仍記錄原本該階段預定的分鐘數（供比對「原訂 vs 實際」）
  「暫停」不會產生任何紀錄列，只有階段真正結束或被跳過才落地一筆

統計卡片口徑：
  「完整完成輪次」「累積專注時數」只統計 phase=focus 且 status=done 的列，
    休息／長休息階段即使完整完成也不計入這兩個數字（避免灌水，這兩個卡片的意義是「有效專注量」）
  「提前結束」則不分階段，只要 status=skip 就計入，讓使用者知道自己中斷了多少次（含休息被跳過）

與番茄鐘頁面（6A 節）「今日完成」卡片的關係：
  番茄鐘頁面的「今日完成」是同一份 PomodoroLog 資料、篩選當天日期、phase=focus 且 status=done 的計數，
  與本頁籤統計卡片算法一致，只是預設篩選範圍不同（今日 vs 使用者自選區間），避免兩處數字對不上

篩選、分頁、空狀態、匯出行為與任務提醒頁籤一致（見 5.0 節），僅篩選欄位（階段/狀態 取代 來源/結果）與
  資料來源不同；兩頁籤各自維護獨立的篩選狀態與頁碼，切換頁籤不會重置對方已設定的篩選條件
```

---

## 6A. 番茄鐘 Pomodoro

對應主規劃書 13A 節。獨立於任務提醒系統之外的個人專注力小工具。版面配置的優先順序為：**計時器操作 > 今日進度儀表板 > 設定**——設定是低頻操作（一次調好就很少再動），因此移到頁面最下方、預設收合，把最上方的視覺焦點留給「現在在做什麼」與「今天累積了多少」：

```text
┌─────────────────────────────────────────────────────────┐
│                          │ ⏱ 本輪狀態                       │
│    ┌───────────────┐    │  目前階段  ●專注中（第2回合）      │
│    │     24:35     │    │  下一階段  ⏱休息（5分鐘）         │
│    │   專注中 🍅    │    │  開始時間  14:32                 │
│    └───────────────┘    │  距離長休息還差 2 回合  ●●○○      │
│                          ├─────────────────────────────────┤
│ [暫停][重設][提前結束]    │ 📊 今日進度            查看番茄鐘紀錄│
│                          │  ╭───╮  🏆 很棒！  │近7天 4.6輪/日│
│                          │  │3/6│    持續專注， │┃┃┃┃┃┃┃    │
│                          │  ╰───╯    離目標更近了！一二三四五六日│
│                          │  今日目標  │09/08.........09/14│
└─────────────────────────────────────────────────────────┘
▾ 番茄鐘設定（預設收合，點擊展開）
  專注 [25] 分鐘    休息 [5] 分鐘    ☑ 每 4 輪長休息 15 分鐘
  ☑ 週期結束播放音效   [選擇音效▾]  [試聽▸]
```

右欄兩張卡片順序為「本輪狀態」在上、「今日進度」在下——本輪狀態是使用者當下最需要確認的即時資訊（現在算第幾回合、下一步是什麼），今日進度則是累積統計，優先順序上更接近「回顧」而非「操作依據」，故排在下方。

待命狀態（尚未開始計時）：計時圈顯示「25:00 / 待命中」，僅 [開始專注] 按鈕可見，其餘操作按鈕隱藏；右欄兩張卡片維持顯示（今日進度不因待命而清空）。

### 6A.1 本輪狀態卡片

原本用 Badge 呈現「目前階段」「下一階段」，改為橫式 label-value 列搭配更豐富的視覺元素，讓使用者一眼看懂目前進度與下一步：

| 元件 | 類型 | 說明 |
| --- | --- | --- |
| 目前階段 | 唯讀，紅點+文字 | 格式「{階段名稱}（第 {N} 回合）」，例如「專注中（第 2 回合）」；待命狀態不顯示回合數，僅顯示「待命中」，紅點在待命狀態改為灰點 |
| 下一階段 | 唯讀，圖示+文字 | 格式「{階段名稱}（{分鐘數} 分鐘）」，例如「休息（5 分鐘）」，圖示為小型時鐘 |
| 開始時間 | 唯讀文字 | 格式 `HH:mm`，待命狀態顯示「—」 |
| 距離長休息還差 | 唯讀，數字+圓點列 | 格式「{N} 回合」+ 長休息週期進度圓點；圓點數量＝長休息間隔（預設 4，可調 2-8），已完成的輪次填滿為實心紅點、目前進行中的那一輪以半透明紅點標示、尚未開始的維持空心灰點 |

### 6A.2 今日進度卡片（BI 化）

原本「🍅🍅🍅 3 個番茄鐘」的純圖示列，改為環形進度圈搭配激勵文字與週趨勢，讓使用者一眼判斷「今天做得夠不夠」而不必自己去數圖示或換算。整張卡片為**左右兩欄並排**：左欄是環形進度圈＋激勵文字（份量較大、視覺優先），右欄是週趨勢膠囊長條（份量較小、居次要參考），左右並排比上下堆疊更能維持卡片整體高度精簡，避免卡片過高，配合下方「本輪狀態」卡片形成較勻稱的頁面比例：

| 元件 | 類型 | 說明 |
| --- | --- | --- |
| 環形進度圈 + 大數字（左欄） | 唯讀，環形進度條 | 中央顯示「{已完成輪次} / {每日目標輪數}」（例如「3 / 6」）+ 下方小字「今日目標」；環的填色比例＝今日輪次 / 每日目標輪數，橘紅色調，超過目標時環維持滿圈（不溢出）；數字僅計 phase=focus 且完整完成，同 5.2 節口徑 |
| 獎盃圖示 + 激勵提示文字 | 唯讀，圖示+文字 | 依進度顯示不同文案：0 輪／進行中／達標三種（見使用邏輯）。**此區塊是同一組節點，會依今日輪次是否為 0 在左右欄之間整組移動、不重複維護兩份內容**：今日輪次 ≥ 1 時置於左欄、環形進度圈右側，版面呈現「一句鼓勵的話搭配一個成績」；今日輪次為 0（尚未開始任何一輪）時，整組（含圖示）改置於右欄週趨勢長條圖上方，文案改為引導語，此時左欄僅單獨顯示環形進度圈 |
| 週趨勢膠囊長條（右欄） | 唯讀，7 欄垂直膠囊 | 近 7 天（含今日）每日完成輪數：每欄是一個固定高度的灰色軌道膠囊，內部由下往上填色至對應比例；填色顏色：今日固定用強調色（紅／橘）標示，其餘日期填綠色；0 輪的日期軌道保持全空；每欄下方兩行文字：中文星期（一～日）在上、日期（`MM/DD`）在下；滑鼠停留顯示「週{星期} {日期}：{輪數} 輪」；欄位較窄時可縮小膠囊寬度或改為橫向捲動，優先保證左欄環形進度的完整呈現 |
| 週平均文字（右欄） | 唯讀文字 | 格式「平均 {平均值} 輪/日」，取近 7 天算術平均，四捨五入至小數點後 1 位，置於右欄標題列；今日輪次為 0 時標題列（含此文字）隱藏，讓位給搬移過來的獎盃圖示+引導語 |
| [查看番茄鐘紀錄] | 連結 | 點擊跳轉至歷史紀錄頁並自動切換到「番茄鐘」頁籤（見 5.2 節） |

### 6A.3 番茄鐘設定（頁面下方，可摺疊）

| 元件 | 類型 | 說明 / 驗證規則 |
| --- | --- | --- |
| 摺疊區塊標題列 | ToggleButton | 點擊展開／收合；首次進入頁面預設展開，之後的展開/收合狀態不記憶（每次重新整理回到預設展開），避免使用者忘記自己調過設定 |
| 專注分鐘數 | NumericUpDown | 範圍 5-90 分鐘，預設 25；僅待命狀態可修改 |
| 休息分鐘數 | NumericUpDown | 範圍 1-30 分鐘，預設 5；僅待命狀態可修改 |
| 長休息規則 | CheckBox + NumericUpDown | 預設開啟，每 4 輪（可調 2-8）進入 15 分鐘長休息；此數字同時決定 6A.2 節圓點列的圓點數量 |
| 週期結束播放音效 | CheckBox | **預設勾選（開啟）**，與設定頁任務通知四等級的音效開關各自獨立（見主規劃書 13A、18.1 節） |
| 選擇音效 | ComboBox | 列出偵測到的 Windows 系統音效名稱；未勾選「播放音效」時停用 |
| 試聽 | IconButton | 播放目前選擇的音效一次 |

### 6A 使用邏輯

```text
[開始專注] 點擊：
  寫入 PomodoroLog 一筆 Type=Focus, StartedAt=now, Result 暫為 Interrupted
  倒數圈開始遞減，階段標籤顯示「專注中 🍅」

倒數歸零（專注階段結束）：
  更新該筆 PomodoroLog 為 CompletedAt=now, Result=Completed
  觸發「中級」通知（見主規劃書 18 節樣式、13A 節番茄鐘專屬文案）：
    「專注時間結束，休息一下吧」
  若「週期結束播放音效」已勾選且已選擇音效：通知彈出的同時播放一次該音效
  使用者點擊通知的「確認」後，自動開始休息倒數（不需要回到本頁面操作）

倒數歸零（休息/長休息階段結束）：
  觸發「中級」通知：「休息結束，準備開始下一輪專注」
  同樣依「週期結束播放音效」設定決定是否播放
  點擊確認後畫面回到待命狀態，等待使用者手動點擊 [開始專注]（不自動連續進行）

[重設]：
  二次確認「確定要重設嗎？本輪將不計入完成紀錄」
  確認後該筆 PomodoroLog 標記 Result=Interrupted，畫面回到待命狀態

[提前結束]：
  效果等同倒數自然歸零，直接觸發對應通知並進入下一階段，但該筆 PomodoroLog 標記為 Interrupted
  （因為未達完整時長，不計入「今日完成」的 🍅 數量）

專注/休息分鐘數調整：
  僅待命狀態下可修改，修改後立即儲存於本機 PomodoroSettings 表，下次開始即套用新數值
  計時進行中，頁面下方「番茄鐘設定」摺疊區塊整塊變唯讀（灰階），避免中途調整造成混淆；
    此時若使用者手動展開設定區塊仍可看到目前數值，只是無法編輯

今日完成卡片（6A.1 節）即時更新時機：
  每次一筆 PomodoroLog 的 Focus 紀錄變為 Result=Completed（倒數自然歸零，非提前結束）時，
    立即重新計算環形進度百分比、中央數字、激勵文字，不需等待離開頁面或手動整理

獎盃圖示＋激勵文字的左右欄搬移邏輯（6A.2 節）：
  今日完成輪次＝0：該區塊（圖示+文字為同一節點，非複製）搬到右欄、置於週趨勢長條圖上方；
    文案固定為「開始第一個番茄鐘，累積今天的專注時間！」；右欄原本的「近 7 天完成輪數」標題列
    （含週平均文字）同時隱藏，避免與搬入的引導語同時出現造成右欄擁擠；左欄僅保留環形進度圈
  今日完成輪次≥1 且 < 每日目標：該區塊搬回左欄、置於環形進度圈右側；文案為「很棒！持續專注，
    離目標更近了！」；右欄標題列恢復顯示
  今日完成輪次 ≥ 每日目標：位置同上（左欄），文案改為「太棒了！已達成今日目標，持續專注，
    離目標更近了！」
  每次今日輪次數字變動時（含前述「今日完成卡片即時更新時機」）都要重新判斷是否需要搬移，
    不是只在頁面載入時判斷一次
  每日目標輪數為本機參考值，儲存於 PomodoroSettings，使用者可自行調整（不在本次 MVP 範圍內開放調整
    UI 者，先以固定預設值 8 輪呈現，未來如需開放調整再加欄位，不影響本節其餘邏輯）
  週趨勢長條圖資料來源：以「今日」為基準，向前取 6 個自然日 + 今日共 7 欄，
    每欄數字＝PomodoroLog 中該日 phase=focus 且 status=done 的筆數（與 5.2 節統計卡片同口徑）
    今日欄位需與計時器即時同步（使用者正在計時看到的中央數字與最後一欄長條應隨時一致，不可有延遲）

本輪資訊卡片的長休息週期圓點（6A.2 節）：
  圓點數量＝目前設定的長休息間隔（預設 4）；已完成輪次對「間隔」取餘數決定填滿幾顆
  例：間隔 4、今日已完成 6 輪 → 6 % 4 = 2，顯示 2 顆實心 + 1 顆進行中（若目前正在專注）+ 1 顆空心
  完成滿一個週期（觸發長休息）的瞬間，圓點重新從空心開始下一輪計數，不會累積成一長串圓點

與任務通知系統的關係：
  番茄鐘的通知視窗與任務通知共用同一套「中級」樣式與畫面堆疊區域，
    可並存顯示，不互相阻擋、不參與任務的 Triggering/Displayed/Acknowledged 狀態機（見主規劃書 13A 節）
  離開「番茄鐘」頁面切到其他分頁時，計時仍在背景繼續運作，不會因為切換頁面而暫停
```

---

## 6. 設定頁 Settings

對應主規劃書第 14 節。分「一般」「我的身分」與「通知」三區塊，鎖定項目統一樣式處理。

```text
┌─────────────────────────────────────────────────────────┐
│ 一般                                                      │
│  Windows 開機自動啟動          ☑                          │
│  啟動後縮小至 System Tray      ☑                          │
│  關閉主視窗時縮小至 Tray        ☑                          │
│  視窗縮放時強制等比例（3:2）    ☑                          │
│  外觀模式          ○ 深色  ○ 淺色  ● 跟隨系統               │
│                                                            │
│ 我的身分（皆由 IT 於安裝時分配，唯讀，如需變更請聯繫 IT）      │
│  使用者名稱         王小明                                  │
│  裝置代碼           EMP-001                                 │
│  [ 建立本機備份 ]  （.calbak 打包檔，見 6.1 節／主規劃書19C.2）│
│                                                            │
│ 通知（左右兩欄卡片，與下方音效提醒同樣式）                    │
│  ┌─────────────────────┐                                  │
│  │ 閃爍            ☑     │                                  │
│  └─────────────────────┘                                  │
│  ┌─────────────────────────────────────────────┐          │
│  │ 靜音時段 / 專注模式  ☑ 啟用 [12:00]-[13:00] [+新增時段]│  │
│  └─────────────────────────────────────────────┘          │
│                                                            │
│  音效提醒（各等級獨立設定，見主規劃書 18.1 節）              │
│   低級   ☐ 播放音效   [選擇音效▾]  [試聽▸]                  │
│   中級   ☐ 播放音效   [選擇音效▾]  [試聽▸]                  │
│   高級   ☐ 播放音效   [選擇音效▾]  [試聽▸]                  │
│   最高級 ☐ 播放音效   [選擇音效▾]  [試聽▸]                  │
└─────────────────────────────────────────────────────────┘
```

```text
不提供「置頂」「全螢幕模式偏好」開關：是否 Topmost、覆蓋螢幕比例皆由通知等級本身決定
  （見主規劃書 18 節：低級不置頂、名片大小，可被其他視窗蓋住；中級同為名片大小但置頂；
  高級 60% 螢幕置頂；最高級 90%+ 全螢幕置頂），這是刻意的分級干擾設計，不開放使用者自行覆蓋，
  也不需要管理者鎖定——因為壓根不存在這兩個可調整欄位，避免使用者或管理者誤以為能繞過等級規則
被本機管理者鎖定的設定項（目前為「閃爍」「靜音時段」）：整張卡片變為灰階不可互動，
  右側固定顯示「🔒 此設定由本機管理者控制」，hover 該卡片時顯示 tooltip 說明鎖定原因
  （見主規劃書 15、16 節，管理者僅能鎖定「這台電腦」的設定）
「使用者名稱」「裝置代碼」皆對應主規劃書 19C 節：兩者皆由 IT 於安裝時分配，一般使用者不可自行修改
  （避免員工留空不填或亂填暱稱，導致歷史紀錄、管理者頁看到的名字無法對應到真人，
  見主規劃書 19C.1 節說明為何不用自動產生或使用者自填）；如需變更需聯繫 IT
  兩者**僅存於本機 SQLite，不回寫至任何 Google Sheet**
「建立本機備份」：點擊後彈出選項視窗（見下方 6.0a 節），可打包裝置身分、個人設定、本機任務、
  簽收紀錄，並選擇性包含管理者密碼，產生一個 .calbak 檔案（見主規劃書 19C.2 節），
  用途是重灌 / 換電腦時完整還原這台電腦的狀態，比原本單純的身分匯出範圍更廣
靜音時段可新增多組時段（例如午休 + 下班後），每組可個別刪除
音效提醒：四個等級各自獨立的「播放音效」開關 + 音效下拉選單 + 試聽按鈕（見主規劃書 18.1 節）
  預設全部關閉；「選擇音效」下拉選單列出偵測到的 Windows 系統音效，未勾選「播放音效」時整列變灰階不可互動
  （最高級的音效開關與其他等級地位相同，同樣可自由開關，不強制開啟）
```

### 6.0 元件清單

| 元件 | 類型 | 說明 / 驗證規則 |
| --- | --- | --- |
| Windows 開機自動啟動 | ToggleSwitch | 切換時立即寫入登錄檔（無需另外儲存） |
| 啟動後縮小至 Tray | ToggleSwitch | 立即生效 |
| 關閉主視窗時縮小至 Tray | ToggleSwitch | 立即生效，影響主視窗 ✕ 按鈕行為（見 2 節） |
| 視窗縮放時強制等比例 | ToggleSwitch | 預設開啟；開啟時拖曳視窗邊框調整大小會同步縮放維持 3:2 比例，關閉時寬高可各自自由調整（見主規劃書 14.1 節）；不論此開關開或關，畫面內容一律響應式重排、不會整體拉伸變形 |
| 外觀模式 | RadioButton×3 | 深色／淺色／跟隨系統，預設「跟隨系統」；切換即時套用全畫面（見主規劃書 34.1 節） |
| 使用者名稱 | 唯讀文字 | 不可編輯的顯示欄位，非輸入控制項，與裝置代碼同樣由 IT 於安裝時分配 |
| 裝置代碼 | 唯讀文字 | 不可編輯的顯示欄位，非輸入控制項 |
| [建立本機備份] | Button | 開啟 6.0a 節備份選項視窗 |
| 閃爍 | ToggleSwitch（卡片） | 立即生效；鎖定時停用 |
| 靜音時段 | ToggleSwitch + 時段列表（卡片，橫跨兩欄） | 每個時段一組 起始/結束 TimePicker + 刪除按鈕；[+新增時段] 上限 5 組；欄位較多故獨占一整行，不與其他項目並排 |
| 播放音效（每等級一組） | CheckBox×4 | 低/中/高/最高級各一個，預設不勾選 |
| 選擇音效（每等級一組） | ComboBox×4 | 列出偵測到的 Windows 系統音效名稱；未勾選對應「播放音效」時停用（灰階） |
| 試聽（每等級一組） | IconButton×4 | 點擊播放目前選擇的音效一次；未選擇音效或開關未勾選時停用 |

### 6.0 使用邏輯

```text
鎖定判斷時機：
  每次進入設定頁時，依本機管理者設定（Settings 表 Locked 欄位）決定哪些項目顯示 🔒
  管理者於 8 節管理者頁調整鎖定狀態時，若使用者當下正開著設定頁，即時反映（不需重新整理頁面）

「使用者名稱」「裝置代碼」：
  兩者皆為安裝時由 IT 一次輸入完成的唯讀欄位，設定頁不提供編輯介面
  如需變更（例如換人使用這台電腦），需聯繫 IT，走「清空本機設定 → 重新跑首次使用設定流程」的路徑
    （見主規劃書 19C 節），不是在設定頁直接修改文字後即時生效

靜音時段新增/刪除：
  時段之間允許重疊（不做衝突檢查，交由使用者自行判斷，維持工具單純性）
  結束時間必須晚於起始時間，否則該筆時段列顯示紅框且不計入生效範圍

「建立本機備份」點擊：
  開啟 6.0a 節選項視窗，讓使用者決定是否包含管理者密碼，再產生 .calbak 檔案

音效設定（對應主規劃書 18.1 節）：
  「選擇音效」下拉選單開啟時，即時查詢 Windows 登錄檔取得目前系統可用的提示音效清單，
    查詢失敗或清單為空時顯示「無可用音效」，該等級「播放音效」開關維持停用
  勾選「播放音效」但尚未選擇音效時：下拉選單自動展開提示使用者選擇，「試聽」按鈕維持停用
  點擊「試聽」：立即播放目前選擇的音效一次（不影響任何通知狀態，純預覽用途）
  四個等級的音效設定完全獨立，互不影響；變更後即時儲存於本機 SQLite，無需另外按「儲存」
```

### 6.0a 建立本機備份選項視窗

對應主規劃書 19C.2 節。點擊「建立本機備份」後彈出的選項視窗：

```text
┌───────────────────────────────┐
│  建立本機備份                ✕ │
├───────────────────────────────┤
│  將打包以下內容：               │
│  ✓ 裝置代碼與使用者名稱          │
│  ✓ 個人偏好設定                │
│  ✓ 本機任務                    │
│  ✓ 簽收紀錄                    │
│                                 │
│  ☐ 包含管理者密碼               │
│     （新電腦可直接沿用同一組密碼）│
│                                 │
│       [ 取消 ]  [ 建立備份 ]    │
└───────────────────────────────┘
```

```text
勾選「包含管理者密碼」時，額外彈出密碼輸入框要求驗證目前管理者密碼，
  驗證失敗則取消勾選並提示「密碼錯誤，未包含管理者密碼」，但仍可繼續建立不含密碼的備份
點擊 [建立備份] → 系統另存新檔對話框，預設檔名 backup_{裝置代碼}_{yyyyMMdd}.calbak
```

#### 6.0a 元件清單

| 元件 | 類型 | 說明 |
| --- | --- | --- |
| 打包內容清單 | 唯讀勾選列表 | 固定四項（裝置身分/個人設定/本機任務/簽收紀錄），皆為必包含，非使用者可取消 |
| 包含管理者密碼 | CheckBox | 預設不勾選；勾選時觸發密碼驗證輸入框 |
| [取消] | Button | 關閉視窗，不產生任何檔案 |
| [建立備份] | Button | 觸發打包並跳出另存新檔對話框 |

#### 6.0a 使用邏輯

```text
勾選「包含管理者密碼」：
  彈出密碼輸入框（見 7 節登入樣式），驗證成功才真正勾選；驗證失敗則保持未勾選狀態

[建立備份] 點擊：
  1. 打包 identity.json + settings.json + tasks.csv + acklog.csv（見主規劃書 19C.2 節格式）
  2. 若已通過密碼驗證，額外打包 admin.key
  3. 壓縮為 .calbak，跳出另存新檔對話框供使用者選擇存放位置
  4. 完成後 Toast 提示「備份已建立：{檔名}」
```

---

## 6.1 首次使用設定視窗（FirstRunSetup）

對應主規劃書 19C、19C.1、19C.2 節。程式第一次啟動時，在主視窗開啟前彈出的一次性視窗，未完成前不可跳過。裝置代碼**不自動產生**，由 IT 於安裝時分配並手動輸入，確保同一台電腦重灌幾次代碼都不會變：

```text
┌───────────────────────────────┐
│  歡迎使用 Cloud Alarm Overlay   │
├───────────────────────────────┤
│  裝置代碼（由 IT 提供）          │
│  [________________________]   │
│                                 │
│  請輸入您的名稱                 │
│  （用於通知簽收紀錄，之後可於    │
│   設定頁修改）                  │
│  [________________________]   │
│                                 │
│  換電腦或重灌？                 │
│  [ 匯入備份檔（.calbak） ]      │
│                                 │
│           [ 開始使用 ]          │
└───────────────────────────────┘
```

```text
「裝置代碼」為必填欄位，由 IT 於安裝時口頭 / 書面告知，使用者依指示填入（例如 EMP-001）
點擊「匯入備份檔」→ 開啟檔案選取視窗，篩選 `*.calbak`
  → 讀取成功：解壓縮並依主規劃書 19C.2 節順序還原（裝置身分/個人設定/本機任務/簽收紀錄/管理者密碼），
    自動帶入裝置代碼與名稱欄位，顯示還原摘要（例如「已匯入 12 筆本機任務」），
    使用者確認後點擊「開始使用」
  → 讀取失敗（檔案格式錯誤、非本程式產生的檔案等）：顯示錯誤訊息，維持手動輸入模式
```

```text
「名稱」欄位留空亦可送出（非必填），此時後續畫面顯示裝置代碼本身作為代稱
不需要密碼、不需要任何驗證，裝置代碼也不做唯一性檢查（由 IT 自行確保不重複分配）
此視窗僅在「本機從未設定過裝置代碼」時出現，重灌或清除 SQLite 才會再次觸發
  → 重灌後只要重新輸入「同一組」IT 分配的裝置代碼，即完全等同重灌前的身分，
    這是本機制的主要使用情境（見主規劃書 19C.1）
「匯入備份檔」為進階情境：連裝置代碼都不記得，或想順便帶回本機任務/簽收紀錄/管理者密碼時使用（見主規劃書 19C.2）
```

### 6.1 元件清單

| 元件 | 類型 | 驗證規則 |
| --- | --- | --- |
| 裝置代碼 | TextBox | 必填（除非透過匯入帶入），不做格式或唯一性驗證，僅檢查非空白 |
| 名稱 | TextBox | 選填，上限 20 字 |
| [匯入備份檔（.calbak）] | Hyperlink/Button | 開啟系統開啟檔案對話框，篩選 `*.calbak` |
| [開始使用] | Button | 裝置代碼為空時停用（灰階），輸入後才可點擊 |

### 6.1 使用邏輯

```text
視窗顯示時機：
  程式啟動流程中，在 MainWindow 顯示前檢查本機 Settings 表是否已有裝置代碼，
  沒有 → 顯示本視窗並阻塞主流程；有 → 直接略過，正常進入主視窗

[開始使用] 點擊：
  1. 驗證裝置代碼非空白（前端檢查）
  2. 寫入本機 SQLite Settings 表（僅本機保留，不觸發任何網路請求，見主規劃書 19C 節）
  3. 關閉本視窗，顯示 MainWindow

匯入備份檔流程：
  點擊匯入且成功讀檔 → 依主規劃書 19C.2 節順序解壓縮還原 → 直接覆蓋兩個輸入框的目前內容
    （即使使用者已手動打了字），並顯示還原摘要（任務筆數、是否含管理者密碼等）
  匯入後仍停在本視窗，使用者確認欄位內容無誤才需再按一次 [開始使用]（不自動送出）
  若備份檔內含 admin.key：於摘要中提示「此備份包含管理者密碼，將一併還原」

異常情況：
  使用者關閉視窗（若允許 ✕）等同取消，下次啟動會再次顯示本視窗（不留部分輸入的殘值）
  建議 UI 不提供 ✕ 關閉按鈕，僅能透過完成設定或匯入離開，避免使用者卡在「沒有身分」的狀態反覆彈出
```

---

## 7. 管理者登入 Dialog

對應主規劃書第 15 節。單一「管理者」身分，不再區分管理員/IT：

```text
┌───────────────────────────┐
│  🔒 身分驗證             ✕ │
├───────────────────────────┤
│  帳號                      │
│  [_______________________]│
│                            │
│  密碼                      │
│  [•••••••••••••••••••••••]│
│                            │
│      [ 驗證並解鎖 ]        │
└───────────────────────────┘
```

```text
驗證失敗：輸入框下方顯示紅字錯誤訊息「帳號或密碼錯誤」，不鎖定帳號（小規模內部工具，不需要防爆破機制）
驗證成功：Dialog 關閉，左側導覽動態新增「管理者」項目（見 2.1 節），進入管理者頁
15 分鐘無操作：自動登出，導覽項目消失，需重新輸入密碼（不影響其他頁面持續運作）
```

### 7.0 元件清單

| 元件 | 類型 | 驗證規則 |
| --- | --- | --- |
| 帳號 | TextBox | 必填 |
| 密碼 | PasswordBox | 必填，輸入時遮蔽顯示 |
| [驗證並解鎖] | Button | 帳號或密碼為空時停用 |

### 7.0 使用邏輯

```text
開啟時機：未登入時點擊左側導覽常駐的「🔒 管理者專區」項目，或點擊 System Tray 的「管理者登入」（見 2.1、10 節）

驗證流程：
  1. 前端檢查帳號密碼非空
  2. 呼叫 AuthenticationService，比對本機 Users 表
  3. 密碼以 PBKDF2/Argon2id 雜湊比對（見主規劃書 30 節），不明碼傳輸或記錄

失敗處理：
  每次失敗僅顯示通用錯誤「帳號或密碼錯誤」，不區分是帳號不存在還是密碼錯（避免帳號枚舉）
  失敗不清空帳號欄位，僅清空密碼欄位，方便使用者重試

成功後：
  Dialog 關閉，觸發 AuditLog 記錄「管理者登入」（見 8.4 節）
  15 分鐘計時器啟動，期間任何滑鼠/鍵盤操作重置計時
  計時歸零 → 自動登出，返回一般使用者畫面；「管理者專區」導覽項目仍顯示，重新點擊需登入
    （若使用者當下正停留在管理者子頁，畫面自動導回首頁，避免停留在已無權限的頁面）
```

---

## 8. 管理者頁 Admin

對應主規劃書第 16 節。**單一管理者身分**，登入後只處理「這台電腦本身」的技術設定，不包含任何全公司範圍的資料——公司政策、警示等級核准、Sheet A／Employees 通知上限例外一律改到 Google Sheet 上直接編輯（見主規劃書 19B、19.1 節），程式內完全不提供這些內容的檢視或編輯畫面。左側次導覽切換三個子頁：

```text
┌─────────────────────────────────────┐
│ [同步來源][本機設定][系統紀錄]        │
├─────────────────────────────────────┤
│                                       │
│  （對應子頁內容）                      │
│                                       │
└─────────────────────────────────────┘
```

### 8.1 同步來源

對應主規劃書 19B 節「兩份 Google Sheet」架構：Sheet A（僅 IT 可編輯，公司核心設定，底下含 Tasks／Holidays／Employees 三個分頁）與 Sheet B（開放一般使用者編輯，團隊任務），程式對兩者皆為**唯讀**：

```text
Sheet A（僅 IT 可編輯・公司核心設定）
  SpreadsheetId    [1AbCdEfGhIjKlMnOpQrStUvWxYz____]  [測試連線]
  Tasks 分頁 GID          [123456]
  Holidays 分頁 GID       [234567]
  Employees 分頁 GID      [567890]

Sheet B（開放團隊成員編輯・團隊任務）
  SpreadsheetId    [2XyZ...____________________]     [測試連線]
  Tasks 分頁 GID          [456789]

同步頻率                [45] 秒
                        [立即同步]  [鎖定連結設定 ☑]

最後同步狀態： ● 正常  20:54:30  取得 7 筆任務、7 位員工資料
```

```text
「測試連線」點擊後即時顯示結果（成功／失敗＋錯誤原因），不影響現有已同步資料
欄位對應主規劃書 20.1「依標題名稱解析」：此頁可顯示目前偵測到的欄位清單，
  若缺少必要欄位（Id/Time/Title/Enabled），以紅字列出缺少項目
「鎖定連結設定」勾選後，上方欄位變唯讀，避免非預期被修改
Sheet A 與 Sheet B 分開設定、分開測試連線，其中一個失敗不影響另一個
  Sheet A 底下的三個分頁（Tasks／Holidays／Employees）雖同屬一次測試連線，仍各自獨立讀取——
  若 Employees 分頁讀取失敗（分頁被誤刪、GID 錯誤），僅影響通知上限例外與 Tasks 分頁以部門為
  鍵值的過濾規則，見主規劃書 19.2 節容錯行為，不影響 Tasks／Holidays 分頁本身的讀取與任務顯示
```

#### 8.1 元件清單

| 元件 | 類型 | 驗證規則 |
| --- | --- | --- |
| Sheet A SpreadsheetId | TextBox | 必填；鎖定時唯讀 |
| Sheet A 各分頁 GID | TextBox×3 | 必填，需為數字（Tasks／Holidays／Employees） |
| Sheet B SpreadsheetId | TextBox | 必填；鎖定時唯讀 |
| Sheet B Tasks 分頁 GID | TextBox | 必填，需為數字 |
| 同步頻率 | NumericUpDown | 範圍 30-300 秒，預設 45 |
| [測試連線]（各自一個） | Button | 點擊後 disabled 並顯示 Spinner，完成後還原；Sheet A / Sheet B 各自獨立 |
| [立即同步] | Button | 觸發一次完整 SyncWorker 流程（同時讀取 Sheet A 與 Sheet B） |
| 鎖定連結設定 | CheckBox | 勾選後所有 SpreadsheetId / GID 欄位變唯讀 |

#### 8.1 使用邏輯

```text
測試連線：
  發送一次 CSV 匯出請求至對應分頁 GID，成功則額外顯示偵測到的欄位清單
    （Tasks 分頁顯示「Id, Time, Title, Level, Enabled」；Employees 分頁顯示「Id, Name」）
  失敗顯示具體原因：連線逾時 / 404（SpreadsheetId 或 GID 錯誤）/ 403（未設為公開檢視）
  Sheet A、Sheet B 各自獨立測試；Sheet A 底下的各分頁亦各自回報成功/失敗，互不影響

儲存時機：
  各欄位失焦即暫存於畫面狀態，須點擊 [立即同步] 或等待下次自動同步才會真正套用新設定並驗證
  未經測試連線成功即點擊立即同步：仍允許執行，同步失敗會寫入 SyncLogs 並於畫面顯示錯誤（不阻擋操作）

其中一份 Sheet（或 Sheet A 底下某一分頁）讀取失敗：
  其餘仍正常運作並顯示於「我的任務」頁（見 4 節），失敗的來源僅在本頁與同步狀態卡片顯示錯誤，
  不影響已快取的舊資料（見主規劃書 29 節錯誤處理原則）
```

### 8.2 本機設定

管理者可鎖定「這台電腦」上開放給一般使用者的部分設定項目（對應設定頁第 6 節「通知」區塊的鎖定狀態），純本機範圍，不影響其他電腦：

```text
閃爍效果可否由使用者自行調整        ☑ 開放
靜音時段是否可由使用者自行設定       ☑ 開放
閃爍速度（本機）                    [500] ms （200~5000 可調）
```

不提供「全螢幕模式偏好」「置頂」的鎖定開關：兩者不是使用者設定頁上存在的欄位（見 6 節），
  而是由通知等級規則直接決定（見主規劃書 18 節），管理者頁自然也無項可鎖

#### 8.2 元件清單

| 元件 | 類型 | 說明 |
| --- | --- | --- |
| 各鎖定開關 | ToggleSwitch | 每項獨立即時生效，關閉即鎖定對應設定頁項目（顯示 🔒） |
| 閃爍速度 | NumericUpDown / Slider | 範圍 200-5000（ms） |

#### 8.2 使用邏輯

```text
每個開關切換時：
  立即寫入本機 Settings 表（Locked 欄位），設定頁（見 6 節）對應項目即時反映 🔒 狀態
  同時寫入本機 AuditLogs（見 8.4 節）

此頁僅影響「這台電腦」，不會同步套用到其他 9 台電腦——若要全公司統一某項設定，
  需個別在每台電腦的管理者頁設定，或由 IT 於安裝時建立標準化的初始設定檔（超出第一版範圍）
```

### 8.3 系統紀錄（同步紀錄 / 系統事件）

原主規劃書第 12-13 節內容，僅顯示這台電腦自己的紀錄（一般使用者的歷史紀錄頁只顯示提醒紀錄，見 5 節）：

```text
┌─────────────────────────────────────────────────────────┐
│ [ 同步紀錄 ] [ 系統事件 ]                                 │
├─────────────────────────────────────────────────────────┤
│ 20:54:30  同步成功  取得 7 筆任務、5 位員工資料（A+B）       │
│ 20:54:20  同步成功  無變更                                 │
│ 20:54:10  同步失敗  網路連線逾時（Sheet B）                 │
└─────────────────────────────────────────────────────────┘
```

```text
「同步紀錄」分頁：時間軸列表，同步成功/失敗以顏色列標示，失敗項目可展開看錯誤訊息，
  並標示是 Sheet A 或 Sheet B 的同步結果
「系統事件」分頁：篩選器（程式啟動/結束、資料庫錯誤、同步失敗、警示開關、設定變更、管理者登入）
支援日期區間篩選、匯出 CSV
```

#### 8.3 元件清單

| 元件 | 類型 | 說明 |
| --- | --- | --- |
| 分頁籤（同步紀錄／系統事件） | TabControl | 各自獨立的篩選狀態，切換分頁不互相影響 |
| 同步紀錄列表 | ListView，唯讀 | 失敗列可點擊展開詳細錯誤堆疊/訊息 |
| 系統事件篩選器 | 多選 CheckBox 群組 | 預設全選；至少需保留一項勾選，全部取消時自動恢復全選並提示 |
| 日期區間 | 兩個 DatePicker | 兩分頁共用同一組區間控制項 |
| [匯出 CSV] | Button | 匯出目前分頁 + 篩選條件下的資料；兩分頁各自匯出各自的檔案，不合併 |

#### 8.3 使用邏輯

```text
資料來源：本機 SQLite 的 SyncLogs 與系統事件表，僅顯示「這台裝置」自己的紀錄，不跨裝置彙整
  （因系統不做集中式資料庫，見主規劃書 21 節設計原則）

失敗列展開：
  點擊該列 → 下方展開詳細錯誤訊息（例如完整例外堆疊或 HTTP 狀態碼），再次點擊收合

篩選器互動：
  取消最後一個勾選項目時，即時跳回全選狀態並提示「至少需選擇一項」

匯出 CSV（格式與流程統一見主規劃書 21.1 節）：
  「同步紀錄」分頁匯出 SyncLog_{起始日}_{結束日}.csv，欄位：Time, Source, Status, Message, RecordCount
  「系統事件」分頁匯出 SystemEvent_{起始日}_{結束日}.csv，欄位：Time, EventType, Message
    （僅匯出目前勾選的事件類型，未勾選的類型不包含在檔案中）
  切換分頁後 [匯出 CSV] 按鈕作用對象自動跟隨目前作用中的分頁，不需另外選擇
```

### 8.4 Audit Log

```text
[起始日期▾] 至 [結束日期▾]                          [ 匯出 CSV ]
┌─────────────────────────────────────────────────────────┐
│ 時間               操作人   項目                舊值    新值 │
│ 2026-09-14 14:20   admin   本機閃爍速度          500ms  800ms│
│ 2026-09-14 10:05   admin   Sheet A SpreadsheetId 舊Id   新Id │
└─────────────────────────────────────────────────────────┘
```

```text
記錄這台電腦上管理者的所有設定變更（僅本機，見主規劃書 31 節）
```

#### 8.4 元件清單

| 元件 | 類型 | 說明 |
| --- | --- | --- |
| Audit Log 表格 | DataGrid，唯讀 | 依時間新到舊排序，不可編輯或刪除任何一列 |
| 日期區間篩選 | 兩個 DatePicker | 預設「最近 30 天」 |
| [匯出 CSV] | Button | 匯出目前日期區間篩選下的所有列 |

#### 8.4 使用邏輯

```text
本頁為唯讀稽核紀錄，不提供刪除功能（確保稽核完整性）
資料量隨時間增長，超過一定筆數（例如 5000 筆）時建議僅預設顯示最近區間，避免一次載入過多資料造成卡頓

匯出 CSV（格式與流程統一見主規劃書 21.1 節）：
  預設檔名 AuditLog_{起始日yyyyMMdd}_{結束日yyyyMMdd}.csv
  欄位：CreatedAt, UserId, Action, OldValue, NewValue
  資料量大時（>1000 列）顯示短暫進度提示，避免使用者誤以為沒反應
  查無資料時仍可匯出（產生僅含標題列的檔案），不停用按鈕
```

---

## 9. 通知視窗（四種等級）

對應主規劃書第 18、22-24 節。這是本系統最核心的畫面，四種等級外觀差異需明確區分。

### 9.1 低級 —— 便利貼・免確認

```text
                                    ┌───────────────────┐
                                    │ ℹ 產出週報          │
                                    │ 18:30               │
                                    │              [確認] │
                                    └───────────────────┘
                                    （右下角，10 秒後自動消失）
```

```text
位置：螢幕右下角，多筆時由下往上堆疊
不 Topmost，可被其他視窗覆蓋
右上角無 ✕，僅右下角一個「確認」按鈕（點擊立即關閉），不點擊則 10 秒後自動消失
消失時有淡出動畫（200-300ms）
```

#### 9.1 元件清單

| 元件 | 類型 | 說明 |
| --- | --- | --- |
| 標題文字 | 唯讀 | 任務名稱，超長時單行截斷加省略號 |
| 時間文字 | 唯讀 | ScheduledAt 的時間部分 |
| [確認] | Button | 點擊立即關閉並寫入 Acknowledged |
| 自動關閉計時器 | 內部邏輯，無 UI | 10 秒倒數，無視覺進度條（維持簡潔） |

#### 9.1 使用邏輯

```text
觸發：AlarmWorker 判定任務到期 → 建立視窗，加入堆疊佇列末端（見 9.6 節多視窗排列規則）
點擊 [確認]：寫入 AcknowledgementLogs（Result=Acknowledged）→ 視窗淡出關閉
10 秒無操作：自動視為「已讀」寫入 AcknowledgementLogs（Result=Acknowledged，非 Overdue，
  因為低級本就設計為非強制關注，不記錄為逾時）
使用者滑鼠移到視窗上時：暫停自動關閉倒數，移開後繼續（避免使用者正要點擊卻突然消失）
```

### 9.2 中級 —— 便利貼・需確認

```text
                                    ┌───────────────────┐
                                    │ ● 團隊同步會議提醒   │
                                    │ 18:00                │
                                    │              [確認] │
                                    └───────────────────┘
                                    （右下角，Topmost，不會自動消失）
```

```text
外觀與低級相同尺寸，但邊框改用 Primary 藍色以區分等級，且 Topmost 置頂
必須點擊「確認」才會關閉；不會自動消失
```

#### 9.2 元件清單

| 元件 | 類型 | 說明 |
| --- | --- | --- |
| 標題文字 | 唯讀 | 任務名稱 |
| 時間文字 | 唯讀 | ScheduledAt 的時間部分 |
| [確認] | Button | 唯一的關閉方式 |

#### 9.2 使用邏輯

```text
與低級的關鍵差異：無自動關閉機制、且 Topmost（不會被其他視窗蓋住）
使用者若允許 Snooze（見主規劃書 23.1 節）：按鈕旁另外提供 [稍後提醒▾] 下拉，
  選擇延後 5/10/15 分鐘後視窗關閉，SnoozedUntil 到期後重新彈出同一則通知
```

### 9.3 高級 —— 60% 螢幕・點確認

```text
        ┌──────────────────────────────────────────────┐
        │                                                │
        │     ⚠ 重要通知                                 │
        │                                                │
        │     每日備份確認                                │
        │     18:30:00                                   │
        │                                                │
        │                                                │
        │                  [ 我已閱讀，確認關閉 ]          │
        │                                                │
        └──────────────────────────────────────────────┘
        （螢幕置中，約佔 60% 寬高，Topmost，Warning 黃色邊框）
```

```text
視窗佔螢幕約 60%，置中顯示，Topmost
邊框 / 標題色使用 Warning 黃，與最高級的 Danger 紅做出區隔
單一按鈕「我已閱讀，確認關閉」，點擊即關閉並寫入簽收紀錄
不可用 ESC / Alt+F4 直接關閉（比照最高級的關閉行為限制，僅按鈕可關閉）
若佇列中還有其他高級/最高級通知：視窗角落顯示「還有 N 個待確認通知」
```

#### 9.3 元件清單

| 元件 | 類型 | 說明 |
| --- | --- | --- |
| 標題 | 唯讀 | 固定文字「⚠ 重要通知」 |
| 任務名稱 / 時間 | 唯讀 | 置中顯示 |
| [我已閱讀，確認關閉] | Button | 唯一互動元件，視窗上無其他可點擊區域 |
| 佇列提示文字 | 唯讀，條件顯示 | 僅佇列中還有其他通知時出現 |

#### 9.3 使用邏輯

```text
視窗開啟：Topmost 強制顯示於前景，攔截 Alt+Tab 切走後仍保持在最上層（下次切回前景）
關閉限制：ESC、Alt+F4、右上角（本視窗無標題列故無 ✕）均無效，僅 [我已閱讀，確認關閉] 有效
點擊確認：寫入 AcknowledgementLogs（Result=Acknowledged）→ 視窗關閉 → 若佇列非空，
  依主規劃書 27 節排隊規則接續顯示下一筆（見 9.6 節）
```

### 9.4 最高級 —— 全螢幕・驗證碼

對應主規劃書第 22-24 節，是既有規劃中最詳細的部分，此處補完視覺配置。

```text
┌────────────────────────────────────────────────────────┐
│░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░│  ← 紅黑閃爍背景
│░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░│
│░░░░░░░░░░░░░░░░░░ ⚠ 注意！任務到期 ░░░░░░░░░░░░░░░░░░░░│
│░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░│
│░░░░░░░░░░░░░ 團隊下班進度同步會議 ░░░░░░░░░░░░░░░░░░░░░░│
│░░░░░░░░░░░░░░░░░░░░░ 18:00:00 ░░░░░░░░░░░░░░░░░░░░░░░░░│
│░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░│
│░░░░░░░░░░ 請輸入以下文字確認已閱讀： ░░░░░░░░░░░░░░░░░░░│
│░░░░░░░░░░░░░░░░░░░░░ R7K2-419 ░░░░░░░░░░░░░░░░░░░░░░░░░│
│░░░░░░░░░░░░░░░ [______________] ░░░░░░░░░░░░░░░░░░░░░░░│
│░░░░░░░░░░░░░ [ 驗證並關閉提醒 ] ░░░░░░░░░░░░░░░░░░░░░░░░│
│░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░│
└────────────────────────────────────────────────────────┘
90% 以上螢幕覆蓋 / WindowStyle=None / Topmost / 紅黑交替閃爍 500ms
```

```text
輸入框自動取得 Focus，驗證碼輸入錯誤時輸入框短暫紅色抖動提示，不清空已輸入內容
輸入正確後：畫面停止閃爍 → 短暫顯示「✓ 已確認」→ 視窗關閉
佇列提示位置：畫面右上角小字「還有 N 個待確認通知」，不與中央內容重疊
```

#### 9.4 元件清單

| 元件 | 類型 | 驗證規則 |
| --- | --- | --- |
| 標題 / 任務名稱 / 時間 | 唯讀 | 置中顯示 |
| 驗證碼顯示文字 | 唯讀 | 8 字元格式 `XXXX-XXX`（見主規劃書 23 節產生規則） |
| 驗證碼輸入框 | TextBox | 自動 Focus，不分大小寫比對，即時逐字比對或送出時比對（見下方邏輯） |
| [驗證並關閉提醒] | Button | 點擊觸發比對；輸入框為空時仍可點擊但立即顯示錯誤（不預先 disabled，維持鍵盤 Enter 也能觸發的一致性） |
| 佇列提示文字 | 唯讀，條件顯示 | 僅佇列非空時顯示 |

#### 9.4 使用邏輯

```text
視窗開啟：
  WindowStyle=None、Topmost、ShowInTaskbar=False，佔滿 90%+ 螢幕（見主規劃書 22 節）
  背景以 500ms 間隔紅黑交替閃爍（管理者可於本機調整速度，見 8.2 節）
  輸入框自動取得鍵盤焦點，使用者可直接輸入不需先點擊

驗證流程：
  點擊 [驗證並關閉提醒] 或按下 Enter：
    比對輸入框內容與產生的驗證碼（忽略大小寫、忽略頭尾空白）
    正確 → 畫面停止閃爍（背景固定為深色）→ 顯示「✓ 已確認」0.5 秒 → 寫入 AcknowledgementLogs
      （Result=Acknowledged）→ 視窗關閉
    錯誤 → 輸入框短暫紅色抖動動畫（約 300ms）→ 不清空輸入內容 → 保持 Focus 於輸入框，
      允許使用者直接修正繼續輸入（不強制重新整個輸入）

關閉限制：
  ESC、Alt+F4、Alt+Tab 切走再切回仍保持 Topmost，均無法關閉視窗
  嘗試用工作管理員強制結束程式：不在 UI 規劃範圍內防範（見主規劃書 23 節「不是安全驗證」的定位聲明）

多任務排隊時的視窗切換：
  目前視窗確認關閉後，若佇列非空，主規劃書 27 節排隊邏輯立即建立下一個最高級/高級視窗
  兩個視窗切換之間無明顯延遲或動畫轉場（直接關閉舊視窗、開啟新視窗）
```

### 9.5 四等級外觀對照表

| 等級 | 尺寸 | 位置 | Topmost | 主色 | 關閉方式 |
| --- | --- | --- | --- | --- | --- |
| 低級 | 名片大小 | 右下角 | 否 | 灰藍 | 點確認 或 10 秒自動關閉 |
| 中級 | 名片大小 | 右下角 | 是 | Primary 藍 | 僅點確認 |
| 高級 | 60% 螢幕 | 置中 | 是 | Warning 黃 | 僅點確認 |
| 最高級 | 90%+ 全螢幕 | 全螢幕 | 是 | Danger 紅（紅黑閃爍） | 僅輸入驗證碼 |

### 9.6 跨等級共通：觸發與佇列狀態機

四種等級共用同一套底層觸發流程，差異僅在於顯示樣式與關閉條件（見主規劃書 27 節）：

```text
狀態流轉：
  Pending → Triggering → Displayed → Acknowledged

Pending → Triggering：
  AlarmWorker 判定 ScheduledAt 已到期，透過 DB Transaction 鎖定該筆任務，防止重複觸發
  （同一任務即使因排程誤差被檢查兩次，也只會產生一個通知視窗）

Triggering → Displayed：
  低級 / 中級：直接建立視窗並顯示（可與其他低/中級視窗並存，見下方堆疊規則）
  高級 / 最高級：先檢查是否已有其他高級/最高級視窗正在 Displayed
    有 → 新任務進入等待佇列（不立即開窗），現有視窗角落更新「還有 N 個」提示
    沒有 → 立即建立視窗並顯示

Displayed → Acknowledged：
  使用者完成對應等級的關閉條件（點確認 / 輸入驗證碼 / 逾時自動確認）
  寫入 AcknowledgementLogs，若為高級/最高級且佇列非空，觸發佇列中下一筆進入 Displayed
```

```text
低級 / 中級視窗堆疊規則：
  多筆同時存在時，由螢幕右下角由下往上堆疊排列，每筆間距固定（例如 8px）
  堆疊超過螢幕可容納高度時：最舊的視窗自動收合成一條「還有 N 則低優先通知」的摘要列，
    點擊摘要列展開完整清單（避免通知疊到蓋住整個螢幕）
```

---

## 10. System Tray 選單

對應主規劃書第 25 節。

```text
┌────────────────┐
│ 開啟主視窗       │
│ 立即同步         │
│ 新增任務         │
│ 測試通知    ▸    │ → 低級/中級/高級/最高級 子選單（同 3.1 節測試邏輯）
│ 設定            │
│ 管理者登入       │
│ 關於            │
│─────────────────│
│ 結束程式         │
└────────────────┘
```

```text
Tray 圖示本身依同步狀態變化：正常=一般圖示 / 同步失敗=圖示加紅點角標 / 有未簽收最高級通知=閃爍圖示提醒
滑鼠 hover 圖示顯示 tooltip：「Cloud Alarm Overlay － 下一個提醒 18:00 團隊同步會議」
「管理者登入」為常駐可見選項（非隱藏連結），點擊直接開啟 7 節登入 Dialog（見 2.1 節）
```

### 10.0 元件清單

| 元件 | 類型 | 說明 |
| --- | --- | --- |
| 開啟主視窗 | MenuItem | 顯示並前景化 MainWindow（若已隱藏於 Tray） |
| 立即同步 | MenuItem | 同 3 節 [⟳ 立即同步] 行為 |
| 新增任務 | MenuItem | 直接開啟 4.1 節 Dialog（新增模式），不需先切到「我的任務」頁 |
| 測試通知 | MenuItem（含子選單） | 子選單四個等級選項，同 3.1 節邏輯 |
| 設定 | MenuItem | 開啟主視窗並導向 6 節設定頁 |
| 管理者登入 | MenuItem | 開啟 7 節登入 Dialog；已登入時文字動態改為「登出管理者」 |
| 關於 | MenuItem | 開啟關於視窗（顯示版本號、版權資訊） |
| 結束程式 | MenuItem | 二次確認「確定要結束 Cloud Alarm Overlay 嗎？結束後將不會顯示任何提醒」 |

### 10.0 使用邏輯

```text
「管理者登入」狀態切換：
  未登入：點擊 → 開啟 7 節登入 Dialog
  已登入：文字變為「登出管理者」，點擊直接登出（不需二次確認，低風險操作）

雙擊 Tray 圖示：等同點擊「開啟主視窗」

「結束程式」與「關閉主視窗」的差異：
  關閉主視窗（✕）依設定僅縮小至 Tray，程式與背景服務持續運作
  「結束程式」才是真正終止程式，AlarmWorker 停止運作，此後不會再有任何通知（含最高級）
```

---

## 11. 更新提示 Dialog

對應主規劃書第 19A 節。

```text
┌───────────────────────────────┐
│  有新版本可更新              ✕ │
├───────────────────────────────┤
│  目前版本：v1.1.0               │
│  最新版本：v1.2.0               │
│  更新內容：修正同步錯誤          │
│                                 │
│     [ 稍後提醒 ]  [ 前往下載 ]  │
└───────────────────────────────┘
```

```text
「前往下載」開啟預設瀏覽器導向 downloadUrl（Google Drive 固定連結），不在程式內下載安裝
「稍後提醒」：關閉 Dialog，下次啟動再檢查一次（不做「本次不再提醒」的永久靜音，避免版本一直卡舊的）
此 Dialog 不 Topmost、不強制，使用者可以先關掉繼續工作
```

---

## 12. 響應式與多螢幕注意事項

```text
主視窗最小尺寸建議：1024×640，小於此寬度時左側導覽收合成純圖示（無文字）
高級 / 最高級通知視窗：依主規劃書 28 節，第一版僅於主螢幕顯示；
  多螢幕設定選項（主螢幕/所有螢幕/指定螢幕）放在「警示政策」子頁（見 8.3）
低級 / 中級便利貼：固定顯示在「主要工作螢幕」（即滑鼠當前活動螢幕），避免使用者在副螢幕看不到
```

---

## 13. 待實作階段補完事項

以下留待實際 XAML 開發時再決定，本文件不預先鎖定：

```text
- 精確的像素尺寸、Grid/StackPanel 版面細節
- 動畫曲線細節（淡入淡出、閃爍的 easing）
- 便利貼堆疊超過螢幕高度時的捲動或收合行為
- Fluent Design 的 Acrylic/Mica 特效是否啟用（視效能實測結果決定）
```
