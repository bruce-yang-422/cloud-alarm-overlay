# Cloud Alarm Overlay 問題與優化追蹤

記錄實際使用後發現的 Bug、操作瑕疵、新需求與調整要求。實機測試項目見 [驗收清單](驗收清單.md)；某項驗收未通過時，在此建立一筆紀錄，避免只留下未勾選的方框。

v1.0.0 已發布且有多台電腦安裝。修正或新增功能若需重新交付安裝檔，必須使用更高版本號，不覆蓋既有 Release。

## 記錄方式

1. 每件事給一個不重複的編號，例如 `CAO-001`。先在下方總覽新增一列，再複製詳細紀錄範本填寫。
2. 類型使用「Bug／瑕疵／需求／調整」；狀態使用「待確認／待處理／進行中／待驗收／已完成／不採納」。
3. 優先級：`P0` 無法正常使用或資料有風險、`P1` 主要流程受阻、`P2` 可繞過但影響體驗、`P3` 細節改善。目標版本在排定前填「未定」。
4. 「已完成」需記錄修正版本與實機驗收結果；只完成程式修改時維持「待驗收」。
5. 本專案儲存庫為公開內容。回報時請遮蔽員工姓名、裝置代碼、密碼、金鑰與含個資的日誌。

## 總覽

| 編號 | 類型 | 問題或需求摘要 | 影響版本 | 優先級 | 狀態 | 目標版本 | 更新日期 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CAO-001 | 需求 | 從 1～12 月複選提醒月份，搭配指定日期與時間 | 1.0.0 | P2 | 待處理 | 未定 | 2026-09-17 |
| CAO-002 | 調整 | 節氣只在節氣當日顯示，其餘日期顯示 `-` | 1.0.0 | P2 | 待處理 | 未定 | 2026-09-17 |
| CAO-003 | 需求 | 提供可選擇的粉紅色系外觀 | 1.0.0 | P3 | 待確認 | 未定 | 2026-09-17 |
| CAO-004 | Bug | 「開啟下載頁」在沒有新版本時按下無反應 | 1.0.0 | P2 | 待處理 | 未定 | 2026-09-17 |
| CAO-005 | 需求 | 內建「任務產生器」表單工具，供同事免手動編輯 Sheet 欄位 | 1.0.0 | P2 | 待驗收 | 未定 | 2026-09-17 |

## 詳細紀錄

### CAO-001｜支援選擇 1～12 月的指定日期提醒

- 類型：需求
- 發現日期：2026-09-17
- 最後更新：2026-09-17
- 回報來源：同事使用需求
- 影響版本：1.0.0
- 環境：任務重複規則；本機任務編輯器目前只能設定每月某一天
- 優先級：P2
- 狀態：待處理
- 目標版本：未定

**現象或使用情境**

同事需要「每個奇數月 10 日」提醒；其他人也可能需要偶數月，或每季指定的月份。使用者應能像選日期一樣，直接從 1～12 月勾選要提醒的月份。目前「每月」只能指定日期，會在每個月都觸發。

**目前結果與期望結果**

目前：`Monthly:10` 代表每個月 10 日，無法直接表達奇數月 10 日。

期望：使用者可複選 1～12 月，再指定日期與時間；只在所選月份的指定日期觸發。不另外設計「每 N 個月」或起算月規則。

**影響與佐證**

目前需拆成多筆單次任務或另行維護，修改日期與通知內容時容易漏改。

**預定調整與驗收條件**

調整方式：擴充重複規則與任務編輯介面，提供 1～12 月複選控制項，沿用現有的指定日期與時間輸入。新規則需能儲存、顯示及計算下一次觸發；保留既有 `Monthly:10` 等每月規則。若讓 Google Sheets 任務使用相同規則，須同步定義 CSV `Recurrence` 格式。

邊界規則：未勾選月份時不可儲存；指定日期在所選月份不存在時，沿用現有每月規則，該月不觸發。Sheet A／B 是否提供新規則的 CSV 寫法，於實作時一併確認。

- [ ] 可以直接複選 1～12 月；例如奇數月選 1、3、5、7、9、11，偶數月選 2、4、6、8、10、12，每季可自行選 1、4、7、10 或其他組合。
- [ ] 只在所選月份的指定日期與時間觸發，未選月份不觸發；跨年後規則仍正確。
- [ ] 儲存、重啟與重新編輯後，月份、日期與時間不變；未選月份不能儲存，不存在的日期該月略過。
- [ ] UI 清楚顯示已選月份及下一次觸發時間；既有「每月」任務的行為不變。
- [ ] 若納入雲端任務，Sheet A／B 的 CSV 同步與排程結果也通過驗證。

**處理紀錄**

- 修正 commit／PR：未開始
- 修正版本：未定
- 自動測試：未執行
- 實機驗收日期與結果：未驗收

### CAO-002｜節氣只在節氣當日顯示

- 類型：調整
- 發現日期：2026-09-17
- 最後更新：2026-09-17
- 回報來源：實際使用回饋
- 影響版本：1.0.0
- 環境：首頁日期與節氣區塊
- 優先級：P2
- 狀態：待處理
- 目標版本：未定

**現象或使用情境**

首頁目前顯示當下所處節氣區間的名稱；節氣日過後，仍會顯示前一個節氣。原本希望只有節氣當日才顯示名稱。

**目前結果與期望結果**

目前：若今天沒有節氣，程式會向前查找最近 30 天的節氣並顯示其名稱。

期望：只在當日資料的 `SolarTerm` 有值時顯示節氣名稱；其他正常日期顯示 `-`。

**影響與佐證**

非節氣日仍顯示節氣名稱，容易讓人誤以為今天就是該節氣的日期。

**預定調整與驗收條件**

調整方式：首頁僅讀取當日 `SolarTerm`，不再向前查找節氣區間；資料讀取失敗時仍應能辨識為錯誤，避免與正常的 `-` 混淆。

相依項目或風險：需確認跨日更新及離線快取仍以當天日期判斷。

- [ ] 節氣當日顯示該節氣名稱；當日欄位為空時顯示 `-`。
- [ ] 節氣日後一天、跨月及跨年時都不沿用上一個節氣名稱。
- [ ] 程式跨午夜、重新啟動與離線使用時，首頁顯示與當日資料一致。

**處理紀錄**

- 修正 commit／PR：未開始
- 修正版本：未定
- 自動測試：未執行
- 實機驗收日期與結果：未驗收

### CAO-003｜提供粉紅色系外觀

- 類型：需求
- 發現日期：2026-09-17
- 最後更新：2026-09-17
- 回報來源：同事許願
- 影響版本：1.0.0
- 環境：設定 → 外觀與資料；目前只有淺色、深色、跟隨系統
- 優先級：P3
- 狀態：待確認
- 目標版本：未定

**現象或使用情境**

有同事希望程式提供粉紅色版本的外觀，讓使用者可依喜好切換。這是新增主題的需求，並非目前主題顯示錯誤。

**目前結果與期望結果**

目前：可選「淺色／深色／跟隨系統」，沒有粉紅色系選項。

期望：在外觀設定中增加可選的粉紅色系主題，原有主題仍可使用；選擇後主要畫面、任務編輯器及設定頁維持一致的配色。

**影響與佐證**

目前無法使用偏好的粉紅色介面；不影響提醒與同步功能。

**預定調整與驗收條件**

調整方式：先確認配色範圍與視覺稿，再擴充主題選項及共用色彩資源。通知等級原本用來區分緊急程度的顏色應維持易辨識。

待確認：粉紅色是淺色主題的完整變體，還是只調整強調色；是否也需要對應的深色粉紅版本。

- [ ] 外觀設定可選粉紅色系，切回淺色、深色或跟隨系統也正常。
- [ ] 選擇後重啟程式仍保留設定；跟隨系統模式的行為不受影響。
- [ ] 首頁、任務、番茄鐘、設定與管理者頁的文字、按鈕、選取、停用及焦點狀態清楚可讀。
- [ ] 四種提醒等級仍能清楚區分；通知確認與強制通知警示不因新主題而失去辨識度。

**處理紀錄**

- 修正 commit／PR：未開始
- 修正版本：未定
- 自動測試：未執行
- 實機驗收日期與結果：未驗收

### CAO-004｜「開啟下載頁」在沒有新版本時按下無反應

- 類型：Bug
- 發現日期：2026-09-17
- 最後更新：2026-09-17
- 回報來源：實際使用回饋
- 影響版本：1.0.0
- 環境：設定 → 外觀與資料 → 軟體更新；目前版本與公開 `version.json` 均為 1.0.0
- 優先級：P2
- 狀態：待處理
- 目標版本：未定

**現象或使用情境**

在「軟體更新」區塊按「開啟下載頁」，瀏覽器沒有開啟，也沒有任何提示。「檢查更新」顯示已是最新版本後再按，結果相同。

**目前結果與期望結果**

目前：`CheckUpdateAsync` 在最新版本不高於目前版本時回傳 `null`；`OpenDownload` 遇到 `AvailableUpdate == null` 便直接返回，因此按鈕看起來沒反應。尚未檢查更新時也會如此。

期望：只要已設定且可讀取有效的公開更新資訊，按鈕就能開啟其中的 `downloadUrl`，不應要求先找到更高版本；無法取得連結或開啟瀏覽器失敗時，應顯示原因。

**影響與佐證**

使用者無法從程式內前往下載頁，需自行查找安裝檔連結。程式邏輯位置：`MaintenanceViewModel.OpenDownload` 與 `UpdateCheckService.CheckAsync`。

**預定調整與驗收條件**

調整方式：讓下載頁操作可獨立取得並開啟公開更新資訊中的下載連結，同時提供讀取中、連結無效及開啟失敗的明確回饋；保留「檢查更新」只在有較新版本時通知的行為。

- [ ] 目前版本與 `version.json` 相同時，按「開啟下載頁」仍會開啟正確連結。
- [ ] 未先按「檢查更新」時也可使用；連結未設定、讀取失敗或無效時有明確提示。
- [ ] 有新版本時仍開啟該版本的下載連結，且不影響原本的更新提示。

**處理紀錄**

- 修正 commit／PR：未開始
- 修正版本：未定
- 自動測試：未執行
- 實機驗收日期與結果：未驗收

### CAO-005｜內建「任務產生器」表單工具

- 類型：需求
- 發現日期：2026-09-17
- 最後更新：2026-09-17
- 回報來源：同事使用需求
- 影響版本：1.0.0
- 環境：主視窗側邊欄、系統匣選單；Sheet B（團隊共用）Tasks 分頁與 Sheet A（IT 專用）Employees 分頁
- 優先級：P2
- 狀態：待驗收
- 目標版本：未定

**現象或使用情境**

同事不熟悉、也不習慣直接在 Google Sheet 上手動填寫 Tasks 分頁的英文欄位（`Id`／`Time`／`Level`／`Recurrence` 等），容易打錯格式（例如日期格式、`TRUE`/`FALSE`、`LunarDay:n` 語法），或不清楚 `TargetDeviceOrName`／`ExcludeDeviceOrName` 該填部門、姓名還是裝置代碼。

**目前結果與期望結果**

目前：需要直接在 Sheet 上編輯，容易打錯欄位格式或誤刪第 2 列中文說明。

期望：提供一個像表單一樣、靠下拉選單與按鈕組合出每一筆任務的小工具，組好後可預覽、可匯出成 `.xlsx` 或複製純資料列，直接貼到 Google Sheet；目標／排除對象可從公司員工名單挑選（部門＋職稱雙篩選），避免手動輸入打錯。

**影響與佐證**

降低同事手動編輯 Sheet 造成的格式錯誤風險（例如漏打 `TRUE`/`FALSE`、日期格式錯誤、`Recurrence` 語法錯誤），也避免因為誤解部門/姓名/裝置代碼三種鍵值而發錯或漏發通知。

**預定調整與驗收條件**

調整方式：

1. 新增 `task_builder_tailwind.html`（獨立表單頁面，Tailwind 樣式），欄位對照 `Sheet範例/README.md` 的 Tasks 分頁格式與規則（等級、`Recurrence` 語法含 `Weekly:n`／`LunarDay:n`、`TargetDeviceOrName`/`ExcludeDeviceOrName` 的部門／姓名／裝置代碼比對規則），可匯出 `.xlsx`（含中英文雙層表頭）或複製純資料列（不含表頭，方便貼進既有 Sheet）。
2. 目標／排除對象提供「從名單挑選部門／人員」彈出視窗，用部門＋職稱雙篩選交叉挑人，也可整個部門一鍵帶入。
3. 為避免 Sheet 的 `SpreadsheetId`／`GID` 寫死在獨立 HTML 檔案中外流，改由程式內建的 `TaskBuilderHostService`（`src/CloudAlarmOverlay.App/Services/TaskBuilderHostService.cs`）用 `HttpListener` 提供本機頁面與 `/api/tasks-csv`、`/api/employees-csv` 兩個代理端點；端點內部讀取管理者頁「同步來源」已設定好的 `SyncOptions`（`SheetBId`/`TasksBGid`、`SheetAId`/`EmployeesGid`），透過既有 `ISheetCsvClient.DownloadAsync` 向 Google 取得 CSV 再轉傳，頁面本身不含任何 Sheet 網址或 ID。
4. `task_builder_tailwind.html` 透過 `.csproj` 的 `<Content>` 規則隨建置複製到輸出目錄，安裝程式會一併打包，不需要另外部署到公開網路或請同事安裝 Python／Node。
5. 入口：主視窗側邊欄「番茄鐘」下方新增「任務產生器」項目、系統匣右鍵選單新增「開啟任務產生器」，皆呼叫 `TaskBuilderHostService.OpenInBrowser()` 以系統瀏覽器開啟。

相依項目或風險：若管理者尚未在「同步來源」設定 Sheet A／B 的 `SpreadsheetId`／`GID`，代理端點會回傳 409 並顯示中文提示，而非顯示原始網址或例外訊息。

- [ ] 側邊欄「任務產生器」與系統匣「開啟任務產生器」皆可正常以系統瀏覽器開啟本機頁面。
- [ ] 頁面可讀取管理者已設定的 Sheet A／B 連線資訊並顯示既有任務與員工名單，且頁面原始碼中不含任何 Sheet 網址或 ID。
- [ ] 尚未設定同步來源時，頁面顯示清楚的中文提示，不顯示技術性錯誤訊息。
- [ ] 目標／排除對象挑選器可依部門與職稱雙篩選正確交叉出人員清單，並正確帶入分號分隔的鍵值。
- [ ] 匯出的 `.xlsx` 與複製的純資料列格式與現有 Sheet 表頭（含第 2 列中文說明）相容，貼上後程式同步正常讀取。
- [ ] 新增任務編號會自動避開本機清單與雲端既有任務的重複編號。

**處理紀錄**

- 修正 commit／PR：未提交
- 修正版本：未定
- 自動測試：未執行（`dotnet build` 通過，0 警告 0 錯誤）
- 實機驗收日期與結果：未驗收

## 新項目範本

複製下列範本，每件事建立一個小節；不適用的欄位可填「不適用」。

```md
### CAO-006｜簡短標題

- 類型：Bug／瑕疵／需求／調整
- 發現日期：YYYY-MM-DD
- 最後更新：YYYY-MM-DD
- 回報來源：使用者／驗收／系統紀錄
- 影響版本：例如 1.0.0
- 環境：Windows 版本、顯示縮放、安裝或免安裝版；必要時記錄是否連網
- 優先級：P0／P1／P2／P3
- 狀態：待確認／待處理／進行中／待驗收／已完成／不採納
- 目標版本：未定

**現象或使用情境**

簡述何時發生；Bug 請寫能重現的操作步驟及發生頻率。

**目前結果與期望結果**

目前：

期望：

**影響與佐證**

說明受影響的流程或人數；附上去識別化截圖、錯誤訊息或相關日誌路徑。

**預定調整與驗收條件**

調整方式：

相依項目或風險：

- [ ] 可觀察的完成條件 1
- [ ] 可觀察的完成條件 2

**處理紀錄**

- 修正 commit／PR：
- 修正版本：
- 自動測試：
- 實機驗收日期與結果：
```
