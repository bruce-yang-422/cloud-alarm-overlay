<Window x:Class="CloudAlarmOverlay.App.Views.IdentityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:adaptive="clr-namespace:CloudAlarmOverlay.App.Styles"
        Title="歡迎使用 Cloud Alarm Overlay"
        Width="520" Height="560" MinWidth="380" MinHeight="300" ResizeMode="CanResize"
        Icon="/CloudAlarmOverlay.App;component/Assets/Brand/LogoCloud.png" WindowStartupLocation="CenterScreen" Background="{adaptive:AdaptiveBrush #F7FAFE}" Foreground="{adaptive:AdaptiveBrush #16243E}"
        FontSize="15" FontFamily="Segoe UI, Microsoft JhengHei UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/LightTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="0,0,8,0">
        <Image Source="/CloudAlarmOverlay.App;component/Assets/Brand/LogoSquare.png" Height="65" HorizontalAlignment="Left" Stretch="Uniform" Margin="0,0,0,10"/><TextBlock Text="設定這台電腦" Style="{StaticResource Heading}"/>
        <TextBlock Text="請輸入 IT 提供的裝置代碼與顯示名稱。通知將依此識別對象，設定後無法在此修改。"
                   Style="{StaticResource Muted}" Margin="0,8,0,22"/>
        <TextBlock Text="裝置代碼"/>
        <TextBox x:Name="DeviceCode" Text="{Binding DeviceId,UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="裝置代碼"/>
        <TextBlock Text="顯示名稱"/>
        <TextBox x:Name="DisplayNameInput" Text="{Binding DisplayName,UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="顯示名稱"/>
        <Button x:Name="ImportButton" Content="匯入備份檔（.calbak）" HorizontalAlignment="Left" Margin="0,8"/>
        <TextBlock Text="{Binding Error}" Foreground="{adaptive:AdaptiveBrush #BD2430}" TextWrapping="Wrap" MinHeight="26"/>
        </StackPanel>
        </ScrollViewer>
        <Button x:Name="StartButton" Grid.Row="1" Content="開始使用" Command="{Binding SaveCommand}"
                Style="{StaticResource Primary}" MinHeight="44" Margin="0,16,0,0"/>
    </Grid>
</Window>
