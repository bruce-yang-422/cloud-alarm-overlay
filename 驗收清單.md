# Cloud Alarm Overlay 實機驗收清單

此清單用於新電腦安裝及往後版本升級。`[ ]` 代表仍需在實際 Windows 電腦確認；自動測試通過不等於實機驗收完成。未通過的項目請在[問題與優化追蹤](問題與優化追蹤.md)記錄現象與驗收條件。已發布的 v1.0.0 不覆蓋，後續更新使用更高版本號。

## 發布前

- [ ] 執行 `dotnet test CloudAlarmOverlay.sln`，確認全部測試通過。
- [ ] 使用高於 `version.json` 目前 `latestVersion` 的版本號打包，例如 `./installer/build-installer.ps1 -Version 1.1.0`。
- [ ] 在測試機安裝新版本，確認程式顯示的版本、安裝檔檔名及 GitHub Release 標籤一致。
- [ ] 上傳後比對 GitHub Release 安裝檔的 SHA-256；再更新並推送 `version.json` 的版本、下載連結、雜湊與發布說明。
- [ ] 若資料庫結構有變更，新增遷移檔，確認既有資料升級成功；不要改寫已發布版本使用的 `V1.sql`。

## 安裝與升級

- [ ] 在乾淨電腦安裝，輸入裝置代碼與顯示名稱後可進入主畫面。
- [ ] 在已安裝電腦升級，確認任務、歷史、裝置身分、管理者帳密與個人設定保留。
- [ ] 重開機並登入 Windows 後，程式依管理者的自動啟動設定出現在系統匣；工作列與捷徑顯示 Logo。
- [ ] 關閉主視窗後提醒仍持續；從系統匣可開啟主視窗，結束程式時依目前設定要求確認或密碼。
- [ ] 在測試電腦解除安裝再安裝，確認安裝行為與本機資料保留符合預期。

## 同步與任務

- [ ] 分別測試 Sheet A、Sheet B 的連線；按「立即同步」後可在狀態與紀錄看到結果，任務載入清單。
- [ ] 暫時離線再恢復網路，確認既有快取仍可使用，重新同步後資料更新。
- [ ] 確認 Sheet A 的工作日 09:50、15:00「提供銷售數據」依指定對象與假日規則觸發。
- [ ] 確認 Sheet B 的單次任務「中秋節祝福」在 2026-09-24 17:00 觸發一次；若驗收日期已過，改以新測試任務驗證單次排程。
- [ ] 本機任務可新增、編輯、複製、啟停與刪除；雲端任務唯讀，但可選取並複製成本機任務。
- [ ] 多筆勾選後等待清單更新，選取狀態不會自行清除；狀態開關只影響本機任務。

## 提醒與設定

- [ ] 預覽一般、重要、緊急、強制通知；實際觸發時尺寸、內容、確認方式與紀錄符合預期。
- [ ] 強制通知的九位數字確認碼可用 `xxx-xxx-xxx` 或連續九位輸入；錯碼不能關閉。
- [ ] 試聽音效時按鈕有反饋；靜音時段與管理者鎖定設定正確，強制通知不被靜音略過。
- [ ] 番茄鐘在切頁、收進系統匣及休眠喚醒後仍能正確計時與記錄。
- [ ] 淺色、深色及 Windows 125%／150% 縮放下，主要畫面、通知與對話框文字可讀、按鈕可操作。
- [ ] 管理者登入、登出、閒置逾時及退出密碼保護正常；安裝後變更預設管理者密碼。
- [ ] 在測試電腦建立備份並還原，確認資料與設定完整；重置功能只在測試電腦驗證。

## v1.1.0 重點回歸

- [ ] 從 v1.0.0 覆蓋安裝到 v1.1.0，任務、身分、帳密及歷史保留，資料庫升級到 V2。
- [ ] 每月可複選月份；確認跨年、未選月份、2 月 31 日略過與下一次提醒正確。
- [ ] 每天／工作日／每週／每月／農曆的過去起始時間可以儲存；新增過去單次任務仍拒絕。
- [ ] 任務只顯示時、分輸入；編輯儲存後秒數為 00，舊任務未儲存前不改動。
- [ ] 節氣只在當日顯示，隔日顯示 `-`。
- [ ] 明暗模式（淺色／暗色／跟隨系統）與色彩風格（預設／粉紅／若竹）可獨立選擇，重啟、備份還原及系統明暗變化時保留色彩偏好；125%／150% 縮放下日期、月份、通知、預覽可讀。
- [ ] 未先檢查更新或版本相同時，「開啟下載頁」仍能開啟；離線或無效網址有原因提示。
- [ ] 雙擊本機／雲端／歷史任務名稱可查看完整唯讀內容，不新增確認紀錄；舊歷史缺少快照時有提示。
- [ ] 正常輪詢無異動不增加日誌，資料更改或錯誤／復原有紀錄，相同錯誤累計次數；手動清理保留失敗與確認紀錄。
- [ ] 側邊欄可分辨排程正常與單一 Sheet 異常／中斷，恢復後自行更新；未設定來源顯示未設定。
- [ ] 從側邊欄與系統匣啟動任務產生器，可選月份／部門／職稱、避開既有編號、複製多行內容與匯出 Excel。

## 驗收紀錄

| 裝置代碼 | 版本 | Windows／縮放 | 驗收日期 | 結果與問題 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |
