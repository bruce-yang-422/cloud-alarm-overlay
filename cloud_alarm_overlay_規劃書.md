# Cloud Alarm Overlay 程式設計規劃書

## 1. 專案名稱

**Cloud Alarm Overlay**
雲端同步全螢幕鬧鐘 / 公司級通知與簽收系統

---

## 2. 專案目的

本專案旨在開發一套 Windows 桌面通知與鬧鐘系統，解決一般通知容易被忽略、隨手關閉、缺乏簽收紀錄等問題。

系統核心目標：

* 從 Google Sheet 或其他雲端資料來源同步任務
* 在指定時間主動提醒使用者
* 重要通知可使用全螢幕強制顯示
* 透過「閱讀確認碼」降低使用者無腦關閉通知的情況
* 保留通知觸發與簽收紀錄
* 支援公司政策、角色與權限管理
* 支援本機任務新增、修改與刪除
* 支援管理員鎖定重要設定

---

## 3. 系統定位

Cloud Alarm Overlay 並非單純鬧鐘，而是偏向：

> **Windows 企業通知、任務提醒、強制閱讀與簽收工具**

主要處理：

* 公司重要公告
* 工作排程提醒
* 例行維運通知
* 下班前檢查事項
* 會議提醒
* 生產 / 倉儲 / IT 維運提醒
* 需要被確認閱讀的重要訊息

---

## 4. 目標使用者 TA

### 4.1 一般員工

需求：

* 查看自己的任務
* 接收公司通知
* 新增個人本機提醒
* 完成通知簽收
* 查看自己的歷史紀錄

限制：

* 不可修改公司同步來源
* 不可修改公司政策
* 不可刪除公司級通知

---

### 4.2 主管

需求：

* 接收重要通知
* 查看通知內容
* 不一定需要全螢幕強制警示
* 查看工作排程與部分紀錄

例如：

```text
主管角色
通知模式：置頂通知
全螢幕：關閉
閱讀確認：啟用
聲音：啟用
```

---

### 4.3 管理者（單機管理，見 19 節）

公司政策、警示等級核准、員工清單與通知上限例外等**公司層級**設定，全部改由 IT／管理者直接在 Google Sheet（Sheet A）上編輯（見 19B、20 節），不透過程式介面操作，也不做任何回寫。因此程式內不再區分「管理員（政策面）」與「IT（技術面）」兩種身分，合併為單一**管理者**身分，登入後只處理**這台電腦本身**的技術設定：

需求：

* 設定這台電腦的 Sheet A 連結（SpreadsheetId、各分頁 GID、同步頻率）
* 查看這台電腦的同步狀態、版本、系統事件紀錄
* 手動觸發這台電腦的立即同步 / 更新檢查

不涉及：任何跨裝置或全公司範圍的資料（公司政策內容、其他使用者的例外設定、其他裝置清單等）——這些一律透過 Sheet A 直接編輯，不在本程式的管理者頁面中出現。

---

## 5. 技術架構

### 開發平台

```text
Language        C#
Framework       .NET 10+
UI              WPF
Architecture    MVVM
Database        SQLite
OS              Windows 10 / Windows 11
```

建議：

```text
.NET 10+
WPF
CommunityToolkit.Mvvm
Microsoft.Data.Sqlite
Dapper 或 EF Core
Microsoft.Extensions.Hosting
Microsoft.Extensions.DependencyInjection
Microsoft.Extensions.Logging
```

---

# 6. 系統架構

```text
Cloud Alarm Overlay
│
├── WPF UI
│   ├── MainWindow
│   ├── Dashboard
│   ├── Tasks
│   ├── History
│   ├── Settings
│   ├── Admin
│   └── AlarmOverlayWindow
│
├── Application Layer
│   ├── AlarmService
│   ├── TaskService
│   ├── SyncService
│   ├── PolicyService
│   ├── UserService
│   └── AuditService
│
├── Infrastructure
│   ├── SQLite
│   ├── Google Sheet Client
│   ├── System Tray
│   ├── Windows Startup
│   └── Notification / Sound
│
└── Background Services
    ├── SyncWorker
    ├── AlarmWorker
    └── HealthCheckWorker
```

---

# 7. UI 架構

主程式目前共有六個主要導覽項目：

```text
首頁
我的任務
歷史紀錄
番茄鐘
設定
管理者專區（側欄單一入口，右側五個 Tabs）
```

另有：

```text
全螢幕通知視窗
System Tray
Dialog
Toast Notification
```

---

# 8. 首頁 Dashboard

## 目的

快速查看目前系統狀態與下一個通知。

## 顯示內容

```text
下一個提醒

18:00
團隊下班進度同步會議
距離：00:05:12
```

```text
今日待辦

7 個
3 等待中
4 已簽收
```

```text
同步狀態

Google Sheet 正常
最後同步 20:54:30
```

```text
SQLite

正常
27 筆資料
```

---

## 即將到期

顯示未來最近數筆任務：

| 時間    | 任務     | 來源    | 狀態  |
| ----- | ------ | ----- | --- |
| 18:00 | 團隊同步會議 | Sheet | 等待中 |
| 18:30 | 產出週報   | Sheet | 等待中 |
| 19:00 | 關閉設備   | 本機    | 等待中 |

---

## 快速操作

```text
新增本機任務
立即同步
測試警示
```

---

# 9. 操作頁

## 目的

完整管理任務。

功能：

* 新增
* 編輯
* 刪除
* 搜尋
* 篩選
* 啟用 / 停用
* 測試

---

## 任務類型

### 本機任務

可以：

```text
新增
修改
刪除
停用
複製
```

---

### 雲端任務

原則上：

```text
唯讀
```

修改須回到 Google Sheet。

畫面顯示：

```text
此任務由 Google Sheet 管理
請至來源修改
```

---

# 10. 新增本機任務

欄位：

```text
日期
時間
任務名稱
任務描述
備註
是否啟用
提醒方式
重複規則
國定假日是否略過
```

例如：

```text
日期
2026/09/14

時間
18:30

任務
每日備份確認

提醒模式
全螢幕

需要閱讀確認
是

重複規則
每天

國定假日略過
是
```

---

## 10.1 重複規則（Recurrence）

避免每天/每週固定的任務要手動一筆筆新增，任務支援重複規則：

```text
不重複        None
每天          Daily
每個工作日     Weekly:1,2,3,4,5    (快捷選項，本質是「每週指定幾天」的預設常用組合，見下方說明)
每週指定幾天   Weekly:1,3,5        (1=一 … 7=日)
每月固定日     Monthly:15
農曆指定幾天   LunarDay:1,15       (依農曆日期排程，例如初一十五，詳見 20.2b 節)
```

```text
「每個工作日」為新增的快捷選項，不是獨立的儲存格式：
  選擇後實際存入 Recurrence 欄位的值就是 Weekly:1,2,3,4,5，與使用者自己手動勾選週一至週五完全等價
  這麼設計的理由：AlarmWorker / SyncWorker 判斷觸發時只需要認得「Weekly:n,n,n」這一種格式，
    不需要為「工作日」另外寫一套判斷邏輯，也不會有兩種格式代表同一件事、日後判斷邏輯要維護兩套的問題
  UI 上呈現為獨立可選的一個選項（見 UI 規劃書 4.1 節），避免使用者每次都要自己勾 5 個框，
    但底層儲存與比對邏輯完全共用「每週指定幾天」的既有機制
  編輯既有任務時：若任務的 Recurrence 剛好是 Weekly:1,2,3,4,5，UI 應直接判定並選中「每個工作日」，
    而非顯示為「每週指定幾天」再列出五個打勾的星期，減少使用者困惑
```

```text
運作方式：
- Tasks 表的一筆「範本」資料只需建立一次，Enabled 期間內持續生效
- AlarmWorker / SyncWorker 於每次判斷觸發時，
  依 Recurrence 規則計算「今天/本次」是否落在規則內，
  並各自產生獨立的 AcknowledgementLogs 紀錄（同一範本可累積多筆歷史簽收紀錄）
- 修改重複規則的任務 = 修改範本本身，不需逐筆修改歷史紀錄
```

Google Sheet 任務同樣支援 `Recurrence` 欄位（見 20 節），公司排程性任務可直接在 Sheet 建一次即可長期使用。

---

# 11. 紀錄頁

## 目的

提供通知、簽收與系統操作的追蹤能力。

分成：

```text
提醒紀錄
同步紀錄
系統事件
```

---

## 提醒紀錄

儲存：

```text
TaskId
任務名稱
預定時間
觸發時間
簽收時間
簽收結果
裝置
使用者
來源
```

例如：

```text
任務
團隊進度會議

預定
18:00:00

觸發
18:00:01

簽收
18:01:17

耗時
76 秒
```

---

# 12. 同步紀錄

例如：

```text
20:54:30
同步成功
取得 7 筆任務

20:54:20
同步成功
無變更

20:54:10
同步失敗
網路連線逾時
```

---

# 13. 系統事件

紀錄：

```text
程式啟動
程式結束
資料庫錯誤
同步失敗
警示開啟
警示關閉
設定變更
管理者登入
```

---

# 14. 設定頁

設定頁主要處理「個人偏好」。

---

## 一般

```text
Windows 登入後自動啟動（管理員專區可開關，預設開啟）

啟動後縮小至 System Tray

關閉主視窗時縮小至 Tray

視窗縮放時強制等比例（見 14.1 節）
```

---

## 14.1 視窗大小與縮放行為

主視窗預設寬高比為 **3:2**（例如 1200×800、1080×720 等比例），這是產品外觀的預設基準，但不鎖死視窗大小本身——使用者仍可自由拖曳視窗邊框調整大小，只是「拖曳時要不要強制維持 3:2 比例」由一個獨立開關控制：

```text
「視窗縮放時強制等比例」開關（見 14 節「一般」設定，預設開啟）：
  開啟時：拖曳視窗任一邊框或角落調整大小，寬高會同步縮放維持 3:2 比例
    （例如拖寬變成 1500px，高度自動連動調整為 1000px，反之亦然）
  關閉時：寬與高各自獨立自由調整，不受 3:2 比例限制，行為等同一般 Windows 視窗

預設開啟的理由：多數使用者不會刻意去拉出奇怪的比例，預設鎖等比可以避免版面被拉伸變形的機率，
  但保留關閉選項給真的需要自訂比例（例如想要特別窄長或特別寬扁）的使用者，不強制所有人接受同一種行為
```

**不論視窗被拉伸成什麼大小、什麼比例（無論「強制等比」開關是否開啟），畫面內容一律以響應式版面重新排列，絕不使用整體縮放（`transform: scale`）或拉伸點陣圖的方式呈現**：

```text
具體規則：
  文字字級（字體大小）固定不隨視窗縮放而放大或縮小，任何情況下都不會出現文字被拉長、壓扁、模糊的情形
  卡片、表格、按鈕等元件的邊框圓角、內距、圖示大小維持固定數值，不隨視窗縮放
  視窗變大時：版面透過既有的 Grid／Flex 版面（見 UI 規劃書各頁面章節）讓元件之間的間距與可用寬度增加，
    或讓原本擠在一起的內容有更多留白，而非把既有元件等比放大
  視窗變小時：版面依既有的響應式規則重新排列（例如雙欄改為觸發捲動、卡片內文字自然換行），
    絕不透過縮小字體或壓扁卡片來硬塞進更小的空間
  這與 UI 規劃書多次強調的「不考慮手機響應式」並不衝突——本產品雖然不特別為手機版面設計，
    但仍需要在「使用者自行調整 PC 視窗大小」這個常見情境下維持版面不跑版、不變形，
    兩者是不同範疇的需求（前者是刻意排除的裝置類型，後者是同一裝置類型下的正常操作彈性）
```

---

## 通知

```text
閃爍
靜音時段 / 專注模式（見 23.2）
各等級音效開關 + Windows 系統音效選擇（低級 / 中級 / 高級 / 最高級，各自獨立，見 18.1 節）
```

不提供「置頂」「全螢幕模式」開關：這兩項與通知等級直接掛鉤，由等級本身決定（見 18 節：
  低級不置頂、名片大小、可被其他視窗蓋住；中級同為名片大小但置頂；高級 60% 螢幕置頂；
  最高級 90% 以上全螢幕置頂），是刻意的低干擾/高強制分級設計，不開放使用者調整、也不需要管理者鎖定
  （因為壓根不存在讓使用者自行切換的欄位），避免使用者誤改破壞各等級原本的干擾程度區隔。

如果某設定被公司政策鎖定：

```text
🔒 此設定由公司管理員控制
```

一般使用者不可修改。

---

# 13A. 番茄鐘

獨立於任務提醒系統之外的個人專注力小工具，左側導覽插在「設定」之前。純本機功能，不涉及 Google Sheet、不涉及裝置身分，也不寫入 AcknowledgementLogs（與正式任務簽收紀錄分開，避免混淆「任務有沒有被簽收」與「番茄鐘用了幾次」這兩件不同的事）。

## 目的

```text
提供簡單的專注/休息計時器，幫助使用者切分工作段落
與主系統的任務提醒功能完全獨立運作，兩者互不影響、互不阻擋
```

## 基本規則

```text
預設週期：25 分鐘專注 + 5 分鐘休息（可由使用者自行調整）
每 4 個專注週期後，休息時間延長為 15 分鐘（長休息，業界慣例，可關閉此規則改成固定週期）
計時運作期間，使用者可隨時暫停 / 重設 / 提前結束
```

## 週期結束通知

```text
套用現有通知等級中的「中級」規格（見 18 節）：
  右下角便利貼、Topmost、需點擊確認才會關閉，不會自動消失
  音效：外觀借用中級的樣式，但音效開關是**番茄鐘專屬的獨立設定**，不與任務通知的中級開關共用同一個值
    （選擇音效的機制與音效清單來源相同，都是讀取 Windows 系統音效，見 18.1 節，但開/關狀態各自獨立）
    番茄鐘的音效開關**預設開啟**（不同於任務通知四等級預設關閉），
    理由：番茄鐘是使用者主動啟動的個人小工具，開啟音效能讓使用者第一次使用就注意到「原來有聲音提示可以關」，
    比預設關閉、大多數人永遠不會發現這個選項更符合工具的可發現性

專注結束 → 通知「專注時間結束，休息一下吧」，點擊確認後自動開始倒數休息時間
休息結束 → 通知「休息結束，準備開始下一輪專注」，點擊確認後停在待命狀態，
  需使用者手動點擊開始才會進入下一輪專注（不自動連續進行，避免使用者不在座位時空轉）
```

## 與任務通知系統的關係

```text
番茄鐘通知與任務通知（低/中/高/最高級）共用同一套「中級」視覺樣式與佇列顯示區域（見主規劃書 27 節堆疊規則），
  但不算入 AcknowledgementLogs、不參與任務的 Triggering/Displayed/Acknowledged 狀態機，
  是完全獨立的計時流程，只是外觀借用同一套元件
若番茄鐘通知與任務通知同時發生：兩者皆為「中級」等級，依 27 節規則可並存顯示（中級便利貼可多筆並存）
```

## 本機儲存

```text
番茄鐘設定（專注/休息分鐘數、長休息規則開關、音效開關與選用的 Windows 系統音效名稱）
  與使用紀錄（今日完成幾個番茄鐘）存於本機 SQLite，
  新增 PomodoroSettings、PomodoroLog 兩張表（見 21 節），純本機保留，不同步、不回寫
  PomodoroLog 與第 11 節「提醒紀錄」使用的 AcknowledgementLogs 是兩張獨立的表：
    番茄鐘紀錄不進入歷史紀錄頁（UI 規劃書 5 節），也不計入簽收率相關統計，
    僅在番茄鐘頁面自己的側邊卡片顯示「今日完成幾輪」
```

---

# 15. 管理者頁面（單機管理）

原規劃的「管理員」與「IT」兩個獨立身分**合併為單一「管理者」身分**。原因：公司層級的設定（公司政策、警示等級核准、員工清單與通知上限例外）全部改為直接在 Google Sheet A 上編輯（見 19B、20 節），不透過程式介面操作、也不做任何回寫；程式內的管理者頁面因此只剩下**這台電腦本身**的技術設定，不需要再分兩種身分、兩套密碼、兩套子頁。

主視窗左側導覽**常駐顯示**一個「管理者專區」入口（一般使用者也看得到）；未登入時點擊會彈出登入 Dialog，登入成功後右側才顯示管理者頁。左側不展開管理子選單，五個子功能由右側 Tabs 切換，登出按鈕在管理者頁首。

---

## 登入

```text
帳號
[________________]

密碼
[••••••••••••••••]

[驗證並解鎖]
```

支援：

```text
15 分鐘無操作自動鎖定
```

> 說明：因公司電腦員工帳號皆為系統管理員權限，無法以 Windows 帳號 / 使用者群組判斷「誰是程式管理者」，故維持**獨立於 Windows 帳號之外的程式內建密碼系統**（見 30 節 PBKDF2 / Argon2id 雜湊）。全公司可共用同一組管理者密碼，或依需要為每台電腦設定不同密碼（存於本機 Users 表，非集中管理，見 21 節）。

---

# 16. 管理者功能（單機範圍）

```text
這台電腦的同步來源設定（Sheet A 的 SpreadsheetId、各分頁 GID、同步頻率）
這台電腦的同步狀態、版本資訊
這台電腦的系統事件紀錄
手動觸發這台電腦的立即同步 / 更新檢查
```

**不包含**任何跨裝置或全公司範圍的資料——公司政策、警示等級核准、員工清單與通知上限例外、其他裝置清單等，一律透過 Sheet A 直接編輯，不在程式的管理者頁面中出現（見 19 節）。這樣設計的好處：任何一台電腦登入管理者密碼，最多只能影響「這台電腦要讀哪個 Sheet、多久同步一次」，無法看到或竄改全公司的政策與其他人員資料。

---

# 17. 公司政策（改為 Sheet A 直接編輯）

原本規劃在程式內設定的公司政策項目（不可刪除 / 不可修改 / 不可停用 / 必須閱讀確認等），改為在 Sheet A 的 Tasks 分頁**以欄位方式**逐筆任務設定（例如 `RequireAck`、`Locked` 欄位），而非一個全公司統一的開關。IT／管理者直接在 Google Sheet 上編輯這些欄位即可，程式僅讀取套用，不提供程式內的政策設定介面。

---

# 18. 通知等級

分成四個等級，由低到高依重要性遞增。

## 低級（便利貼・免確認・自動關閉）

```text
右下角小視窗，名片大小
不 Topmost（可被其他視窗蓋住）
不需要點確認：10 秒後自動關閉
若使用者主動點擊「確認」，立刻關閉
音效：預設關閉，使用者可於設定頁自行開啟（見 18.1 節）
```

用途：低急迫性、資訊性質的提醒，看不看都不影響業務。

---

## 中級（便利貼・需確認）

```text
右下角小視窗，名片大小
Topmost 置頂
需要點擊「確認」才會關閉，不會自動消失
音效：預設關閉，使用者可於設定頁自行開啟（見 18.1 節）
```

用途：需要使用者知道但不需要強力打斷工作的事項。

---

## 高級（60% 螢幕・點確認）

```text
視窗約佔螢幕 60% 大小，置中顯示
Topmost 置頂
不需要輸入閱讀確認碼，只需點擊「確認」即可關閉
音效：預設關閉，使用者可於設定頁自行開啟（見 18.1 節）
```

用途：**日常較重要任務的預設等級**，比全螢幕溫和但比便利貼更難忽略，避免使用者對全螢幕強制通知產生疲乏。此等級的啟用需求需經主管同意後由 IT 於 Sheet A 上設定該任務的 `Level` 欄位，不開放一般使用者自行改用最高級。

---

## 最高級（全螢幕・驗證碼）

```text
全螢幕（90% 以上螢幕覆蓋）
Topmost
紅黑閃爍
音效：預設關閉，使用者可於設定頁自行開啟（見 18.1 節），與其他等級一致，不強制
閱讀確認碼
禁止直接關閉
```

僅用於真正必要、有明確時效性與業務風險的任務（例如業務未回報銷售數據）。是否可設為此等級須經主管核准，核准後才由 IT 於 Sheet A 上將該任務的 `Level` 設為最高級。

---

## 18.1 通知音效（每等級獨立開關，預設關閉）

原規劃為「全系統不使用警示音」，理由是辦公室環境中部分人會聆聽音樂，音效容易造成干擾。修正後改為**預設關閉、使用者可自行開啟**，四個等級各自獨立開關（例如只想讓最高級有聲音、其他等級維持靜音），滿足「想要額外聽覺提示」的使用者需求，同時不影響不需要音效的人。

```text
每個通知等級（低級 / 中級 / 高級 / 最高級）在設定頁各自有一個獨立的音效開關（見主規劃書 14 節、UI 規劃書 6 節）
預設值：全部關閉（維持原本安靜的辦公室體驗，音效是選用加分項，不是預設行為）
最高級的音效開關與其他等級一致，同樣可自由開關，不強制——因為最高級本身已透過全螢幕 + 紅黑閃爍 +
  禁止直接關閉等視覺 / 互動手段確保被注意到，不需要額外用音效綁架使用者
```

## 音效來源：讀取 Windows 系統音效

```text
不內建/自帶音效檔案，改為讀取 Windows 系統內建的提示音效清單
  （儲存於登錄檔 HKEY_CURRENT_USER\AppEvents\Schemes\Apps\.Default 底下的各個 Scheme，
   例如 .Default（預設提示音）、SystemAsterisk（提示）、SystemExclamation（驚嘆號）、
   SystemNotification（通知）、Notification.Default 等，實際清單依 Windows 版本與使用者主題而異）
使用者於設定頁該等級的音效區塊看到下拉選單，列出偵測到的系統音效名稱，可選擇並試聽
好處：
  不需要额外附帶音效檔案、不佔用安裝檔體積
  音效與使用者當前 Windows 佈景主題一致（換了 Windows 音效主題，程式的提示音也會跟著換）
  若該台電腦找不到任何系統音效（少見情況），下拉選單顯示「無可用音效」，該等級音效開關維持停用狀態
```

---

## 通知等級總覽

| 等級 | 顯示方式 | 需要確認 | 未確認時 | 適用情境 |
| --- | --- | --- | --- | --- |
| 低級 | 右下角便利貼（名片大小） | 否 | 10 秒後自動關閉 | 資訊性質、低急迫性 |
| 中級 | 右下角便利貼（名片大小），Topmost | 是（點擊確認） | 持續顯示，不自動消失 | 需要被看到但不強力打斷 |
| 高級 | 60% 螢幕置中，Topmost | 是（點擊確認） | 持續顯示 | 日常較重要任務，預設等級 |
| 最高級 | 全螢幕（90%+）紅黑閃爍 | 是（輸入閱讀確認碼） | 禁止關閉，持續顯示 | 高風險 / 高時效業務任務，需主管核准 |

---

# 19. 角色策略

第一版簡化為**兩層角色**：

```text
一般使用者      可接收所有等級通知（含最高級全螢幕），可管理自己的本機任務

管理者          單機技術設定（見 15、16 節），不涉及公司層級的政策或例外設定
```

各任務可否套用「高級 / 最高級」等強制性等級，不綁死在角色上，而是**由 Sheet A 上該任務本身的欄位設定決定**（見 19.1），IT／管理者直接在 Sheet 上調整，不需要透過程式介面，也不需要預先設計出一整組角色階層。

例如任務層級設定（Sheet A／Tasks 分頁的欄位值）：

| 任務 / 對象 | 可用最高等級 | 需要閱讀確認碼 |
| ---- | ---: | ---: |
| 一般公司公告 | 中級 | 否 |
| 業務銷售數據提醒 | 最高級 | 是 |
| 特定人員（如主管）之例外設定 | 高級（不套用最高級） | 否 |

## 19.1 員工清單與通知上限（Sheet A／Employees 分頁，直接編輯，不回寫）

原本規劃是獨立的 LevelOverrides 分頁，僅收錄「有例外的人」。改為併入 **Sheet A 底下的 Employees 分頁**（見 19B 節），變成一張完整的員工清單——每位員工都有一列，不需要判斷「有沒有出現在例外表」，查一個人只要看這一張表，不用在多份表之間切換對照，也方便日後擴充部門／英文名等基本資料時不必維護兩份重複的人員清單。Employees 與 Sheet A 底下的 Tasks／Holidays 同屬一份試算表、同一套「僅 IT／管理者可編輯」權限，不需要另外管理一份 Sheet 的共用設定與 SpreadsheetId。

**由 IT／管理者直接在 Google Sheet 上編輯**，程式僅單向讀取套用，不提供程式內編輯介面，也不做任何回寫。因系統不做帳密登入（見 19C 節），比對仍以**裝置代碼或使用者自訂名稱**為鍵值，而非傳統的使用者帳號：

```text
比對用欄位（程式讀取並實際套用邏輯）：Id, Name, MaxAllowedLevel, RequireAckOverride

輔助欄位（僅供 IT 在 Sheet 上肉眼辨識用，程式讀取時忽略，不影響比對結果，Department 除外——
  Department 雖列為輔助欄位，仍會被 19.2 節的部門過濾邏輯讀取，見下方比對規則）：
  Department      部門，方便 IT 依部門分組檢視這張表，也供 19.2 節 Tasks 過濾以部門為鍵值時查表展開
  Title           職稱，純顯示用，方便 IT 從職稱快速判斷這個人是否合理需要例外設定
                    （例如「業務經理」比對照最高級上限，比裸看一個名字更容易確認合理性）
  EnglishName     英文名，部分公司內部系統習慣以英文名對照，純顯示用
  Note            備註，說明為什麼設這筆例外（例如「主管，避免開會時被全螢幕打斷」），
                    方便半年後回頭看這張表時還記得當初為何這樣設定

範例（見 Sheet範例/SheetA_Employees.csv）：
  Id       Name      Department  Title    MaxAllowedLevel  RequireAckOverride  EnglishName    Note
  EMP-001  陳建志     IT          IT主管    高級              否                  -              IT主管，避免全螢幕打斷維運作業
  EMP-003  王小明     業務部      業務主管  高級              否                  Wang Xiaoming  業務主管，開會頻繁避免全螢幕
  EMP-007  行政櫃檯公用 行政部    -        中級              否                  -              公用機器，非特定個人使用
  EMP-010  李國華     業務部      業務經理  高級              否                  Li Guohua      同為主管職，比照王小明降級
  EMP-012  (留空)     倉儲部      -        中級              -                  -              倉儲現場共用電腦
  EMP-020  王小強     業務部      業務專員  -                 -                  -              非主管職，沿用全體預設上限

同一部門內用 Title 快速區隔主管與非主管，是最實用的辨識情境：
  上表業務部四人中，王小明（業務主管）、李國華（業務經理）皆為主管職，MaxAllowedLevel 降為高級，
  避免頻繁開會時被全螢幕打斷；王小強（業務專員）非主管職，沿用全體預設（最高級），
  因為業績數據回報這類任務對第一線業務人員而言需要確實看到、不宜降級
  （若沒有 Title 欄，IT 得另外查一份組織圖才知道誰是主管，這正是新增 Title 欄位要解決的問題）
```

```text
MaxAllowedLevel / RequireAckOverride 填 - 代表這位員工沒有個人例外，沿用「全體預設上限」
  全體預設上限＝程式內建固定值「最高級」（即「不設上限，任務本身是什麼等級就顯示什麼等級」），
    不是另外存在 Sheet 或設定裡的可調整數值——多數員工都沒有個人例外，只有少數需要降級的人
    才會在自己的 MaxAllowedLevel 填實際數值，其餘留 - 即可，減少 IT 日常維護負擔
    （RequireAckOverride 的「不覆寫」同理：沿用該筆任務 Tasks.RequireAck 欄位原本的設定）

Name 欄不強制對應真人——公用電腦（例如倉儲現場、行政櫃檯）不該硬塞一個特定員工的名字，
  避免歷史紀錄查起來像是「某人簽收了所有倉儲通知」但實際操作的可能是當班的任何人：
    可填「XX公用」「XX共用電腦」等描述性文字（例如 EMP-007），不需要是真實姓名
    也可以直接留空（例如 EMP-012），此時該裝置的簽收紀錄與比對邏輯完全以 Id（裝置代碼）為準，
      Name 欄留空不影響任何比對規則，只是少了一個可用的比對鍵值而已
  兩種寫法皆合法，由 IT 依實際情況選擇，程式邏輯不區分處理

比對規則：
  程式同步後，只讀取 Id / Name / MaxAllowedLevel / RequireAckOverride 欄位進行比對，
    優先以「使用者自訂名稱」比對 Name 欄（Name 為空值時此步驟直接跳過，不會誤比對成空字串），
    找不到則以「裝置代碼」比對 Id 欄，皆找不到（此人完全不在 Employees 清單裡）則套用全體預設上限
  找到該員工列後，若 MaxAllowedLevel 為 -，同樣視為沿用全體預設，不特別處理
  Department / EnglishName / Note 等輔助欄位可自由增減，不影響同步邏輯（見 20.1 節「依標題名稱解析」規則：
    新增未知欄位不影響既有解析），純粹是給 IT 自己看的維護資訊，不會顯示在程式任何畫面上

裝置代碼與名稱的對照關係：
  裝置代碼（Id 欄）與使用者姓名的對應，直接體現在這張 Employees 表本身，
  不再需要 IT 另外維護一份 Excel 或紙本對照表（相較舊版設計的簡化）
```

IT／管理者直接在 Sheet 上調整「誰／哪台裝置的通知上限套用到哪一級」，不需要在角色系統中預先窮舉所有職稱，也不需要等使用者完成登入才能設定例外，更不需要透過程式介面操作。

---

## 19.2 任務受眾過濾（TargetDeviceOrName / ExcludeDeviceOrName，Sheet A/B Tasks 分頁）

原本所有任務一律觸發給拉取到這份 Sheet 的每一台裝置，沒有「只給特定人／部門」的機制。新增 Tasks 分頁的兩個欄位解決「只有業務才需要收到數據回報要求，非業務不需要被打擾」，以及「發給整個部門，但排除其中個別人員」這類需求：

```text
TargetDeviceOrName（選填，決定要發給誰）：
  欄位值格式：以分號（;）分隔的鍵值清單，每個鍵值可以是「裝置代碼」「使用者名稱」或「部門」三種之一，
    例如 業務部  或  EMP-003;王小明  或  業務部;EMP-020（部門與個別鍵值可混用）
  留空：維持原本行為，全公司所有裝置都會收到（預設值，向下相容既有任務不受影響）
  部門鍵值的展開方式：程式讀取 Sheet A／Employees 分頁（見 19.1 節），
    找出 Department 欄等於該部門名稱的所有員工，展開成這些員工各自的 Id/Name，
    再依下方比對規則逐一比對，等同把整個部門的人都當成個別列在清單裡

ExcludeDeviceOrName（選填，決定要從上面的受眾中排除誰）：
  欄位值格式同 TargetDeviceOrName，同樣支援裝置代碼／使用者名稱／部門三種鍵值，可混用
  用途：發給整個部門，但排除其中個別人員，例如業務部裡有位實習生不需要收到業務數據回報要求
  僅在 TargetDeviceOrName 有值時才有意義；若 TargetDeviceOrName 留空（代表全公司），
    ExcludeDeviceOrName 一樣可用來排除特定人，例如「全公司公告，但排除請假中的某人」
  留空：不排除任何人（預設值）

比對規則：
  1. 先依 TargetDeviceOrName 決定候選受眾範圍（留空＝全公司皆為候選）
  2. 候選範圍中的部門鍵值先展開成個別員工（查 Sheet A／Employees）
  3. 再依 ExcludeDeviceOrName 從候選範圍中剔除對應的裝置代碼／使用者名稱／部門（同樣先展開部門）
  4. 每台裝置各自以「本機使用者自訂名稱」優先比對，找不到則以「裝置代碼」比對，
     命中最終受眾清單才觸發，皆未命中則此裝置不觸發、不顯示、不寫入 AcknowledgementLogs
     （不是「收到後被過濾掉」，而是從一開始就不會進入這台裝置的觸發排程，
     對非目標裝置而言等同這筆任務不存在）

範例（見 Sheet範例/SheetA_Tasks.csv、SheetB_Tasks.csv）：
  TargetDeviceOrName=業務部, ExcludeDeviceOrName=王小強
    → 展開業務部所有人（王小明、李國華、王小強）後排除王小強，最終只有王小明、李國華收到
  TargetDeviceOrName=王小明;EMP-010（空 Exclude）
    → 只有王小明、EMP-010（李國華）這兩個對象收到，與部門無關
```

與 19.1 節「通知上限例外」的差異——兩者概念容易混淆，務必分清楚：

```text
19.1 節（MaxAllowedLevel）  決定「收到之後用什麼強度顯示」（降級不影響是否收到）
19.2 節（本節）             決定「收不收得到這筆任務」（範圍過濾，不是強度調整）

兩者可疊加使用：先由 TargetDeviceOrName / ExcludeDeviceOrName 決定哪些人會觸發這筆任務，
  觸發的人再各自依 Sheet A／Employees 的 MaxAllowedLevel 套用自己的通知等級上限
  例如：一筆最高級的業務數據回報任務，TargetDeviceOrName 設為「業務部」
  → 業務部所有人都會收到；
  若其中王小明在 Employees 分頁被設定 MaxAllowedLevel=高級，
  → 王小明收到時會被降級顯示為高級樣式，其他業務部同仁若無個人例外則正常顯示最高級
```

容錯行為：若 Sheet A／Employees 分頁讀取失敗（見 19B 節，例如分頁被誤刪、GID 錯誤），TargetDeviceOrName / ExcludeDeviceOrName 中的**部門鍵值**因無法查表展開而視為空清單（不展開出任何人），但**裝置代碼／使用者名稱**鍵值仍可正常比對（因為不需要查表）；換言之 Employees 分頁故障時，以部門為鍵值的任務會暫時「發不出去給任何人」，以個別人員鍵值設定的任務則不受影響——這是刻意的保守失敗方向（寧可漏發也不要因為查表失敗而誤發給不該收到的人），並會於 SyncLogs 與 Dashboard 顯示警告，提醒 IT 儘速修復該分頁的連結。

對 UI 層完全透明，不需要新增任何畫面或設定介面：過濾發生在同步/資料層——不符合條件的任務根本不會進入這台裝置的本機 SQLite，因此「我的任務」頁（見 UI 規劃書 4 節）看到的清單，本來就已經是「篩選過後屬於這台裝置的任務」，無需在畫面上額外顯示「這筆任務是否有受眾限制」（也不應該顯示，避免讓一般使用者看出「某任務只給誰看」這種過濾邏輯，維持公司政策的例外設定僅由 IT 在 Sheet 上掌握，比照 19.1 節的隱蔽原則）。

---

# 19A. 部署與更新機制

10 台電腦規模，採**手動安裝、自動檢查更新**策略，不架設區域網路服務，避免額外連線設定的複雜度與維運負擔。

## 安裝

```text
Inno Setup 打包單一安裝檔（CloudAlarmOverlaySetup.exe）
IT 人員手動於每台電腦執行安裝

安裝完成、程式首次啟動時，IT 需分配並輸入一組固定裝置代碼（見 19C、19C.1 節），
  建議 IT 自行以內部文件記錄「電腦 ↔ 固定代碼」對照關係，供日後重灌時查詢
  （此對照表為 IT 內部維護，不由程式管理或回寫）
```

## 更新檢查

採用 **Google Drive 固定檔案連結**，不使用 Google Drive API（不需要 API Key / OAuth 憑證），只靠「公開分享連結」即可運作。

```text
Google Drive 建立一個資料夾，例如「CloudAlarmOverlay 更新」
裡面固定放兩個檔案（檔名與檔案 ID 永久不變）：
  version.json                    每次發新版「上傳新版本 / 取代」覆蓋內容
  CloudAlarmOverlaySetup.exe      每次發新版覆蓋同一個安裝檔

取得檔案的共用連結（設為「知道連結的任何人皆可檢視」），
會得到固定的檔案 ID：
  https://drive.google.com/file/d/1AbCdEfGhIjKlMnOp/view
                                    └────檔案ID────┘

轉成直接下載連結：
  https://drive.google.com/uc?export=download&id=1AbCdEfGhIjKlMnOp

關鍵：更新版本時用「上傳新版本取代舊檔」（或刪除重傳同檔名），
檔案 ID 不會改變，程式端寫死的下載連結永遠不用修改。
```

```text
version.json 範例：
{
  "latestVersion": "1.2.0",
  "downloadUrl": "https://drive.google.com/uc?export=download&id=固定的安裝檔ID",
  "releaseNote": "修正同步錯誤"
}
```

程式運作流程：

```text
程式啟動時：
  1. 下載固定連結的 version.json（純文字、檔案小，一般 HTTP GET 即可，無需驗證）
  2. 比對 latestVersion 是否比本機版本新
  3. 有新版 → 跳出提示「有新版本 vX.X.X，請下載更新」，
     附上 downloadUrl（同樣是固定連結）
  4. 使用者 / IT 人員點擊連結，手動下載安裝檔並執行安裝
```

不做「自動下載並靜默安裝」，避免程式在使用者不知情的狀況下自我更新、或需要處理安裝過程中程式互相鎖定檔案的問題。更新仍由人工手動安裝，但版本比對與提醒是自動的，避免 IT 人員要一台台檢查目前版本。

---

# 19B. 集中設定分發（公司共用設定）

新增一個獨立的「公司共用設定檔」機制，與「個人偏好設定」分開儲存，避免修改公司設定時影響到本機或使用者個人化的東西。

## 設計原則

```text
公司共用設定           個人 / 本機設定
────────────           ────────────
Google Sheet（兩份）    Windows 登入後自動啟動（管理員可開關）
同步頻率               啟動後縮小至 Tray
公司政策（見 Sheet A）  靜音時段 / 專注模式
員工清單與通知上限      使用者名稱／裝置代碼（見 19C 節，IT 分配、僅本機保留）
  （Sheet A，見 19.1 節）
國定假日／補班日（Sheet A，見 20.2 節）
農曆對照表（Sheet A，見 20.2b 節）
團隊任務（Sheet B）

註：置頂 / 全螢幕覆蓋比例不列入任一欄，因為兩者不是「設定值」，而是由 18 節通知等級規則直接決定，
  無論公司共用或個人本機都沒有對應的可調整欄位
```

## 分發方式：兩份 Google Sheet（依編輯權限分開）

依「**Google Sheet 網頁端的編輯權限**」分成兩個獨立檔案——不是依讀寫（程式對兩者皆為**單向唯讀**，完全不回寫，不需要 Google Sheets API、不需要 Apps Script），而是依「誰被允許在 Google 端編輯這份試算表內容」分開：

```text
Sheet A：公司核心設定（僅 IT／管理者可編輯）
├── Tasks              公司層級公告 / 排程任務（需審核性質，僅 IT 異動）
├── Holidays           國定假日 / 特殊日期清單（含補班日，見 20.2 節）
├── Employees          員工清單與通知上限例外（見 19.1、19.2 節）
│                       Id / Name / Department / MaxAllowedLevel / RequireAckOverride /
│                       EnglishName / Note 七欄，一張表同時扮演兩個用途：
│                       1. 通知上限例外（原規劃的獨立 LevelOverrides 分頁併入於此，見 19.1 節）
│                       2. Tasks 分頁 TargetDeviceOrName / ExcludeDeviceOrName 欄位
│                          以部門為鍵值時查表展開（見 19.2 節）
└── LunarCalendar      西曆↔農曆日期對照表，供 Tasks 分頁 Recurrence 的農曆規則
                        （LunarDay:n，見 20.2b 節）查表判斷，每年由 IT 更新下一年度資料
    Google 端共用權限：檢視「知道連結的任何人皆可檢視」／編輯僅限 IT／管理者本人的 Google 帳號

Sheet B：團隊任務（開放一般使用者編輯）
└── Tasks              團隊成員可自行新增 / 修改的排程任務
    Google 端共用權限：檢視與編輯皆為「知道連結的人皆可編輯」（或限定公司網域帳號皆可編輯）
    用途：例如某人幫團隊排定一個共同任務，編輯後所有人的程式下次同步都會抓到同一筆資料，
      不需要每個人在自己電腦上各自新增一次
```

```text
程式對兩份 Sheet 的處理方式基本相同：只讀取、合併顯示，不區分來源給使用者看，
  「我的任務」頁面（見 UI 規劃書 4 節）表格上的「來源」欄以 Sheet A / Sheet B 標示（Sheet A 的
  Employees 分頁不會出現在這個來源標示，因為它本身不含任何任務，只是提供人員資料查表用途）
  兩者皆為唯讀來源（不可在程式內編輯，比照原「Sheet 任務」的唯讀規則，見 9 節）

編輯權限差異只影響「誰能在 Google Sheet 網頁上編輯這份試算表」，與程式邏輯無關，
  程式端不需要任何額外程式碼處理權限判斷——這件事完全交給 Google Sheet 原生的共用設定機制
```

每個分頁皆可各自產生公開匯出連結，不需要 Google Sheets API / OAuth 憑證：

```text
https://docs.google.com/spreadsheets/d/{SpreadsheetId}/export?format=csv&gid={分頁GID}
取得方式：開啟該分頁 → 網址列的 gid=xxxx 即為該分頁 ID，與 SpreadsheetId 一起設定於程式
```

IT／管理者於「同步來源」設定頁（見 16 節）分別填入 **Sheet A** 與 **Sheet B** 的 SpreadsheetId，程式依固定的分頁名稱 / GID 分別拉取內容並合併顯示。兩份 Sheet 讀取互相獨立——其中一份失敗（例如 Sheet B 被誤刪）不影響另一份繼續正常運作。Sheet A 底下的分頁（Tasks／Holidays／Employees）雖同屬一份試算表，仍各自獨立讀取——若 Employees 分頁讀取失敗（例如分頁被誤刪、GID 錯誤），將同時影響 19.1 節的通知等級上限例外（全數退回全體預設上限）與 19.2 節以「部門」為鍵值的任務過濾規則（退回「僅依裝置代碼/使用者名稱逐一比對」，見 19.2 節容錯行為），但不影響 Tasks／Holidays 分頁本身的讀取與任務觸發顯示。

> 📎 IT 建立正式 Sheet 時可直接匯入 `Sheet範例/` 資料夾內的 4 份範例 CSV（`SheetA_Tasks.csv`、`SheetA_Holidays.csv`、`SheetA_Employees.csv`、`SheetB_Tasks.csv`）作為分頁初始內容與欄位格式參考，詳見該資料夾內 `README.md` 的建立步驟與共用權限設定說明。

## 快取與覆寫規則

```text
公司共用設定（Sheet A 的 Tasks/Holidays/Employees + Sheet B 的 Tasks）：
  程式啟動 + 定期同步時一併拉取
  本機保留一份快取（SQLite，Locked = true）
  比對雲端版本 → 有變更才覆寫「公司共用」區塊

個人設定（Locked = false）：完全不受影響，永遠只存在本機
```

如此一來，IT 只要改 Sheet A（核心設定與員工清單），或任何團隊成員改 Sheet B（團隊任務），10 台電腦下次同步時就會自動套用最新內容，不需要逐台手動修改；同時保證不會意外覆蓋員工自己的個人化設定。

---

# 19C. 使用者身分識別（免帳密，裝置 + 自訂名稱）

本系統定位為**全開放的內部小工具**，不做帳號密碼登入、不做 Windows 帳戶驗證（管理者身分除外，見 15 節）。但為了讓 Sheet A／Employees 分頁的通知上限例外設定（例如「這台電腦的通知上限只到高級」，見 19.1 節）與簽收紀錄仍可對照到人，採用「裝置代碼 + 使用者自訂名稱」的輕量識別方式。

## 裝置代碼由 IT 手動分配（不自動產生）

裝置代碼**不由程式自動產生**（不用 GUID、不用 Windows 電腦名稱、不綁硬體資訊——理由見 19C.1 節），而是**由 IT 在安裝時手動分配一組固定代碼**，並於首次啟動時手動輸入。好處是同一台電腦無論重灌幾次、換了幾次系統，只要 IT 每次都輸入同一組代碼，裝置代碼永遠固定不變，Sheet B 上不會出現「同一個人對應好幾組代碼」的混亂情況。

```text
IT 安裝流程（見 19A 節）新增一步：
  1. IT 依公司內部編號規則，決定這台電腦的固定代碼（例如 EMP-001、IT 可自訂編碼規則）
  2. 安裝完成、程式首次啟動時，跳出設定視窗（見下）
  3. IT（或使用者依 IT 提供的紙條 /通知）手動輸入這組固定代碼

IT 建議自行維護一份「電腦 ↔ 固定代碼」對照表（例如另存一份內部文件或 Sheet 分頁），
  重灌時查表取得原本的代碼，重新輸入即可延續身分，不需依賴程式自動記憶
```

## 首次使用設定流程

```text
程式第一次啟動時，跳出一次性設定視窗：

┌───────────────────────────────┐
│  歡迎使用 Cloud Alarm Overlay   │
├───────────────────────────────┤
│  裝置代碼（由 IT 提供，安裝時    │
│   分配的固定代碼）               │
│  [________________________]   │
│                                 │
│  使用者名稱（由 IT 提供，安裝時  │
│   一併分配，用於通知簽收紀錄）    │
│  [________________________]   │
│                                 │
│           [ 開始使用 ]          │
└───────────────────────────────┘
```

```text
裝置代碼：必填，由 IT 提供，格式不限（建議 IT 自訂規則，例如 EMP-001、業務-王小明 等好辨識的文字）
使用者名稱：必填，與裝置代碼同樣由 IT 於安裝當下決定並提供，不開放使用者自行輸入或事後修改
  （理由與裝置代碼相同，見 19C.1 節：避免員工留空不填、或亂填暱稱/縮寫導致歷史紀錄與管理者頁
  看到的名字無法對應到真人，喪失簽收紀錄原本要追蹤「誰簽收了」的意義）
兩者存入本機 SQLite（Settings 表），**僅本機保留，不回寫至任何 Google Sheet**（見下方說明）
```

## 純本機保留，不回寫（不需要 GAS / API / 公開可編輯連結）

裝置代碼與自訂名稱**只存在本機 SQLite**，程式完全不會將這兩項資訊上傳或寫入任何 Google Sheet。理由：

```text
避免需要 Google Apps Script（GAS）或 Google Sheets API 等額外技術棧與部署維護成本
避免為了「能回寫」而必須把某份 Sheet 設為公開可編輯，形成資料被任意竄改的風險
對 10 人規模而言，裝置代碼本就由 IT 手動分配（見上方），IT 心裡或紙本記錄即可掌握對照關係，
  不需要程式自動彙整成一份雲端清單
```

若 IT 需要一份「電腦 ↔ 代碼 ↔ 使用者」的對照表，建議自行維護一份 Excel 或紙本記錄（安裝當下順手登記），非本程式的職責範圍。

## 之後修改

```text
設定頁（14 節「一般」區塊）的「使用者名稱」與「裝置代碼」皆為唯讀顯示欄位，
  兩者設定後皆不可由一般使用者自行修改（避免與 IT 的對照表脫節、避免員工自行亂改名稱），
  如需變更須聯繫 IT，於「我的身分」區塊統一提供 [ 聯繫 IT 修改身分資訊 ] 提示文字，
  實際修改仍是清空本機設定後，重新跑一次首次使用設定流程（由 IT 或依 IT 提供的資訊重新輸入）
```

---

## 19C.1 為何裝置代碼改由 IT 手動分配，不自動產生

評估過三種識別來源：

```text
Windows 電腦名稱（MachineName）
  問題：IT 報修換機、或用映像檔佈署多台電腦時可能重複或被手動更動，
        改名即等同變成新裝置，且公司既有電腦命名規則不受本系統控制

硬體資訊指紋（CPU ID / 主機板序號 / 硬碟序號 Hash）
  問題：對 10 人內部小工具而言複雜度過高，需要額外的 WMI 查詢與權限，
        虛擬機環境下硬體資訊可能不穩定或多台 VM 共用同一組特徵碼，
        且換主機板 / 换電腦一樣會導致 ID 改變，並未真正解決「重灌後代碼改變」的問題

程式自產生 GUID
  問題：唯一性有保證、實作最簡單，但 GUID 存於本機 SQLite，
        重灌系統或移除重裝程式會導致 GUID 遺失，下次啟動產生一組全新的 GUID，
        即使使用者名稱相同，IT 也難以判斷新舊代碼是否為同一台電腦（尤其在純本機保留、無雲端彙整的情況下更難追蹤）

IT 手動分配固定代碼（採用）
  優點：代碼完全由人為決定且固定不變，不受重灌、換系統版本、硬體更換影響；
        只要 IT 每次重灌都輸入同一組代碼，該裝置的識別永遠一致，Sheet A／Employees 的通知上限例外設定不會失效
  代價：需要 IT 在安裝流程中多一個步驟（決定並輸入代碼），
        以及 IT 自行維護一份「電腦 ↔ 固定代碼」對照表以便重灌時查詢——
        對 10 台電腦規模而言此成本很低，換得的是識別穩定、不需事後人工比對是否為同一台裝置
```

## 19C.2 本機備份 / 還原（.calbak 打包檔）

即使裝置代碼由 IT 分配，仍提供一個**完整的本機備份/還原**功能，用於重灌、換電腦、或想快速複製整台電腦狀態到新機器的情況。範圍比單純的身分識別更廣，涵蓋裝置身分、個人偏好、本機任務與簽收紀錄，並可選擇性包含管理者密碼，讓新電腦不需要重新設定就能延續原本的一切。此功能純粹是本機檔案的封裝與還原，與 Google Sheet 無關（不讀取也不寫入任何 Sheet）。

### 打包內容與格式

```text
備份檔為一個 Zip 壓縮檔，副檔名 .calbak（Cloud Alarm Overlay Backup 的縮寫，避免與一般 .zip 混淆）：

backup_{裝置代碼}_{yyyyMMdd}.calbak
├── identity.json      裝置代碼、使用者自訂名稱（見 19C 節）
├── settings.json       個人偏好設定（通知樣式、靜音時段等，見 14 節，不含公司鎖定政策；登入自動啟動由管理員專區及 Windows 啟動項目管理）
├── tasks.csv          本機任務清單（Source=本機 的 Tasks 表資料）
├── acklog.csv         簽收紀錄（AcknowledgementLogs 表，見 21 節）
└── admin.key          管理者密碼雜湊 + Salt（選用，見下方說明）
```

```text
選用 admin.key 的原因：
  預設不包含在備份中——多數情況下使用者只是重灌自己的電腦，管理者密碼理應維持不變，
    不需要每次備份都連密碼一起搬動
  勾選「包含管理者密碼」時才產生 admin.key，用於「整台電腦搬到新機器」或「多台電腦要套用同一組管理者密碼」的情境，
    還原時新電腦會直接套用備份內的雜湊值，不需要重新輸入設定一次管理者密碼
```

### 匯出（備份前，使用者主動操作）

```text
設定頁「我的身分」區塊 → [ 建立本機備份 ] 按鈕
  → 彈出選項：☐ 包含管理者密碼（預設不勾選，需輸入目前管理者密碼驗證後才能勾選）
  → 產生 .calbak 檔案，另存新檔對話框讓使用者選擇存放位置
  （建議存到隨身碟 / 雲端硬碟等重灌後還找得到的地方）
```

### 匯入（新電腦或重灌後，首次啟動時）

```text
首次使用設定視窗（見 UI 規劃書 6.1 節）提供選項：
  [ 輸入裝置代碼（由 IT 提供）]   或   [ 匯入備份檔（.calbak）]

選擇「匯入備份檔」→ 選取 .calbak 檔案 → 解壓縮並依序還原：
  1. identity.json  → 還原裝置代碼與使用者名稱
  2. settings.json  → 還原個人偏好設定
  3. tasks.csv      → 匯入本機任務（見下方衝突處理）
  4. acklog.csv     → 匯入歷史簽收紀錄（僅新增，不覆蓋本機既有紀錄）
  5. admin.key（若存在）→ 還原管理者密碼雜湊，新電腦可直接用原密碼登入管理者
```

### 衝突處理

```text
identity.json：直接覆蓋本機目前設定（還原流程本就發生在「本機從未設定過裝置代碼」的情況，無衝突可言）

settings.json：直接覆蓋（同上，是全新安裝情境）

tasks.csv：
  若目標電腦已有本機任務（例如使用者匯入前手動測試新增過幾筆），採「合併」而非覆蓋：
  以任務 Id 比對，Id 相同則跳過（保留本機既有版本），Id 不存在才新增
  匯入結果顯示摘要：「已匯入 12 筆任務，略過 2 筆重複」

acklog.csv：僅新增歷史紀錄，不做任何覆蓋或刪除（歷史紀錄本質是唯一時間序列，沒有覆蓋的必要）

admin.key：
  若目標電腦已設定過管理者密碼，匯入前二次確認「這台電腦已有管理者密碼，匯入備份將覆蓋為原密碼，確定嗎？」
  若目標電腦尚未設定過，直接套用，不需確認
```

### 與 19C.1 節的關係

```text
沒有備份、但知道 IT 分配的裝置代碼：直接於首次使用設定視窗選「輸入裝置代碼」重新輸入同一組即可，
  完全等同重灌前的狀態，Sheet A／Employees 的通知上限例外也不受影響——這仍是本機制設計的主要情境（見 19C.1 節），
  匯入 .calbak 是「連裝置代碼都不記得，或想順便帶走本機任務/密碼」時的進階備援

萬一裝置代碼也忘記、又沒有備份檔：
  只能聯繫 IT 查詢「電腦 ↔ 固定代碼」對照表（見 19C 節）取得原代碼後手動輸入，
  此時本機任務與簽收紀錄無法復原（因為從未備份過），視為全新裝置重新開始
```

---

# 20. Google Sheet 同步

管理者於「同步來源」設定頁（見 16 節）分別填入 **Sheet A** 與 **Sheet B** 的識別資訊（見 19B 節「兩份 Google Sheet」架構，兩者程式皆為唯讀）：

```text
Sheet A SpreadsheetId（僅 IT 可編輯，公司核心設定）
（從 Sheet A 網址列取得，例如 1AbCdEfGhIjKlMnOpQrStUvWxYz）
各分頁 GID（Tasks / Holidays / Employees，各自對應一個 gid 數字）

Sheet B SpreadsheetId（開放一般使用者編輯，團隊任務）
（從 Sheet B 網址列取得）
Tasks 分頁 GID
```

提供：

```text
測試連線（Sheet A、Sheet B 各自獨立測試）
立即同步
鎖定 SpreadsheetId
同步頻率
```

---

## 建議 Tasks 分頁格式（Sheet A、Sheet B 共用相同格式）

| Id  | Time             | Title | Level | Enabled | TargetDeviceOrName |
| --- | ---------------- | ----- | ----- | ------- | ------------------- |
| 001 | 2026-09-14 18:00 | 團隊會議  | 中級    | TRUE    | （留空＝全公司）       |
| 002 | 2026-09-14 19:00 | 關閉設備  | 最高級   | TRUE    | （留空＝全公司）       |
| 003 | 2026-09-20 17:00 | 業務數據回報提醒 | 最高級 | TRUE  | EMP-003;王小明        |

Sheet A 與 Sheet B 的 Tasks 分頁格式相同，程式讀取後合併顯示於同一份任務清單，僅在「來源」欄位標示為 Sheet A 或 Sheet B（見 UI 規劃書 4 節），兩者皆為唯讀，不可在程式內編輯（見 9 節）。

`TargetDeviceOrName` 欄位（選填，留空＝全公司廣播）用於限定只有特定裝置代碼／使用者名稱才會觸發這筆任務，例如上表 003 這筆業務數據回報任務只給 `EMP-003` 與「王小明」兩個對象——完整格式規則與比對邏輯見 **19.2 節「任務受眾過濾」**。

其餘可擴充欄位：

```text
Description
StartTime
ExpireTime
RequireAck
Recurrence      (重複規則，見 21.1)
```

---

## 20.1 欄位解析容錯規則

同步來源為 Google Sheet 公開發布的 CSV/HTML 連結。為避免 IT／使用者在 Sheet 上新增欄位、調整欄位順序時讓同步失效，解析採用「**依標題名稱**」而非「依欄位順序（index）」讀取。

```text
解析邏輯：
1. 讀取第一列（標題列），建立欄位名稱 → 欄位索引的對照表
2. 逐列資料時，依名稱取值，而非依固定欄位順序
3. 新增欄位：不影響既有解析，忽略未知欄位即可
4. 調整欄位順序：不影響解析結果
5. 刪除必要欄位（如 Id / Time / Title）：
   → 同步失敗，寫入 SyncLogs（Status = Failed）
   → 於 Dashboard / System Tray 跳出警告：「Google Sheet 缺少必要欄位：XXX，同步已暫停」
   → 保留 SQLite 現有資料，不清空
```

必要欄位（不可缺少）：`Id`、`Time`、`Title`、`Enabled`
選填欄位（缺少則採預設值）：`Description`、`Level`（預設「中級」）、`RequireAck`、`Recurrence`、
  `TargetDeviceOrName`（預設空值＝全公司廣播，見 19.2 節）

---

## 20.2 跨日 / 國定假日 / 請假處理

## 跨日觸發

```text
任務的 ScheduledAt 為完整日期時間（非單純時間），本身即支援跨日
Recurrence 規則產生的下一次觸發時間一律重新計算完整日期時間，不需額外處理「跨日」邏輯
```

## 國定假日 / 特殊日期

```text
比照公司共用設定的做法，另外用一份 Google Sheet 維護「國定假日 / 特殊日期清單」
欄位：Date, Type（國定假日 / 全公司停班 / 補班日 / 其他）, Note

程式同步時一併拉取此清單，快取於本機 SQLite（Holidays 表）

任務若設定「國定假日不觸發」（欄位 SkipOnHoliday = TRUE）：
  AlarmWorker 判斷當天是否在 Holidays 清單中，且 Type ≠ 補班日 → 是則跳過本次觸發，記錄「因假日略過」
```

### 補班日（Type = 補班日）

政府調整放假時，有時會將原本的週末訂為補班日（例如連假前後某個週六要正常上班），這與「國定假日」在邏輯上是**相反**的效果，因此獨立一個 Type 值，不能跟其他假日類型混用同一套判斷：

```text
補班日的效果：
  這天本來是週末，Weekly 重複規則（含「每個工作日」快捷選項，見 10.1 節）原本不會在這天觸發，
  但因為是補班日，實際上需要「恢復觸發」，如同這天是正常工作日一樣

判斷順序（AlarmWorker 每次判斷觸發時，依序檢查）：
  1. 先依 Recurrence 規則判斷今天本來會不會觸發（例如 Weekly:1,2,3,4,5 在週六本來不會觸發）
  2. 若本來不會觸發，但今天在 Holidays 清單中且 Type = 補班日 → 視為工作日，照常觸發
     （即使該任務沒有勾選 SkipOnHoliday，補班日邏輯仍然生效，因為這是「讓週末變工作日」而非「假日略過」）
  3. 若本來會觸發，但今天在 Holidays 清單中且 Type ∈ {國定假日／全公司停班／其他} 且任務
     SkipOnHoliday=TRUE → 略過本次觸發
  4. 其餘情況維持 Recurrence 規則原本的判斷結果

2026 年台灣目前公告的政府行政假期中沒有需要補班的連假（見 Sheet範例/SheetA_Holidays.csv），
  故此機制目前用不到實際資料，但架構先設計好，避免未來年度真的出現補班日時要重新設計判斷邏輯
```

### Holidays 資料需視為會動態異動，不是排一次就整年不管

政府（行政院人事行政總處）公布的年度行政機關辦公日曆表，過去曾發生公告後又因社會意見調整的情況（例如連假天數增減、補班日取消或新增），尤其容易發生在農曆春節前後的長假安排上。這代表：

```text
Holidays 表不能被當成「年初排一次、整年套用」的靜態資料，
  IT 需要在假期公告異動時，直接修改 Google Sheet 上 Holidays 分頁對應的列（新增/刪除/改 Type），
  程式下次同步即自動套用最新版本，不需要改程式或改任何任務本身——
  這也是為什麼假日資訊放在 Google Sheet 而非寫死在程式碼裡的原因之一（見 19B 節分發架構）

若原本排定的補班日後來被官方取消，IT 需記得同步移除該筆 Type=補班日 的列，
  否則系統會誤判當天需要恢復觸發平日任務，導致週末不該跳出的通知意外跳出
```

## 20.2b 農曆排程（LunarCalendar）

部分任務（例如祭祀、財務結算、店面盤點）習慣依農曆日期排程，而非固定的西曆星期/日期，例如「農曆初一、十五前一天與當天」或「農曆初二、十六」。原本的 Recurrence 只支援西曆規則（不重複／每天／每週幾天／每月固定日），無法表達這種需求，因此新增一張獨立的農曆對照表，供 Recurrence 新增的農曆規則查表判斷：

```text
Sheet A 新增分頁：LunarCalendar（僅 IT／管理者可編輯，見 19B 節）
欄位：Date（西曆日期，YYYY-MM-DD）、LunarDate（農曆日期文字，如「正月初一」）、
      LunarDay（農曆當月第幾天的數字，1-30，供程式比對用）、SolarTerm（節氣，選填，多數日期為空）

用途：純粹是「西曆日期 → 農曆日期」的查表資料，供 Recurrence 的農曆規則使用，
  本身不代表任何假日或排程意義，與 Holidays 表的職責完全分開
  （Holidays 表回答「這天該不該略過觸發」，LunarCalendar 表回答「這天農曆是幾號」）

程式同步時一併拉取，快取於本機 SQLite（LunarCalendar 表），每年由 IT 更新下一年度的對照資料
  （農曆與西曆的對應每年不同，需要逐年更新，不像國定假日規則相對固定）
```

### Recurrence 新增農曆規則：LunarDay

```text
格式：LunarDay:n1,n2,...（n 為農曆日期 1-30，可複選多個），例如：
  LunarDay:1          農曆初一當天
  LunarDay:1,15       農曆初一、十五當天（不含前一天）
  LunarDay:2,16       農曆初二、十六當天

若需要「前一天 + 當天」的組合（例如「初一十五前一天跟當天」），不新增額外語法，
  而是直接把「前一天」也算成獨立的農曆日期值一併列出，例如：
    農曆十五前一天 = 農曆十四 → LunarDay:14,15,29,30（十五前一天是十四，
      月底的月份無三十則會是農曆廿九，兩者都要列出以涵蓋大月小月的差異，見下方判斷邏輯）
  這樣設計的理由：不需要為「前一天」發明新的日期運算語法，
    只需要在建立任務時，把所有想要觸發的農曆日期都列進同一個清單即可，語意最直接、最不容易算錯

判斷邏輯：
  AlarmWorker 每次判斷觸發時，查詢 LunarCalendar 表找出「今天」對應的 LunarDay 數字，
  比對是否存在於任務的 LunarDay:n1,n2,... 清單中，存在則觸發

「十五前一天」需涵蓋大月小月差異的具體處理：
  農曆有大月（30 天）與小月（29 天）之分，「十五前一天」永遠是農曆十四，這點沒有問題
  但「初一前一天」（即上個月最後一天）不固定是三十或廿九，取決於上個月是大月還是小月
  故若任務需求是「初一前一天與當天」，Recurrence 應設為 LunarDay:29,30,1，
    同時涵蓋大月小月兩種可能，程式查表判斷時只要「今天」的 LunarDay 命中清單中任一值就觸發，
    不需要程式額外判斷本月是大月還是小月——由 IT 建立任務時把兩種可能都列進去即可
```

容錯行為：若 LunarCalendar 查無「今天」對應的資料（例如 IT 忘記更新下一年度對照表），該筆農曆任務當天不會觸發，並於 SyncLogs／Dashboard 顯示警告，提醒 IT 更新 LunarCalendar 分頁，行為與 19.2 節 Sheet A／Employees 查表失敗時「寧可漏發也不誤發」的保守原則一致。

## 請假 / 未到座位

```text
不串接請假系統，維持單純：
- 使用者沒開機 / 沒開程式 → 任務時間到也不會有人看到，視為「未上班」，無需特別處理
- 使用者有開機但不在座位上 → 通知照常顯示，掛著等待，不自動關閉、不自動略過
```

## 逾時 / 未開機的記錄

```text
AcknowledgementLogs.Result 增加狀態：
  Acknowledged           準時簽收（於逾期寬限時間內完成簽收，見 20.2a 節）
  Overdue_Acknowledged   逾期簽收（超過寬限時間才簽收——錯過了，但後來補上了確認動作）
  Overdue_Unacked        逾期未簽收（超過寬限時間，且自始至終都沒有簽收動作）
  NotLaunched            程式未啟動 / 電腦未開機（由下次啟動時回填判斷）

程式啟動時的補登邏輯：
  檢查是否有 ScheduledAt < 上次關閉時間 且 IsTriggered = false 的任務
  → 標記為 NotLaunched，寫入紀錄
  → 不補跳全螢幕通知（喚醒/啟動不倒帶過去，見 26.1）
```

### 20.2a 逾期寬限（Overdue Grace Period）—— 程式持續運行時的定案機制

`NotLaunched` 靠「下次啟動」回填、`Overdue_Unacked`（睡眠喚醒版）靠「喚醒事件」回填，這兩個都是**被動**觸發。但若一台電腦長時間開機、程式也從未關閉或睡眠（例如 24 小時常駐的座位機），逾期的通知會一直停留在「等待確認中」，永遠不會被這兩個時機捕捉到、也就永遠不會寫入 AcknowledgementLogs 的最終結果——歷史紀錄頁因此會有筆記錄卡在「查無結果」的中間態，且 UI 上「狀態於程式下次啟動時更新」的提示在這種情境下並不成立。

```text
新增一個「逾期寬限」定時檢查（與 AlarmWorker 共用同一個背景排程，不另開執行緒）：
  每次排程執行（見主規劃書 AlarmWorker 相關章節）時，額外檢查一次：
    是否有 IsTriggered = true 且 AcknowledgedAt = null 的通知，
    其 TriggeredAt 距今已超過「逾期寬限時間」
  → 超過寬限時間者，先暫定為 Overdue_Unacked，寫入 AcknowledgementLogs，
    畫面上的通知視窗維持原樣不受影響（不因為背景判定結果而自動關閉通知，使用者晚點回來仍可點擊確認）

錯過寬限時間之後，該筆記錄的最終結果只會落在兩種之一，取決於使用者有沒有回來點確認：
  使用者之後點擊「確認」  → 改寫為 Overdue_Acknowledged（逾期簽收：錯過了，但後來補上了）
  使用者自始至終未點擊    → 維持 Overdue_Unacked（逾期未簽收：連事後補簽收都沒有）
  即：Overdue_Unacked 是「目前為止都沒人確認」的暫定/終局共用狀態，一旦事後補簽收即改寫為
    Overdue_Acknowledged；若程式關閉或電腦關機前都沒人確認，Overdue_Unacked 就是最終結果
    （不會再有機會被改寫，因為通知本身已隨程式關閉而消失，見主規劃書相關通知生命週期章節）

逾期寬限時間：
  四個等級統一預設 15 分鐘，明確定調為「錯過」的判定基準：
    通知觸發後 15 分鐘內沒有任何簽收動作，就視為使用者當下沒看到／沒處理，
    不是「多給一點緩衝時間」的溫和寬限，而是「超過這個時間就認定為錯過」的明確界線
    （之所以選 15 分鐘而非以小時計，是因為使用者本來就可能暫時離座，
    但超過 15 分鐘代表當下真的沒在處理這件事，該筆通知就該有一個結果，不宜無限期懸而未決）
    低級本身為免確認、10 秒自動關閉，實務上幾乎不會進入這個判斷（IsTriggered=true 且
      AcknowledgedAt=null 的狀態存在時間極短），此寬限值對低級影響甚小
  由 IT 於 Sheet A 統一設定（獨立欄位或設定分頁），非使用者可調整項目，
    避免使用者自行拉長寬限時間來規避簽收；若日後需要依等級差異化，
    可在同一欄位機制下個別覆寫，不影響本節其餘邏輯

與「未開機」判斷的邊界：
  逾期寬限只處理「程式持續運行中」的逾期，不取代 NotLaunched 判斷
  若電腦真的關機/程式真的沒開，仍照原本的「下次啟動補登」邏輯標記為 NotLaunched
  兩者不會重複觸發同一筆記錄：寬限檢查只掃描 IsTriggered=true（代表當時程式有開、通知已經跳出）的項目
```

---

## 20.3 簽收紀錄集中化（第一版不做，列為未來開發目標）

第一版／第二版**不做**集中化資料庫、不做 Web 後台、也不將簽收紀錄回寫至任何 Google Sheet 或送到內網主機（見 21 節 AcknowledgementLogs 表結構）。理由：無論是回寫 Google Sheet（需要 GAS/Sheets API，也容易因「需要可寫入」而必須開放公開編輯權限，產生資料被竄改的風險）或是內網集中（需要 IT 先架好一台常駐主機、確認全公司電腦網路環境），都還沒有定案的實作方式——但這不是永久放棄，而是列為明確的**未來開發目標**（見 38 節第三階段），架構上先留接口，避免將來要做時得整個重新設計。

```text
第一版／第二版現況：
  AcknowledgementLogs 僅存在本機 SQLite，供使用者自己或 IT 到場時查閱、排查問題使用
  不提供任何形式的送達率 / 簽收率彙整報表（無論程式內或 Sheet 上）

未來開發目標（見 38 節）：簽收紀錄回寫至某個集中位置，讓 IT / 主管不需要到場就能看到送達率、簽收率統計
  尚未解決的關鍵問題：如何在不開放 Sheet 公開可編輯、不引入複雜後端的前提下，安全地把本機資料集中起來
    候選方向（依可行性排序，留待日後評估時決定實際採用哪個）：

      方向 A：內網直連至公司現有的 Linux 主機（目前最可行的方向）
        公司已有一台由 IT 管理的 Linux PC，可直接作為集中收件的目的地，
          不需要額外採購或架設新設備，這是三個方向中唯一已經有明確落地目標主機的選項
        由每台員工電腦**主動**定期把新增的 AcknowledgementLogs 回報給這台內網 Linux 主機，
          可以是簡單的內部 HTTP API（例如用輕量框架起一個接收端點），或甚至只是寫入一個
          共用網路資料夾／SFTP，由這台主機定期彙整，完全不經過網際網路、不需要 Google Apps Script
        方向刻意設計成「員工電腦推給主機」而非「主機去查員工電腦」：
          員工電腦不需要開放任何連入 port、不需要固定 IP（DHCP 動態配發也沒關係，
          因為連線永遠是員工電腦主動發起），Linux 主機只要能被動接收即可，防火牆與資安疑慮最小，
          且由 IT 直接管理這台主機，權限與存取控制都在公司內部，不涉及任何第三方帳號
        限制：僅在員工電腦於公司內網（或已連上公司 VPN）時才能回報；筆電帶出公司、
          或透過行動熱點上網時，該時段的紀錄會累積在本機，等下次回到內網時再一併補送
          （不會遺失，只是延後同步，這點與方向 B 的行為一致）
        優點：資料完全不出公司內網，沒有 Google 帳號權限、Sheet 竄改、外部 API 金鑰外洩等風險，
          是三個方向中隱私與安全性最高、且現在就有現成主機可用的選項

      方向 B：Google Apps Script Web App（前提：公司電腦連得到外部網際網路）
        以 doPost 接收資料，搭配簡單的裝置代碼/密鑰驗證，不直接開放 Sheet 編輯權限
        僅在方向 A 的內網主機因故不可行時（例如該 Linux 主機另有他用、或需要員工在外時也能即時回報）才考慮

      方向 C：直接等到第三階段的 REST API / 中央管理 Server 到位後，由該後端接收並落地
        （這台 Linux 主機屆時也可能就是這個中央管理 Server 的載體，方向 A 與方向 C 未必互斥，
        很可能是同一台主機從「簡單接收端點」逐步演進為「正式後端服務」）

    三個方向的共通點：都是「本機先存 SQLite，之後才有一個獨立的背景同步流程把資料送出去」，
      不影響第一版／第二版現有的本機寫入邏輯，差別只在同步流程的目的地與傳輸方式，
      這也是下方要先在表結構預留 SyncedAt/SyncStatus 欄位的原因——不論日後選哪個方向，
      這兩個欄位的語意都通用（「這筆有沒有送出去、送到哪個目的地」不需要因應方向而重新設計）

架構上的接口預留（現在就做，不等到真正開發回寫功能才加）：
  AcknowledgementLogs 表新增 SyncedAt、SyncStatus 兩個欄位（見 21 節），第一版／第二版永遠是預設值
    （SyncedAt = null、SyncStatus = NotApplicable），不做任何同步行為，欄位存在但沒有對應的程式邏輯
  好處：未來真的要做回寫時，不需要對既有資料做 schema migration 補欄位，
    只需要新增一個背景同步服務，掃描 SyncStatus 仍為待同步的列並嘗試回寫，不影響既有的本機讀寫邏輯
    （方向 A／B／C 都是同一套「掃描待同步列 → 送出 → 更新 SyncedAt/SyncStatus」的流程，
    只是送出的目的地與傳輸方式不同，所以先留這兩個欄位對三個方向都適用，不會選錯方向而白做）
  這兩個欄位在第一版／第二版的歷史紀錄頁、CSV 匯出（見 21.1 節）中不顯示、不使用，純粹是資料庫層的預留
```

---

# 21. SQLite 資料表規劃

## Tasks

```text
Id
ExternalId
Title
Description
ScheduledAt
Source                  本機 / SheetA / SheetB（見 19B 節兩份 Google Sheet 架構）
Level                   低級 / 中級 / 高級 / 最高級
Enabled
IsTriggered
RequireAcknowledgement
TargetDeviceOrName      受眾過濾清單（裝置代碼/使用者名稱/部門皆可），格式與比對規則見 19.2 節；
                          空值＝全公司廣播（預設，向下相容）
ExcludeDeviceOrName     從 TargetDeviceOrName 決定的受眾中排除特定人／部門，格式同上，見 19.2 節；
                          空值＝不排除任何人（預設）
Recurrence              None / Daily / Weekly:1,3,5 / Monthly:15 / LunarDay:1,15 等（見 21.1、20.2b）
SkipOnHoliday           是否於國定假日略過觸發
CreatedAt
UpdatedAt
```

---

## Holidays

```text
Id
Date
Type            國定假日 / 全公司停班 / 補班日 / 其他（補班日效果與其餘三種相反，見 20.2 節）
Note
Source          Sheet / 本機
```

用途：跨日 / 國定假日略過觸發、補班日恢復觸發判斷（見 20.2）。由公司共用的 Google Sheet 同步而來，本機不可編輯。

---

## LunarCalendar

```text
Id
Date            西曆日期
LunarDate       農曆日期文字，例如「正月初一」
LunarDay        農曆當月第幾天，1-30（供 Recurrence 的 LunarDay:n 規則查表比對用）
SolarTerm       節氣，選填，多數日期為空
```

用途：Tasks 分頁 Recurrence 農曆規則（LunarDay:n，見 20.2b 節）查表判斷「今天農曆是幾號」。純查表資料，不代表任何假日或排程意義，與 Holidays 表職責完全分開。由公司共用的 Google Sheet（Sheet A／LunarCalendar 分頁）同步而來，本機不可編輯，每年由 IT 更新下一年度資料。

---

## AcknowledgementLogs

```text
Id
TaskId
DeviceId          裝置代碼（見 19C 節，取代原本的 UserId）
DisplayName       使用者自訂名稱快照（記錄當下的名稱，即使日後改名也不影響歷史紀錄）
TriggeredAt
AcknowledgedAt
DurationSeconds
Result          Acknowledged / Overdue_Acknowledged / Overdue_Unacked / NotLaunched（見 20.2、20.2a 節）
SyncedAt        【未來擴充預留欄位】回寫雲端成功的時間戳記；第一版／第二版永遠為 null（見 20.3 節）
SyncStatus      【未來擴充預留欄位】NotApplicable / Pending / Synced / Failed；
                  第一版／第二版永遠為 NotApplicable，無對應程式邏輯（見 20.3 節）
```

> 註：此表**目前**僅作本機保留，供事後排查問題使用，不回寫、不做跨裝置集中統計。系統不做帳密登入（見 19C 節），故以裝置代碼 + 自訂名稱識別，而非傳統使用者帳號 UserId。`SyncedAt`／`SyncStatus` 為未來「簽收紀錄回寫」開發目標預留的欄位（見 20.3、38 節），現階段不使用，避免日後真的要做回寫時需要對既有資料做 schema migration。

---

## Devices

```text
Id
DeviceId          裝置代碼（由 IT 手動分配，見 19C、19C.1 節）
DisplayName       使用者名稱（與 DeviceId 同樣由 IT 於安裝時分配，見 19C 節，一般使用者不可自行修改）
LastSeen
Version
```

用途：僅本機保留自己這一台裝置的身分設定，**不回寫至任何 Google Sheet**（見 19C 節）。此表在每台電腦上永遠只有一列（自己），非跨裝置清單。

---

## Users

```text
Id
Username
DisplayName
PasswordHash
Enabled
```

用途：僅供「管理者」單一身分登入使用（見 15、19 節，第一版不再區分管理員/IT 兩種角色）。一般使用者不需帳號，見 19C 節的免帳密身分識別機制。

---

## Employees

取代原本的 Roles 表，也取代原本規劃的獨立 LevelOverrides 表。角色不再決定通知行為，改以「員工」為單位的清單，同時承載通知等級上限與部門查表用途（來自公司共用設定 Sheet A／Employees 分頁，見 19.1、19.2 節）：

```text
Id
DeviceId          裝置代碼，對應本機 Devices.DeviceId 的比對鍵值
Name              使用者名稱，可留空（公用電腦無對應個人時，見 19.1 節）
Department        部門，供 Tasks.TargetDeviceOrName / ExcludeDeviceOrName 以部門為鍵值查表展開（見 19.2 節）
MaxAllowedLevel   低級 / 中級 / 高級 / 最高級 / -（該員工可被套用的最高等級上限，- 代表沿用全體預設）
RequireAckOverride
```

本機此表為 Sheet A／Employees 分頁的唯讀快取，僅 IT 於 Google Sheet 上編輯，程式不提供任何編輯介面（見 19.1 節）。

---

## Settings

```text
Key                 例如：SoundEnabled_Low, SoundEnabled_Mid, SoundEnabled_High, SoundEnabled_Max
                    （四個等級各自獨立的音效開關，預設 false，見 18.1 節）
                    SoundName_Low, SoundName_Mid, SoundName_High, SoundName_Max
                    （各等級選用的 Windows 系統音效名稱）
Value
Locked
```

---

## PomodoroSettings

```text
Key                 例如：FocusMinutes, BreakMinutes, LongBreakMinutes,
                    LongBreakEnabled, LongBreakInterval,
                    SoundEnabled（預設 true，見 13A 節）, SoundName（選用的 Windows 系統音效名稱）
Value
```

用途：番茄鐘個人設定（見 13A 節），純本機保留，不受公司政策鎖定影響。

---

## PomodoroLog

```text
Id
Type            Focus / Break / LongBreak
StartedAt
CompletedAt     若提前結束則為 NULL
Result          Completed / Interrupted
```

用途：番茄鐘使用紀錄（見 13A 節），純本機保留，與 AcknowledgementLogs 分開，不算入任務簽收統計。

---

## SyncLogs

```text
Id
Time
Status
Message
RecordCount
```

---

## AuditLogs

```text
Id
UserId
Action
OldValue
NewValue
CreatedAt
```

---

## 21.1 CSV 匯出規格（三處共用規則）

系統共有三個「匯出 CSV」按鈕，分別對應三張表：**歷史紀錄**（見 11 節，AcknowledgementLogs）、**系統紀錄**（見管理者頁，SyncLogs + 系統事件）、**Audit Log**（見 31 節，AuditLogs）。三者皆為唯讀資料的本機匯出，不涉及網路請求，共用同一套技術規則，僅欄位定義不同。

### 共用規則

```text
編碼：UTF-8 with BOM（確保 Excel 開啟繁體中文不亂碼）
分隔符號：半形逗號，欄位值內含逗號/換行/雙引號時以雙引號包覆並跳脫（標準 CSV 轉義規則）
換行符號：CRLF（Windows 慣例）
第一列：標題列（英文欄位名，方便日後程式讀取比對，見下方各表定義）
時間格式：ISO 8601 本地時間，例如 2026-09-14 18:00:00（不含時區，因僅本機單機使用）
匯出範圍：一律匯出「目前篩選條件」下的完整資料集，不是只匯出目前畫面捲動可見的部分
排序：與畫面顯示排序一致（新到舊）
```

### 檔名規則

```text
{資料類型}_{起始日yyyyMMdd}_{結束日yyyyMMdd}.csv

範例：
  AckLog_20260908_20260914.csv          歷史紀錄
  SyncLog_20260908_20260914.csv         系統紀錄－同步紀錄分頁
  SystemEvent_20260908_20260914.csv     系統紀錄－系統事件分頁
  AuditLog_20260908_20260914.csv        Audit Log

若該次匯出無日期區間限制（例如 Audit Log 選擇「全部」）：
  {資料類型}_全部_{匯出當下時間戳記yyyyMMddHHmmss}.csv
```

### 各表欄位定義

```text
AckLog（歷史紀錄，見 11 節 / AcknowledgementLogs 表）：
  TaskName, ScheduledAt, TriggeredAt, AcknowledgedAt, DurationSeconds, Result

SyncLog（系統紀錄－同步紀錄分頁 / SyncLogs 表）：
  Time, Source, Status, Message, RecordCount
  Source 欄位值：SheetA / SheetB，標示這筆同步紀錄來自哪個來源

SystemEvent（系統紀錄－系統事件分頁）：
  Time, EventType, Message
  EventType 欄位值：AppStart / AppExit / DbError / SyncFailed / AlarmShown /
    AlarmClosed / SettingChanged / AdminLogin

AuditLog（見 31 節 / AuditLogs 表）：
  CreatedAt, UserId, Action, OldValue, NewValue
```

### 匯出流程與失敗處理

```text
點擊 [匯出 CSV]：
  1. 依目前篩選條件（日期區間、分頁籤、勾選的事件類型等）查詢資料
  2. 資料量 > 1000 列時，畫面顯示短暫進度提示（見主規劃書 21 節設計，避免使用者誤以為沒反應）
  3. 查詢完成後產生 CSV 內容，觸發系統「另存新檔」對話框，預設檔名依上方規則
  4. 使用者選擇存放路徑後寫入檔案，完成後 Toast 提示「已匯出 {檔名}」

查詢結果為空時：
  仍允許匯出，產生僅含標題列的 CSV（不因為沒有資料就停用按鈕），
  避免使用者誤以為功能故障

寫入失敗時（例如磁碟空間不足、路徑無寫入權限）：
  Toast 顯示「匯出失敗：{原因}」，不留下部分寫入的損壞檔案（先在記憶體組好內容再一次性寫入）
```

---

# 22. 全螢幕警示

Window 設定：

```text
WindowStyle=None
ResizeMode=NoResize
WindowState=Maximized
Topmost=True
ShowInTaskbar=False
```

---

## 畫面內容

```text
⚠ 注意！任務到期

團隊下班進度同步會議

18:00:00


請輸入以下文字確認已閱讀：

R7K2-419

[______________]

[驗證並關閉提醒]
```

---

# 23. 閱讀確認碼

目的：

> 防止使用者反射性直接關閉警示。

不是安全驗證。

---

## 規格

例如：

```text
R7K2-419
```

產生方式：

```text
ABCDEFGHJKMNPQRSTUVWXYZ
23456789
```

排除：

```text
0
O

1
I
L
```

---

## 關閉行為

以下操作不能直接關閉：

```text
ESC
Space
Alt + F4
X
```

而是：

```text
Focus 到確認碼輸入框
```

只有輸入正確後：

```text
更新 SQLite

IsTriggered = 1

AcknowledgedAt = Now

寫入 AcknowledgementLogs

Close()
```

---

## 23.1 稍後提醒（Snooze）

```text
低級：本身 10 秒即自動關閉，無需 Snooze
中級 / 高級：預設允許 Snooze，可延後 5 / 10 / 15 分鐘
最高級：預設不允許 Snooze，是否開放由 IT 於 Sheet A 上以任務欄位設定（例如允許 1 次、最多延後 5 分鐘）
```

```text
Snooze 行為：
  記錄 SnoozeCount、SnoozedUntil 於任務執行狀態（非修改 Tasks 範本本身）
  時間到後重新觸發同一等級通知
  AcknowledgementLogs 仍以最終「確認關閉」的時間計算 DurationSeconds
```

---

## 23.2 靜音時段 / 專注模式

```text
使用者個人設定（本機設定，不受公司政策鎖定，除非管理者於本機特別鎖定）：
  啟用時段：例如 12:00–13:00（午休）、18:00 後（下班）

生效規則：
  低級 / 中級 / 高級 → 於靜音時段內不彈出，改為排入佇列，時段結束後才顯示
  最高級 → 不受影響，照常跳出（業務時效性任務不可被個人設定影響）
```

---

# 24. 閃爍模式

強制通知可使用：

```text
紅色
#FF0000
```

與：

```text
黑色
#000000
```

交替。

預設：

```text
500ms
```

亦可由管理者於本機設定：

```text
200ms ~ 5000ms
```

---

# 25. System Tray

程式平常常駐系統匣。

右鍵：

```text
開啟主視窗
立即同步
新增任務
測試通知
設定
關於
結束程式
```

從系統匣選擇「結束程式」後，先確認是否退出。管理員專區「本機設定」的「結束程式時要求密碼」開關預設開啟；
關閉時略過密碼驗證，但仍檢查是否有待確認的強制通知。開啟時依選擇的方式驗證：
預設使用管理者帳號與密碼；設定專用結束密碼後，退出時只需輸入專用密碼。
管理員登入後可直接設定或重設專用密碼（不需舊結束密碼），也可切回管理者帳密。
即使管理者已登入，退出仍須重新驗證。取消或驗證失敗時維持提醒與系統匣執行。
此流程只保護應用程式的正常退出，無法防止 Windows 工作管理員強制結束處理程序。

Inno Setup 安裝後，程式登錄在目前使用者的 Windows 登入啟動項目；重開機後登入 Windows 會自動開啟程式，解除安裝時移除啟動項目。
通知音效與番茄鐘的試聽按鈕在送出播放時提供短暫動畫與「已執行試聽」文字，即使 Windows 靜音也可辨識操作已執行。
公開更新連結只有管理員登入時可編輯與儲存；登出後欄位保持可見但呈灰色且不可編輯。

---

# 26. Background Service

程式啟動後至少有兩個背景服務。

## SyncWorker

預設：

```text
每 30~60 秒（原規劃 10 秒過於頻繁，降低頻率以減少 API 負擔與衝突機率）
```

流程：

```text
取得 Google Sheet
↓
解析資料（依標題名稱，見 20.1）
↓
比對 SQLite
↓
新增 / 更新
↓
一併拉取公司共用設定 / 國定假日清單（見 19B、20.2）
↓
寫 Sync Log
```

---

## AlarmWorker

不採用持續輪詢，改為**一次性 Timer 精準觸發**，對 CPU / 筆電電力較友善：

```text
啟動時 / 每次任務異動後：
  從所有 Pending 任務（含 Recurrence 展開後的下一次觸發時間）中
  找出「最早的下一個 ScheduledAt」
  → 設定一個一次性 Timer，於該時間點觸發

Timer 到期時：
  1. 執行該任務（顯示對應等級的通知，見 18 節排隊規則於 27 節）
  2. 重新計算下一個最早任務時間
  3. 重設下一個 Timer
```

---

## 26.1 系統喚醒校正（Resume from Sleep）

筆電睡眠 / 休眠時，Timer 會停止累積或被系統凍結，喚醒後需要重新校正：

```text
監聽 Windows 電源事件（SystemEvents.PowerModeChanged → Resume）

喚醒後：
  1. 取消目前排定的 Timer（時間可能已經失準）
  2. 重新查詢目前時間，重新計算「下一個最早任務時間」
  3. 重設 Timer

原則：不補跳過去已經錯過的任務全螢幕通知
  → 睡眠期間錯過的任務（從未 IsTriggered=true，通知根本沒跳出來過）直接標記 Overdue_Unacked，
    寫入 AcknowledgementLogs，且此類記錄不會再有機會被改寫為 Overdue_Acknowledged
    （與 20.2a 節「已觸發但沒人確認」的情境不同：這裡的通知從未顯示，使用者無從點擊確認）
  → 避免喚醒瞬間被一連串補跳的全螢幕通知轟炸
```

---

# 27. 防止重複觸發 / 多任務排隊

必須處理：

```text
同一任務不能開多個 Overlay
多個不同任務同時到期時，不能同時彈出多個全螢幕視窗互相疊加
```

## 單一任務防止重複

建議：

```text
TriggerState
```

狀態：

```text
Pending
Triggering
Displayed
Acknowledged
```

流程：

```text
Pending

↓

DB Transaction

↓

Triggering

↓

Open Window

↓

Displayed

↓

Acknowledged
```

## 多任務排隊顯示

```text
全域維護一個「待顯示佇列」（依 Level 高到低、再依 ScheduledAt 早到晚排序）

高級 / 最高級（60% 螢幕 / 全螢幕）：同一時間只顯示一個視窗，
  目前視窗完成確認（Close）後，才從佇列取出下一筆並顯示
  視窗上顯示佇列提示：「還有 N 個待確認通知」

低級 / 中級（右下角便利貼）：不受此限制，可多個同時並存顯示，
  因為不需要 Topmost 強制驗證（中級雖 Topmost 但體積小），不會互相遮擋主要工作視窗，
  多筆便利貼由下往上堆疊排列。
```

---

# 28. 多螢幕

第一版至少支援：

```text
主螢幕全螢幕警示
```

第二版可支援：

```text
所有螢幕同時警示
```

設定：

```text
顯示於：

● 主螢幕
○ 所有螢幕
○ 指定螢幕
```

---

# 29. 錯誤處理

Google Sheet 失敗：

```text
保留 SQLite 現有資料
```

不可：

```text
因為網路失敗
把所有任務清空
```

---

## 離線模式

網路中斷：

```text
SQLite 任務照常執行
```

等網路恢復後：

```text
自動重新同步
```

---

# 30. 安全性

管理員密碼不可明碼存 SQLite。

使用：

```text
PBKDF2
```

或：

```text
Argon2id
```

儲存：

```text
PasswordHash
Salt
```

---

# 31. Audit Log

這台電腦的管理者設定修改必須留下紀錄（僅記錄本機異動，不涉及公司層級的 Sheet A／Sheet B 內容變更，那些變更由 Google Sheet 自身的版本歷史記錄）。

例如：

```text
2026-09-14 14:20

admin

Sheet A SpreadsheetId

Old:
1AbC...OldId

New:
1AbC...NewId
```

---

# 32. 權限原則

一般使用者：

```text
可以：
新增自己的任務
刪除自己的任務
查看紀錄
修改個人偏好
```

不能：

```text
修改這台電腦的 Sheet A / Sheet B 連結設定（需管理者登入）

停用同步

刪除 Sheet A / Sheet B 來源的任務（唯讀，需至 Google Sheet 上修改）

修改公司政策內容（需至 Sheet A 上由 IT 編輯）

關閉被鎖定的警示功能
```

---

# 33. 非功能需求

## 穩定性

程式應可：

```text
24 / 7 常駐
```

避免：

```text
Memory Leak
UI Thread Block
Timer 累積
SQLite Locked
```

---

## 效能

一般狀態：

```text
CPU 接近 0%
```

記憶體目標：

```text
< 150 MB
```

視實際 WPF 套件調整。

---

# 34. UI 設計規範

設計方向：

```text
Windows 11
Fluent Design
支援深色 / 淺色 / 跟隨系統 三種主題模式（見 34.1 節）
```

深色主題（Dark）主要顏色：

```text
背景        #0F1722
Card        #182433
Border      #30445A
Primary     #1683FF
Success     #37D67A
Warning     #FFC928
Danger      #FF3B30
```

淺色主題（Light）主要顏色：

```text
背景        #F5F7FA
Card        #FFFFFF
Border      #DDE3EC
Primary     #0F6FDE
Success     #1E9A5A
Warning     #B87700
Danger      #D6362C
文字（主要） #1B2430
文字（次要） #5B6B80
```

```text
淺色主題非深色主題的簡單反白，而是重新校正過對比與飽和度：
  Primary/Success/Warning/Danger 在淺色背景下降低明度、提高飽和度，確保 WCAG AA 對比度
  Warning 從深色主題的亮黃 #FFC928 改為較深的琥珀色 #B87700，避免在白底上刺眼且對比不足
四種通知等級的識別色（低級灰藍／中級 Primary／高級 Warning／最高級 Danger，見 18 節）
  在兩種主題下分別使用對應的深色/淺色版本，視覺語意保持一致
```

## 34.1 主題模式切換

比照 Windows 11 系統設定的慣例，提供三種模式供使用者選擇：

```text
深色       固定使用深色主題，不受系統設定影響
淺色       固定使用淺色主題，不受系統設定影響
跟隨系統    讀取 Windows 目前的「淺色/深色模式」設定，並持續監聽該設定變更
           （使用者在 Windows 設定中切換時，程式主題即時跟著切換，不需重啟程式）
```

```text
設定位置：設定頁「一般」區塊新增「外觀模式」選項（見 UI 規劃書 6 節）
預設值：跟隨系統（多數 Windows 應用程式的預設行為，減少使用者第一次需要額外設定的項目）
儲存：本機 Settings 表（Key=ThemeMode，Value=Dark/Light/System），個人設定，不受公司政策鎖定
```

---

# 35. MVVM 專案結構

```text
CloudAlarmOverlay
│
├── Models
│   ├── AlarmTask.cs
│   ├── Device.cs           本機裝置身分（見 19C 節：DeviceId + DisplayName，純本機不回寫）
│   ├── User.cs             僅供「管理者」單一身分登入使用（見 15、19 節）
│   ├── LevelOverride.cs    Sheet A 唯讀快取
│   └── Policy.cs
│
├── ViewModels
│   ├── MainViewModel.cs
│   ├── DashboardViewModel.cs
│   ├── TasksViewModel.cs
│   ├── HistoryViewModel.cs
│   ├── SettingsViewModel.cs
│   └── AdminViewModel.cs      對應 16 節（單機同步來源設定 / 版本 / 系統事件）
│
├── Views
│   ├── MainWindow.xaml
│   ├── DashboardView.xaml
│   ├── TasksView.xaml
│   ├── HistoryView.xaml
│   ├── SettingsView.xaml
│   ├── AdminView.xaml
│   ├── FirstRunSetupWindow.xaml   首次啟動的裝置身分設定視窗（見 19C 節）
│   └── AlarmOverlayWindow.xaml
│
├── Services
│   ├── AlarmService.cs
│   ├── SyncService.cs           讀取 Sheet A/B/C（Tasks/Holidays/Employees，見 19B 節，皆唯讀）
│   ├── DeviceIdentityService.cs 產生/讀取本機 DeviceId，管理 DisplayName（純本機，不回寫任何 Sheet），
│   │                             匯出/匯入 identity.json（見 19C、19C.2 節）
│   ├── DatabaseService.cs
│   ├── PolicyService.cs
│   ├── AuthenticationService.cs 僅供「管理者」單一身分登入
│   └── AuditService.cs
│
├── Repositories
│   ├── TaskRepository.cs
│   ├── DeviceRepository.cs
│   ├── UserRepository.cs
│   └── LogRepository.cs
│
└── Infrastructure
```

---

# 36. MVP 第一版

第一版建議先完成：

```text
WPF 主框架

SQLite

首頁

任務管理

本機新增 / 修改 / 刪除

Google Sheet 同步

Alarm Worker

全螢幕警示

閱讀確認碼

System Tray

紀錄頁
```

---

# 37. 第二階段

加入：

```text
管理者登入（單機技術設定，見 15、19 節）

兩層角色（一般使用者 / 管理者）

設定鎖定

Audit Log

Sheet A／Employees 例外設定人員不同通知模式
```

---

# 38. 第三階段

可擴充：

```text
REST API

中央管理 Server

多電腦裝置管理

Web 管理後台

AD / Entra ID SSO

部門群組

裝置群組

推播

報表

通知送達率

簽收紀錄集中化（見 20.3 節）：解決「如何安全集中」的問題後，開放 AcknowledgementLogs
  同步至內網主機或雲端，據此才能做通知送達率／簽收率的跨裝置彙整報表；現階段已在表結構預留
  SyncedAt / SyncStatus 欄位（見 21 節），待此階段實作對應的同步服務
  公司現有由 IT 管理的 Linux 主機是目前最可行的落地目標（見 20.3 節方向 A），
    「中央管理 Server」很可能就是這台主機逐步演進而來，不一定需要另外採購新設備
```

---

# 39. 使用情境

## 情境 A：一般公司提醒

```text
17:55

Google Sheet 新增：

18:00
每日進度確認
```

程式同步。

18:00：

```text
全螢幕通知
```

使用者閱讀：

```text
R7K2-419
```

輸入後：

```text
完成簽收
```

SQLite：

```text
AcknowledgedAt = 18:01:17
```

---

## 情境 B：例外設定人員（例如主管）

同一通知原始設定：

```text
Level = 最高級
```

該使用者的例外設定（Sheet A／Employees，見 19.1，以自訂名稱或裝置代碼為鍵值）：

```text
Name = 王小明（該主管於 19C 節首次啟動時填寫的自訂名稱）
MaxAllowedLevel = 高級
```

結果：

```text
此使用者實際只會看到「高級」（60% 螢幕、點確認），不會強制紅黑全螢幕
```

其餘一般使用者仍依任務原始設定收到最高級全螢幕通知。

---

# 40. 專案成功標準

第一版可視為完成的條件：

* Google Sheet 可正常同步
* 網路中斷仍可使用 SQLite 任務
* 本機任務可 CRUD
* 到時間必定觸發通知
* 強制通知不可直接按 X 關閉
* 必須完成閱讀確認才能簽收
* 簽收結果有紀錄
* 管理員可鎖定關鍵設定
* 不同角色可以有不同警示政策
* 程式可長時間在 System Tray 穩定運作

---

# 41. 核心價值

Cloud Alarm Overlay 的核心並不是「提醒得更吵」，而是：

> **讓重要通知真正被看到、被確認，並且留下可追蹤的紀錄。**

一般通知解決：

```text
我有沒有通知你？
```

Cloud Alarm Overlay 要進一步解決：

```text
通知有沒有送達？

使用者有沒有看到？

什麼時候確認？

什麼通知必須強制提醒？

哪些設定只能由公司管理？
```

這會是整個系統後續設計、資料模型與權限架構的核心。

## 提醒文字與 Markdown（2026-09-15 更新）
- 兩個選填欄位為「詳細內容」與「補充說明及備註」，跨欄直接展開；通知有內容才顯示。
- 用於簡短提醒與項目整理：詳細內容最多 1,000 字，補充說明及備註最多 500 字，包含 Markdown 符號。
- 每格提供「純文字／MD 格式／預覽」三種模式，共用同一份文字，不會切換後遺失內容。
- 純文字：直接輸入原文。MD 格式：提供粗體、斜體、項目列表、編號列表、引用、連結與常用 emoji 插入按鈕。預覽：顯示實際排版。
- 純文字模式不會自動移除既有 Markdown 符號；通知與預覽使用相同 Markdown 解析方式。
- 支援標題、空白行分段、行末兩空格換行、粗斜體、等寬文字、分隔線、列表、引用、程式碼區塊、HTTP/HTTPS 連結、Unicode emoji 與 :smile: 等 emoji 短碼。
- 支援文字型 HTML（例如 abbr 的 title 滑鼠提示、b、i、br）；不執行腳本、不嵌入網頁、不套用任意 HTML/CSS。
- 不支援圖片，不讀取圖片檔案或下載圖片；圖片語法僅保留替代文字。
- SQLite Description/Note 欄位與既有資料保留，不需要 schema migration。
- Markdown 解析使用 [Markdig](https://github.com/xoofx/markdig)，轉為原生 WPF 文字元件呈現。

## 常用 emoji 與待辦方框
- 設定新增「常用 emoji 倉庫」：每行一個，可新增、刪除、調整順序、恢復預設；按「儲存 emoji」後寫入本機 Settings，重啟保留。最多 60 項。
- 任務兩格的 MD 工具列共用此清單，以選單插入游標位置。
- 新增「☐ 待辦」「☑ 已完成」「標題」「等寬」快捷按鈕。
- 支援 - [ ] 與 - [x]（工具列與勾選操作寫入小寫 x）。
- 在任務編輯器的預覽中，可以滑鼠點選或鍵盤操作方框；狀態回寫 Markdown 原文，按「儲存提醒」後才保存，取消則不保存。
- 通知中的方框只呈現狀態。子項目的勾選不會自動代表提醒已簽收，不建立另一套任務紀錄；雲端任務仍唯讀。
- 程式碼區塊內的方框語法保留原文，不變成可勾選項目。
